# SlimeFun4.1 v5.0.0 全量审计追踪

/loop 驱动（每分钟一轮）的全项目审计。每轮覆盖不同方面，本文件记录已覆盖范围、发现与处置，避免重复劳动。

## 审计维度总览

| 维度 | 状态 |
|---|---|
| 物品修改类事件（dup/吞没/NPE） | ✅ 第 1 轮（货运接线） |
| 数值修改类事件（边界/溢出） | ✅ 第 2 轮 |
| 命令与参数解析（不可信输入） | ✅ 第 3 轮 |
| GUI/菜单点击（客户端可控数据） | ✅ 第 3 轮（并入） |
| 玩家交互监听器（右键/弓/工具） | ✅ 第 8 轮 |
| 并发与异步（ticker/auto-save/缓存可见性） | ✅ 第 4 轮（事件异步声明） |
| 高负载稳定性（无界增长/缓存清理/日志风暴） | ✅ 第 5 轮 |
| 领地保护闭合（新增事件路径的权限校验） | ✅ 第 6 轮 |
| 数据持久化（BlockStorage/PlayerProfile 损坏容错） | ✅ 第 7 轮 |
| 附属 API 误用容错（事件 setter 校验一致性） | ✅ 第 8 轮（向量 NaN 专项） |

## 第 1 轮（2026-08-13）：物品修改类事件 — 货运接线

**范围**：全部带 ItemStack setter 的事件类校验一致性排查 + CargoNetworkTask 物品流端到端审计。

**排查结果（无问题）**：
- 约 190 个事件带 setter；物品类 setter 几乎全部有 `Validate.notNull`/`Objects.requireNonNull`；`AndroidFarmEvent.setDrop` 的 `@Nullable` 是有意设计（文档注明）
- 基座 place/take 接线：消耗手物品→展示替换物→取回替换物，闭环无 dup；取消语义正确
- 货运撤回/插入接线：取消退回来源槽、替换物分发、异常时物品回退（try/catch + insertItem 兜底），无 dup/吞没

**发现并修复**：
- `fix(cargo)`（8e7518c）：`CargoItemInsertEvent/WithdrawEvent.setItem(AIR)` 语义依赖落地路径——vanilla 容器路径把空气"插入"空格视为传输完成（物品凭空消失），其它路径退回来源，行为不一致。统一为：替换物为空气/空栈 = 销毁物品。补 Javadoc。
- 新增 `TestCargoNetworkTaskItemFlow` 7 项端到端接线回归（同包反射直驱 routeItems；CargoNet mock 掉 attached block/filter/regulator，物品流走真实代码与箱子库存）

**遗留观察（后续轮次）**：
- ~~基座 `placeItem`：setItem(AIR) 会先消耗手物品再尝试生成空气展示实体~~ → 第 8 轮已归一化（769df11：与货运同约定=销毁手物品、不生成展示实体，消除不可移除的损坏展示状态）
- `SmeltersPickaxeSmeltEvent`/`SlimefunMobDropEvent` 等 setter 仅 notNull、未拒空气——确认各落地路径对空气的行为是否一致

## 第 2 轮（2026-08-13）：数值修改类事件 — 边界/溢出校验

**范围**：两批共约 40 个事件的数值/向量/对象 setter 校验一致性 + 消费侧循环与算术安全性。

**排查结果（无问题）**：
- 绝大多数 setter 有领域对齐校验（EnergyConsumeEvent 封顶 maxTransfer、InfernalBonemealGrowEvent 封顶 maximumAge、CargoNodeChannelChangeEvent 限 0-15、BackpackCreateEvent 限 9-54 的 9 倍数等）
- `AnimalAccelerateEvent.setAgeBoost` 无校验系**有意设计**（Javadoc 注明负值使动物返幼）
- `MagicEyeOfEnderLaunchEvent.setVelocity(null)`/`StomperBootsPushEvent.setPushVelocity(null)` 落地路径均有 null 分支回退默认行为，@Nullable 语义成立
- Crop/Animal 加速器消费侧有 Math.min 钳制或 setAge 语义安全

**发现并修复**：
- `fix(events)`（84e3fa0）：`TreeAccelerateEvent.setGrowthBoost` 仅校验非负，消费侧按 boost 循环 `applyBoneMeal`（tick 线程），异常大值卡死服务器 → 新增 `MAX_GROWTH_BOOST=100` 上限校验
- 同提交：`StomperBootsPushEvent.setDamage` 无校验，NaN/无穷伤害写入实体生命状态 → 拒绝非有限值

**遗留观察（后续轮次）**：
- ~~向量类 setter 未校验 NaN 分量~~ → 第 8 轮已加固（b149c7f：6 个向量 setter 统一拒绝 NaN/无穷分量，两个 @Nullable 仅非空时校验）
- `StomperBootsPushEvent.setDamage` 负值未拒（Bukkit 语义下近似无效），暂保留

## 第 3 轮（2026-08-13）：不可信输入 — 命令、文本输入、GUI 点击

**范围**：全部 16 个子命令的参数解析、聊天文本输入路径（电梯楼层重命名/全息投影文本/机器人脚本上传/传送点命名）、菜单点击调度（MenuListener/BlockMenuPreset）、新增 SlimefunItemWorkstationEvent 接线。

**排查结果（无问题）**：
- Give/Backpack/PlayerProfile.getBackpack 数字解析溢出防护为 4.9.4 已加固项；ResearchCommand 控制台/权限门控正确
- 命令层在本分支零改动（git diff 确认）；菜单点击调度链（MenuListener/ChestMenu/BlockMenuPreset/ChestMenuUtils）在本分支零改动，维持上游久经考验状态
- 电梯/全息文本输入长度受聊天协议上限（~256 字符）约束；两事件 setter 均有 notNull/有限值校验
- Script.upload 文件路径仅含服务端可信分量（安卓类型枚举 + 玩家名[Mojang 字符集受限] + int id），用户输入只进配置值不进路径，无路径穿越
- dough ChatInput 的 handlers map 有 quit/kick 清理，无断线泄漏；同玩家连续多次 awaitInput 不输入会堆积（下一条聊天触发全部回调），上游 dough 行为，接受
- AnvilListener 新事件接线正确：默认拒绝（上游行为）、事件取消才放行；guide 物品无 SlimefunItem 不发事件但仍拒绝（有意设计）

**发现并修复**：
- `fix(gps)`（f8af49f）：传送点名字仅由颜色代码组成（如 "&k"）时净化 ID 为空字符串，以空 YAML 键写入存档 → 拒绝并新增 `gps.waypoint.invalid-name` 本地化键（en/zh-CN）+ 回归测试

**遗留观察（后续轮次）**：
- TeleporterCommand/BackpackCommand 的 getOfflinePlayer(String) 同步 Mojang 查询（4.9.4 已记录为接受的权衡，权限门控）

## 第 4 轮（2026-08-13）：并发与异步 — 事件线程声明契约

**范围**：全量 ~200 个新事件的触发点线程上下文 vs `Event` 构造器的 async 声明。

**线程模型核实**：TickerTask/机器 BlockTicker/盔甲三任务（SlimefunArmor/Radiation/RainbowArmor 均 runTaskTimerAsynchronously）/AutoSavingService 为异步；CargoNetworkTask 经 runSync 回主线程；玩家任务（Jetpack 等）与 AncientAltarTask/MiningTask 为主线程。

**发现并修复**：
- `fix(events)`（b362d6a）：45 个从异步 ticker 线程触发的事件以同步构造器声明，`isAsynchronous()` 误报 false → 统一自适应 `super(!Bukkit.isPrimaryThread())`（含 3 个 this() 委托构造器特判、2 个 PlayerEvent 双参形式）。此前仅 EnergyConsumeEvent 等 6 个事件有自适应声明，属遗漏
- 全量 3029 项测试通过、0 失败

**遗留观察（后续轮次）**：
- 事件在异步线程触发意味着**附属监听器也在异步线程执行**——属文档级契约（各事件 Javadoc 多数已注明 ticker thread），可在发布说明中统一提示
- BlockStorage/PlayerProfile 并发模型已在 4.9.4 加固（CME/纪元/dirty 可见性），本轮未复发

## 第 5 轮（2026-08-13）：高负载稳定性 — 每 tick 分配 / 无界增长 / 日志风暴

**范围**：全部新事件触发点的监听器守卫（`getRegisteredListeners().length > 0`）核查；新代码路径的无界集合增长；日志风暴面。

**排查结果（无问题）**：
- TrashCanVoidEvent 已有监听器守卫（TrashCan.java:73，包裹整个收集+触发段）；此前粗扫因守卫距 callEvent 超 8 行误报
- 其余无守卫触发点均为上游既有（MachineProcessor/FarmerAndroid/MinerAndroid 的旧事件，按操作完成触发而非每 tick）或灾难低频路径（ReactorExplodeEvent）
- CargoNetworkTask ownerCache 为每任务每 tick 局部、有界；MenuListener.menus 关界面即清；ItemFilter 失败重试有 MAX_FAILED_UPDATES=3 封顶
- 新代码零日志调用（api/events 包无 logger；分支改动文件中 3 处 "warning" 均为 Javadoc 文字），无日志风暴面

**发现并修复**：
- `fix(cargo)`（80eb524）：TrashCan 豁免匹配误用 `ItemStack#equals`——Bukkit equals 只比类型/数量/耐久、忽略 meta。两槽位放同型同量不同 meta 物品（如一把普通镐 + 一把命名镐），监听器赦免命名镐时，先扫描到的普通镐会"等于"赦免项被保留，**真正点名的命名镐反被销毁**（数据丢失）。修复：tick 内保留收集时的物品引用与槽位映射、按原引用身份匹配（监听器传入的正是这些引用），兜底用 `SlimefunUtils.isItemSimilar`（meta 感知）+ 数量相等覆盖防御性克隆。回归测试：meta 区分槽位赦免精确性
- 测试环境确认（已入记忆库）：MockBukkit 下 `ItemStack.isSimilar` 同样不区分 display name，物品比较一律用 SlimefunUtils.isItemSimilar

## 第 6 轮（2026-08-13）：领地保护闭合 — 新增事件路径的权限校验

**范围**：全部"改变世界"动作的新事件触发点 vs ProtectionManager 校验的先后次序、setter 是否可使已通过的校验失效、cancel veto 完整性（子代理全量核查 + git diff 确认本分支零删除/移动校验）。

**排查结果（OK）**：
- 机器人全系列（Mine/ChopTree/Farm/Attack/ItemDeposit/Refuel/Move）：校验一律在事件之前、逐目标（方块/实体）进行；事件无可改变被校验对象的 setter；cancel 正确跳过动作。无 owner 的旧机器跳过校验为 merge-base 已有历史兼容行为
- InfusedHopper（吸取前按物品位置校验）、基座 place/take（usePedestal 入口校验）、AutoBreeder（逐实体校验）、BlockPlacer（owner + PLACE_BLOCK）：均闭合
- 本就无需校验：FisherAndroid（不改世界）、SoulboundItemsKeepEvent（仅自身物品）、OilPump/GEOMiner（区块虚拟储量，GUI 已有 INTERACT_BLOCK）

**发现并修复**：
- `fix(blocks)`（41b8fd4）：`BlockPlacerPlaceEvent.setItemStack` 生效于 `isAllowed()` 之后——addon 可把已核准材料换成非方块（setType 抛异常）或 unplaceable-blocks 黑名单材料，绕过服务器配置。两个落地路径（vanilla/SF 方块）现对替换物重跑 isAllowed，不合格整体跳过、不消耗物品 + 回归测试
- `fix(events)`（b69b286）：`AsyncEntityAssembleEvent.setSpawnLocation` 零校验——null 世界/NaN/无穷坐标传入同步生成任务会损坏生成。setter 校验世界非空、坐标有限（跨世界重定向仍允许）。Wither 生成点重定向无领地校验属历史如此（Assembler 无 owner 记录），已记录

**遗留观察（后续轮次）**：
- AbstractEntityAssembler 无 owner 跟踪，生成动作天然无法做 ProtectionManager 校验；如需闭合需引入 owner 记录（功能变更，超本轮范围）
- GEOMiningCompleteEvent cancel 时产物被丢弃而非返还（设计选择，Javadoc 已注明）

## 第 7 轮（2026-08-13）：数据持久化损坏容错

**范围**：本分支改动文件中全部 BlockStorage/PlayerProfile/Config 读取解析点对损坏数据（缺键、null、类型错误）的容错（子代理全量核查 + diff 确认本分支未改任何解析逻辑）。

**排查结果**：本分支**零引入/零加重**——改动均为事件注入层，不触碰持久化解析；货运 channel/owner、安卓 fuel/index/rotation、全息 offset、经验收集器、Research 读取、waypoint 读取侧等容错均良好（安全回退或 per-entry 隔离）。

**发现并修复（3 条上游既有数据丢失 BUG，loop 指令要求修复一切数据操作 BUG）**：
- `fix(assembler)`（47b07cb）：AbstractEntityAssembler 的 offset 键缺失时 `Double.parseDouble(null)` 抛 NPE 绕过 NumberFormatException catch，且解析在 consumeResources 之后——每个失败 tick 白吃整套材料+电量，连续 4 次报错被 TickerTask 摧毁机器。修复：解析前置、null/NFE 均安全跳过不消耗；GUI 两处读取统一走带回退默认值的 getSpawnOffset
- `fix(blocks)`（492a627）：BlockPlacer 的 owner 字段为非 UUID 字符串时 `UUID.fromString` 抛异常且发生在 setCancelled 之前——退化为原版发射器、物品以弹射物损失。修复：解析失败与 owner 缺失一致失败关闭
- `fix(storage)`（7d6d60d）：LegacyStorage 背包 size 非数字时 getInt 返回 0 → 按空背包加载 → 下次保存清空内容（连锁丢数据）。修复：size<=0 时从最高内容槽位推断尺寸（9 的倍数、封顶 54）；无内容可推断则跳过并告警
- 附带 `docs(research)`（7d8b85b）：meetsDependencies javadoc 与代码相反——profile 未缓存时实际返回 false（失败关闭，安全侧），以代码为准修文档

**遗留观察（后续轮次）**：
- ~~ItemFilter 的 filter-type 损坏时被静默当作黑名单~~ → 第 8 轮已修复（b22a014：非白/黑名单值抛 IllegalStateException 走既有 fail-closed 通道；缺失值保留历史黑名单默认）

## 末轮（2026-08-13）：全量测试回归 + 汇总

- 全量 **3035 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）；测试数较第 4 轮 +6（TrashCan meta 赦免、BlockPlacer 非法替换/损坏 owner、Assembler 损坏 offset、LegacyStorage 损坏 size ×2）
- 7 轮审计合计修复 10 项缺陷（3 项上游既有数据丢失 BUG）+ 1 项文档纠正，另有若干遗留观察已记录于各轮
- note/release/5.0.0.md 已补"发布前全量审计"一节并更新测试计数

## 第 8 轮（2026-08-13）：玩家交互监听器（右键/弓/工具/食物/装备/护身符）

**范围**：约 50 个玩家交互驱动的新事件——消耗/冷却/耐久扣除与事件触发次序（取消是否完整回滚）、setXxx 修改后原校验是否仍成立、高频连点/双持 dup、客户端可控数据信任面（子代理全量核查 + diff 确认）。

**排查结果（OK）**：绝大多数事件 veto 在消耗之前且完整；clicked block/face 均经 Bukkit 服务端校验；VeinMining/LumberAxe 的 blocks 列表被 addon 修改后落地逐块重跑保护校验（正面范例）；SoulboundRune/EnchantmentRune 延迟任务重检实体有效性无 dup；食物/医疗类 setter 校验齐全。

**发现并修复（本分支引入 4 项 + 待确认 2 项 + 上游 1 项）**：
- `fix(staff)`（68c032e）：WindStaffLaunchEvent 取消仍扣饥饿——扣饥饿在事件触发之前，与 Javadoc 承诺相悖；移到 veto 之后
- `fix(runes)`（7ba9823）：EnchantmentRuneApplyEvent.setLevel 越界 → 落地先删实体后 addEnchantment 抛异常，物品+符文双双吞没；setter 按 getMaxLevel 校验 + 落地先附魔后删实体
- `fix(events)`（823e976）：VampireBladeHealEvent.setHealAmount 无校验——负值可致 setHealth(<0) 异常甚至杀死玩家；拒绝负值与非有限值
- `fix(events)`（1901dd4）：StormStaffStrikeEvent.setLocation 拒绝无世界/非有限坐标；Javadoc 注明重定向不重跑距离/保护校验
- `fix(gadgets)`（e8edd9b）：MultiTool 篡改 NBT 的模式索引越界 IOOBE → 回退首模式
- `fix(weapons)`（509368d）：ExplosiveBow 受影响实体列表容忍 addon 加入的非生物实体（原强转 CCE）
- `fix(armor)`（c1b92f5，上游既有）：StomperBoots 击退在保护校验之前——保护区内玩家仍被击退；先校验后击退
- `fix(events)`（b149c7f）：6 个向量类 setter 统一拒绝 NaN/无穷分量（第 2 轮遗留专项）

**另有本轮回合内完成的遗留修复**：基座 setItem(AIR) 归一化（769df11）、ItemFilter filter-type 损坏失败关闭（b22a014），均已销记于第 1/7 轮遗留清单。

## 第 9 轮（2026-08-13）：玩家资产与进度数据系统（背包/研究/灵魂绑定/知识典籍）

**范围**：直接读写玩家持久化数据的新事件路径——背包开关/创建/扩容/限制、研究解锁链（费用/时长/依赖）、灵魂绑定保留与取回、知识典籍与经验瓶、PlayerProfileUnload 脏数据面。重点 dup/吞没/并发窗口（子代理全量核查 + diff 确认）。

**排查结果（OK）**：背包打开事件在 put/open 之前 veto 完整、扩容缩小前越界槽位非空检查物品不丢、BackpackRestrictionEvent 各分支对称豁免无 dup；灵魂绑定 keep 的 excludeWhen 豁免在存储/剔除/cursor 三处同一判定无"既保留又掉落"；取回先取后给无双倍返还；典籍绑定/分享 veto 完整；unload 事件异步声明正确且保存失败的 profile 不被卸载。

**发现并修复（本分支引入 3 项）**：
- `fix(research)`（5bc37d1）：ResearchCostEvent 附加费超过玩家等级 → setLevel 负经验（或吞掉全部等级）且照常解锁。扣费前复核调整后费用、负担不起拒绝解锁；Javadoc 注明折扣方向受基础费用闸门限制
- `fix(magical)`（f66b4d8）：KnowledgeFlaskFillEvent.setLevelCost 无上界 → 负经验。扣费前复核，负担不起失败关闭（不扣/不产/不耗）
- `fix(research)`（912efc7）：setResearchTimeTicks 无上限 → 极端值研究软锁（currentlyResearchingPlayers 永不清理）；超长时长 + 玩家中途退出 → 解锁落在孤儿 profile 研究丢失且经验已扣。封顶 72000t + 延迟解锁时 PlayerProfile.get 重解析

**上游既有（第 10 轮已全部修复）**：
- ~~BackpackListener 两段式开包竞态~~ → 9ad72fc80：先 backpack.open 后 backpacks.put，打开时触发的关闭事件不再移除新映射
- ~~PlayerBackpack.setSize 在玩家观看时换 Inventory~~ → a076da49f：换实例后把观看者 close+open 重定向到新 Inventory
- ~~ResearchUnlockEvent 取消不退还已扣等级~~ → 9fa97e80f：Research.unlock 新增 cancelHandler 重载，guide 路径取消即退还实际扣除的等级
- ~~KnowledgeTome 分享在来源 profile 加载失败时典籍已消耗研究未授予~~ → e2f17cbfd：两侧 profile 均挂 failureHandler，失败退还典籍副本（新增 messages.error-occurred 语言键）
- ~~SoulboundListener 取回：setItem 覆盖他插件物品、addItem 剩余量被忽略致丢失~~ → 1a5fcb2bb：槽位被占时改为合并入背包，放不下的余量掉落在玩家脚下

**设计问题（2026-08-13 已经用户定夺）**：
- ResearchCostEvent 折扣方向受 canUnlock 基础费用闸门限制 → **用户决定维持现状**（Javadoc 已注明限制，零改动）
- Research#addDependency 绕过路径 → **用户决定在 unlock 统一强制**（第 11 轮实施）

**第 9 轮回归结果**：全量 **3044 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）；测试数较第 8 轮 +2（知识瓶附加费拒绝、研究附加费拒绝解锁）。收尾修正：`e3fbd71f9` 补 KnowledgeFlask 缺失导入（编译错误）、`a5f8ff9bc` 修正 TestResearchCostEvent 的 profile 获取（awaitProfile 对同一玩家不可重复调用，改 PlayerProfile.find）与物品 id 冲突（三测试共用 id 改 40/50/60）。

## 第 10 轮（2026-08-13）：第 9 轮上游既有遗留清账

**范围**：第 9 轮记录在案的 5 项上游既有数据丢失缺陷，逐项修复 + 回归测试。

**修复（5 项，均上游既有）**：见上方销记清单（1a5fcb2bb / 9ad72fc80 / a076da49f / e2f17cbfd / 9fa97e80f）。

**测试**：新增 5 项回归（灵魂绑定取回不覆盖占位物品/放不下掉落、开包越过其他库存视图追踪存活、扩容观看者重定向、研究取消退款）。MockBukkit 实证 `openInventory` 先 `closeInventory()` 发关闭事件，竞态可端到端复现。

**回归**：全量 **3049 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）。

**仍遗留**：
- KnowledgeTome 失败退款路径本身无测试（MockBukkit 无法模拟 profile 加载失败，成功路径由 TestKnowledgeTomeShareEvent 7 项覆盖）
- GEOMiner 取消丢弃产物（设计选择，已记录）；AbstractEntityAssembler 无 owner 追踪（特性变更，超范围）
- 待用户定夺：ResearchCostEvent 折扣方向语义；addDependency 绕过路径（典籍分享//sf research all/research.unlock()）

## 第 11 轮（2026-08-13）：研究前置依赖统一强制（用户定夺项实施）

**背景**：第 9 轮记录的两个设计问题经用户定夺——折扣语义维持现状；addDependency 的绕过路径在 unlock 统一强制。

**实施**：
- `feat(research)`（0a8428103）：PlayerResearchTask.accept 在解锁前统一校验 meetsDependencies——知识典籍分享、/sf research all、research.unlock() 全部 funnel 于此，不再绕过研究树；拒绝时走 cancelHandler（与取消退款同通道），guide 路径已扣费用可获补偿。addDependency Javadoc 同步更新
- `fix(commands)`（ad5cd1277）：/sf research all 改造为依赖感知多趟解锁——即时解锁实际落库在延迟同步任务里，单趟循环会因前置尚未落库而误拒依赖方；每趟仅解锁前置已满足的研究（每个研究最多尝试一次，被 addon 否决的不重试），下一 tick 重查直至无进展

**测试**：新增 3 项（直调 unlock 依赖强制、拒绝时 cancelHandler 补偿、/sf research all 含依赖全解锁）。

**回归**：全量 **3051 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）。

**行为变更提示（发版须知）**：/sf research <player> <research> 单个给予在依赖未满足时现在会被静默拒绝（统一强制的代价，经用户确认）；知识典籍分享只复制前置已满足的研究。

## 第 12 轮（2026-08-13）：传送与 GPS 系统

**范围**：传送序列（TeleportationManager/PortableTeleporter）、电梯（ElevatorPlate/Floor/Rename）、传送点持久化、GPS 发射器状态/复杂度事件、传送卷轴。子代理全量核查 + 逐条 file:line 证据复核。

**发现并修复（7 项，含 2 项上游既有 + 1 项第 4 轮遗漏）**：
- `fix(events)`（e6bdfdf4a）：GPSNetworkComplexityEvent 无自适应异步声明——异步 ticker 线程触发即抛 IllegalStateException 刷异常（第 4 轮 b362d6a 的遗漏项）；Javadoc"同步触发"描述一并纠正
- `fix(gps)`（2b004a432）：TeleportationStartEvent.setDestination 不校验无世界/非有限坐标，且 teleporterUsers 认领在耗时计算之前——监听器设 null 世界目的地 → distanceSquared NPE → 认领残留 → 玩家永久打不开传送器 GUI（软锁）。setter 校验 + 认领移到计算之后
- `fix(teleporter)`（b80a5fbcd）：PortableTeleporter 先扣电后开 GUI——传送读条中（最长 50 秒）右键白扣 10 电（上游既有）。新增 TeleportationManager.isTeleporting 查询，先查后扣
- `fix(elevator)`（800b22568）：ElevatorTeleportEvent.setFloor 不拒跨世界楼层——目的地按玩家世界拼 XYZ，跨世界重定向落到未校验坐标且 users 条目泄漏；renameFloor 在电梯板被挖后仍写楼层数据成幽灵 BlockStorage（上游既有）
- `fix(events)`（09d84ec97）：TelepositionScrollRotateEvent.setNewYaw 不拒 NaN/∞
- `fix(storage)`（66a537934，上游既有多世界数据丢失）：世界未加载的 waypoint 读取时被跳过、下次保存被 clear() 永久抹掉——现捕获原始 YAML 条目入 PlayerData.unresolvedWaypoints，保存时原样回写（同名 id 被已解析 waypoint 占用时让位）

**确认无问题**：openTeleporterGUI 认领/打开原子化与 profile 失败不泄漏；死亡/下线/移动中止路径正确移除 teleporterUsers 并发 AbortEvent；GPSNetwork.addWaypoint 空 id/净化/重名/死亡点防碰撞齐全；WaypointRemoveEvent veto 完整。

**记录在案（设计层，未动）**：传送/电梯目的地无窒息/虚空/领地安全检查（上游一贯设计）；teleportationTime 覆写 >100 时实际时长封顶约 50 秒（speed 粒度 1）；GPSNetwork.cachedComplexity 只增不删（量小，仅注册监听器时维护）。

**测试**：新增 6 项回归（setDestination 校验、复杂度事件线程声明、跨世界 setFloor、幽灵楼层数据、waypoint 未加载世界保留、isTeleporting），1 项原地扩充（setNewYaw NaN/∞）。

**回归**：全量 **3057 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）。

## 第 13 轮（2026-08-13）：自动保存与数据落盘管线

**范围**：AutoSavingService（间隔校验/单世界异常隔离）、PlayerProfile.save 并发、LegacyStorage 背包段清理与原子写、BlockStorage/DirtyChestMenu 原子写、落盘相关事件的异步声明。

**发现并修复（9 项）**：
- `fix(services)`（24ff1c0）：AutoSavingService.start 对非法间隔（0/负数）不校验——0 导致每 tick 全量保存刷爆磁盘日志，负数导致自动保存静默失效；现回退默认 10 分钟并 WARN。saveAllBlocks 单世界 computeChanges/save 异常会杀死整个定时任务（之后所有世界与玩家数据永久不落盘）——现逐世界 try/catch 并统计成功世界数供 SlimefunBlockDataSaveEvent 使用
- `fix(player)`（cb72ca2）：PlayerProfile.save 无锁——自动保存线程与手动 /sf save 并发时两个线程写同一临时文件互相截断；markDirty 先置脏标记后增纪元，竞态窗口内 epoch 快照可能晚于修改而漏存——现 per-profile saveLock 同步 save 主体，markDirty 先增纪元后置脏标记
- `fix(storage)`（9609fc0）：LegacyStorage 保存背包前不清空 backpacks 段——已删背包的 YAML 段残留，下次加载复活（物品复制）；原子写（LegacyStorage/DirtyChestMenu）用 Config.save 吞 IOException——写一半失败的临时文件能通过 exists() 检查原子替换好文件——现改 Files.write + 失败即删半成品
- `fix(blocks)`（e0475a1）：BlockStorage 同款原子写加固；删除 .sfb 文件失败（IO 占用等）时静默跳过——已删方块下次加载复活（幽灵方块）——现失败重新入队待下轮重试
- `fix(events)`（827059a）：WaypointRemoveEvent/BackpackResizeEvent 无自适应异步声明（异步 profile API 路径触发即抛 IllegalStateException）；PlayerProfileUnloadEvent 注释"异步 ticker 线程"纠正为"异步自动保存线程"

**测试**：新增 TestAutoSavingService（4 项：非法间隔钳制 ×3 参数化 + 合法间隔保留）；TestLegacyBackend +2（未加载世界 waypoint 原样回写、已删背包不复活）；TestBackpackResizeEvent +2（观看者重定向到扩容后 Inventory、异步声明）；TestGpsApiEvents +1（WaypointRemoveEvent 异步声明）。

**记录在案（容忍，未动）**：自动保存线程读取玩家打开的背包库存视图（读时不加锁，上游一贯容忍——读到的 transient 视图最坏保存出稍旧内容，不造成数据丢失）；监听器异常路径（Bukkit 层既有行为）。

**回归**：全量 **3064 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）。

## 第 14 轮（2026-08-13）：可编程机器人脚本系统

**范围**：ProgrammableAndroid 脚本解析/执行循环、链式移动数据迁移、屠夫/砍树/矿工/渔夫变体、脚本上传/下载/评分、DirtyChestMenu.pushItem。子代理全量核查（14 项发现）+ 逐条 file:line 证据复核。

**发现并修复（11 项，含 2 项高危）**：
- `fix(androids)`（5d37875）：**链式移动竞态（高危）**——两台机器人同 tick 相向移动时，物理块即时变化但数据迁移经 ConcurrentHashMap 延迟队列（无序、无条件覆盖目标）：一序下 android2 的数据/库存被 android1 覆盖（物品销毁+跨玩家库存转移），另一序下 android1 的头被物理删除且数据滞留成幽灵。修复双管齐下：tick 在目标位置有待入库迁入数据时跳过（防 tick 到别家机器人的头）；move 拒绝目的地存在待处理迁入/迁出移动（TickerTask 新增 isMovingFrom）。未知指令 token 与类型不匹配指令（setScript/篡改数据可达）原永久停摆且每 tick 烧燃料+刷警告——现跳过推进。NaN/Infinity 燃料（parseFloat 不抛异常）原为无限燃料——现失败关闭按 0 处理。"index" Integer.MAX_VALUE 溢出为负原抛 AIOOBE 致方块被 4 错终结——现回退 0。空桶（液体燃料）与 addItems 余量原静默吞没——现自然掉落
- `fix(androids)`（e8533d5）：**屠夫标记泄漏（高危）**——击杀标记在 damage 前设置，实体未死则标记残留：他人之后在自己领地杀死该家畜，掉落物跨领地真空吸入机器人（无保护复检）。现伤害结算后立即移除标记（致死时 EntityDeathEvent 已在 damage 内同步消费）。砍树机器人对无权砍伐的树原每 tick 播效果+烧燃料永久停摆——现放弃该树推进脚本（与矿工失败穿透一致）。矿工/渔夫输出槽满余量自然掉落
- `fix(androids)`（0900dd6）：脚本上传文件名嵌入原始玩家名——离线服伪造名可路径穿越写出 scripts 目录，改名后同名玩家覆盖前人脚本。现以 UUID 为文件名键（加载不解析文件名，旧文件兼容）。Script.rate 写入侧防重复投票（原仅点击侧预检）
- `fix(inventory)`（7ed0762）：pushItem 合并量小于槽位空余时按全额空余扣减，输入栈被改为负数（调用方地雷）

**确认无问题**：挖掘/攻击/砍伐/存放/加油/移动的领地保护均为每次执行时校验（非编辑时）；owner UUID 解析失败安全；脚本格式三要素校验齐全；ticker 每块异常隔离与 4 错终结；存放/加油物品守恒；canOpen/onBreak 权限完整；AndroidScriptChangeEvent veto 不触碰存储脚本。

**记录在案（设计层，未动）**：脚本上传无数量配额（主线程同步写盘，刷上传可致磁盘压力）；脚本下载器每次打开主线程全量读盘解析（滞后放大器）；ownerless 机器人 move 对领地失败开放（注释声明的遗产行为，与其他动作失败关闭不一致）；燃料接口接受任意物品可卡死燃料槽；砍树补种与砍伐同位置（破坏权限已覆盖该位置，未另验 PLACE_BLOCK）；FarmerAndroid 对未成熟作物也发事件（监听器噪音）。

**测试**：新增 TestAndroidScriptExecution（9 项：未知/类型不匹配 token 跳过、NaN/Infinity 燃料、索引溢出、迁入中跳过 tick、迁入/迁出目的地拒绝移动、无权砍树放弃、幸存实体不留击杀标记）、TestScriptUpload（2 项：UUID 文件名+加载回读、重复评分拒绝）、TestPushItem（3 项：部分合并不腐蚀输入栈、溢出余量正确、空槽推入）。

**回归**：全量 **3078 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）。

## 第 15 轮（2026-08-13）：货运网络（Cargo Network）

**范围**：CargoNet 频率解析与节点缓存、CargoUtils 撤回/插入与 preset 钩子交互、CargoNetworkTask 回流路径、过滤器脏标记、网络生命周期事件线程声明。子代理全量核查（9 项发现）+ 逐条 file:line 证据复核。

**发现并修复（9 项）**：
- `fix(cargo)`（2f97bbc）：损坏 frequency（NBT 篡改可达）双重危害——数字但超 int 范围时 parseInt 抛 NumberFormatException，经 ticker 4 错终结机制**摧毁货运管理器方块**（连累整个网络）；非数字时静默改道 0 号频道（容器被抽进错误目的地）且每个货运 tick 刷一条 SEVERE（永久日志风暴）。现统一失败关闭返回 -1（输入/输出映射双双跳过该节点），每节点只报一次警。节点移除时驱逐 filterCache/roundRobin 条目（原只增不删，节点高更替的长存网络无界泄漏）
- `fix(cargo)`（859712c）：**preset onItemStackChange 钩子否决/缩减提交与货运的交互（复制/吞没）**——撤回路径不验提交结果：钩子否决（第三方机器拒被抽取）时物品留在槽里而货运照样拿走一份，每 tick 复制；插入路径假定提交值即传入值：钩子缩减时余量被吞。现撤回/插入/合并三条路径均读回实际提交量，只承认真实发生的部分。模板撤回 vanilla 变体 setAmount(0) 留 0 数量幽灵栈——改 setItem(null)
- `fix(cargo)`（0a1007d）：监听器在货运事件中同步拆掉容器时，回流写入每 tick 缓存的已死 Inventory 对象，物品静默吞没——现回流前复检容器存活。机器菜单回流原槽被占直接落地（不对称于 vanilla 分支先试其它槽）——现先 pushItem 进 INSERT 槽族。spawnItem 被插件否决时溢出物品吞没——兜底自然掉落
- `fix(events)`（a610d9f）：NetworkCreateEvent/NetworkDisposeEvent 可由异步货运 tick 线程触发但声明同步——补自适应异步声明
- `fix(cargo)`（9e018cc）：ItemFilter.dirty 异步 ticker 写/主线程货运任务读，补 volatile

**确认无问题**：撤回-分发-回流管线保证已撤回物品必有归宿（分发异常/全目的地拒收/事件否决均回源，容器消失则落地+警告）；部分栈/满目的地合并数学正确；轮询索引无差一错误（逐步推演验证）；事件 veto 语义齐全（setItem 空气=销毁在撤回/插入对称归一）；频道 GUI 与事件层 0-15 校验、越界对称丢弃；输入/输出双侧领地保护按次校验、owner 解析失败安全；潜影盒套娃防护与熔炉/酿造台槽位范围正确。

**记录在案（设计层，未动）**：无所有者（损坏数据）货运节点失败开放工作（注释声明的遗产兼容行为）。

**测试**：新增 TestCargoFailsClosed（6 项：损坏频率回退/缓存驱逐/幽灵栈/钩子否决撤回不复制/否决插入全量回运/缩减插入只认实收）；TestCargoNetworkTaskItemFlow +1（事件中拆容器→物品落地不吞没）；TestNetworkCreateDisposeEvent +1（异步声明）。

**回归**：全量 **3086 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）。

## 第 16 轮（2026-08-13）：能源网络（Energy Network）

**范围**：EnergyNet 结算管线（发电机池化/消费者分配/余量回存）、EnergyNetComponent 充放电 API、EnergyRegulator 网络解析、ChargingBench 能耗核算。7 项发现逐条 file:line 证据复核后修复 6 项，1 项架构层记录在案。

**发现并修复（6 项）**：
- `fix(energy)`（e15b08c）：**被取消/异常的发电机存储电量被结算清零（高危）**——tickAllGenerators 把存储电量池化进事件可见值后，EnergyGenerateEvent 取消时 contributed=0（存储电量未入 supply），而 storeRemainingEnergy 无条件重写所有发电机为余量份额或 0，这部分电量被直接抹除；与 EnergyGenerateEvent 文档契约（"its stored charge is unaffected"）直接矛盾。异常路径同理（发电机留在 map 中却被重写为 0）。现 tickAllGenerators 记录未汇入位置（取消且可储电 / 抛异常），结算跳过它们
- `fix(energy)`（cac7a8a）：getCharge 只捕 NumberFormatException——负值（NBT 篡改/崩溃写可达）解析正常流入 tickAllCapacitors 使全网 supply 为负，结算把所有电容与可储电发电机清零（全网擦除）；超容量值白送一个 tick 的电量。现钳制到 [0, capacity] 失败关闭。setCharge/addCharge/removeCharge 向已删除方块（无 id 的空 Config）写入会造出无 id、网络不可见、永不清理的幽灵 BlockInfo 记录——三个入口均加守卫直接返回
- `fix(energy)`（4f7bd35）：**双调节器网络并入时互相锁死**——EnergyRegulator.tick 按位置取首个候选网络：两网被电缆接通后互相包含对方调节器位置，A 块 tick 拿到 B 网（regulator 不匹配拒绝结算）、B 块 tick 拿到 A 网（同样拒绝），两张网同时停摆。现优先取 regulator 即本方块的候选网络，仅首 tick 无注册网络时回退位置解析
- `fix(energy)`（8f3094a）：ChargingBench 部分充入白扣整次能耗——addItemCharge 按物品上限截断（如 9.5/10 时只充入 0.5J），原仍 removeCharge 全额能耗，未交付电量凭空消失。现按充入前后差值以 2:1 默认比率结算，充满移出输出槽不耗能

**记录在案（架构层，未动）**：同步机器（isSynchronized=true 的 main-thread tick）与异步 EnergyNet 结算对同一 energy-charge 的读-改-写存在每 tick 级丢失更新竞态（check-then-act 跨线程）。根治需统一充放电线程模型或引入原子 RMW API，属上游架构决策，本轮不动。

**确认无问题**：flowSafeAddition 全程防溢出；setCharge 校验非负并钳容量；消费者逐组件异常隔离（单组件损坏不中断结算，不重复扣电）；失败组件报告限流（每连败一次 ErrorReport）；调节器多网络仲裁（最小位置当选）确定性正确；爆炸发电机重分类防 TERMINUS 残留。

**测试**：新增 TestEnergyNetSettlement（4 项：取消事件保留存储电量、异常发电机保留存储电量、正常池化后结算重写、余量回存）、TestEnergyNetComponent +5（负值/超容量钳制、三入口幽灵写入拒绝）、TestChargingBenchPartialCharge（3 项：部分充入按比例耗能、全额充入全额耗能、满电移出不耗能）。

**回归**：全量 **3098 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）。

## 第 17 轮（2026-08-14）：反应堆系统（Reactor / AccessPort / 燃料管线）

**范围**：Reactor 内部燃料→能量→副产物→冷却→爆炸管线、ReactorAccessPort 跨块转移、NuclearReactor/NetherStarReactor 变体、FuelOperation、EnergyNet 爆炸路径、owner 跨领地防线。子代理 847s 全量核查（5 项发现）+ 逐条 file:line / 数学语义复核。

**确认无问题（无可利用缺陷）**：
- 燃料消耗守恒：`findFuel` 按 `fuel.getInput().getAmount()` 从单槽消耗并启动满时长 `FuelOperation`（totalTicks>0 校验），无半量燃料换满额能量路径（MachineFuel.test 忽略数量仅影响 addon 自定义 amount>1 燃料，默认燃料 amount 均为 1，上游一贯行为）
- access-port↔reactor 物品转移无 dup/吞没：restockFuel、hasEnoughCoolant 冷却液回拉、createByproduct 副产物推送均用 `leftover=pushItem(...); replaceExistingItem(slot,leftover)` 模式，部分栈/满槽各情形 totals 对账一致；副产物溢出掉落不虚空（既已硬化）
- 爆炸状态机正确：explosionsQueue 为 ConcurrentHashMap.newKeySet，willExplode 恰好 remove 一次 + endOperation 一次；EnergyNet 移除爆炸发电机（不再 tick/再爆）；createByproduct 与 generateEnergy 互斥分支；队列每 tick 由 willExplode 排空，endOperation 不重复
- 冷却数学无除零：COOLANT_DURATION 常量 50；getPercentage 仅全息显示；默认燃料时长（1200/600/1500/1800）均为 50 倍数，检查点一致
- 并发：explosionsQueue/MachineProcessor 为并发集合，跨线程 contains/add/remove 至多一 tick 延迟无丢失更新；runSync 块不修改库存（仅关视图/掉落已提取余量/入队/上药水）。async EnergyNet 结算 vs 主线程点击/cargo 的库存竞态为 Slimefun 全局设计特征（AGenerator 同），未证实可靠 dup，记录非排名
- ReactorAccessPort cargo 路由正确（CoolantCell→冷却槽、其余→燃料槽、输出槽与燃料/冷却槽互斥）

**5 项发现均经复核裁定为「不改」（design / non-exploitable / non-bug）**：
1. `isSameOwner`（SlimefunUtils.java:645）owner 缺失时 fail-open → 子代理标 HIGH。**裁定：不改**。此为 javadoc 明确的遗留兼容 conscious decision（"either is ownerless → true, existing setups keep working"）；且 owner 由服务端 `BlockPlaceHandler` 写入、**非客户端可控**，玩家无法借数据包清除他人反应堆 owner——仅影响功能上线前放置的遗留反应堆。fail-closed 会破坏既有合法反应堆。与第 14 轮「ownerless 机器人领地失败开放=遗产行为」同类。
2. NetherStarReactor.extraTick owner==null 时凋零全部实体（NetherStarReactor.java:61-63）→ 同根 legacy-only，非客户端可利用，记录。
3. 爆炸虚空整库存（EnergyNet.java:380 clearBlockInfo + :391 setType LAVA/0F 爆炸，Reactor.willExplode 无 dropItems，异于 break handler :153-155）→ **vanilla 熔毁一贯行为**（熔毁=灾难性损失 vs break=小心拆解），与第 6/9 轮记录的 GEOMiner 取消丢弃产物设计选择同类；改动属游戏平衡决策，记录不擅自改。
4. ReactorExplodeEvent 经 runSync 在 clearBlockInfo 之后触发 → addon 收到事件时库存已清无法读取（LOW）；且 javadoc 称 "async ticker thread" 实际主线程触发（文档/代码不符）。LOW：addon 便利性，修需触动爆炸序列，风险/收益不划算，记录。
5. generateEnergy 直接 parseInt energy-charge 无 [0,capacity] 钳制（Reactor.java:363）→ **裁定：非缺陷**。生成门控 `space >= produced`（produced ≤ capacity 恒成立）在有/无钳制下可证明等价：负 charge→space 巨大→生成，钳 0→space=capacity→仍生成（capacity≥produced）；超容 charge→停转，钳 capacity→space=0→仍停转。clamp 仅改变返回值而 Reactor 不返回 charge。子代理亦认同 non-exploit、self-healing。AGenerator（兄弟类）已用硬化 getCharge，Reactor 内联解析为孤立代码重复异味但行为等价。

**结论**：反应堆子系统在历经第 1/6/7/15/16 轮（货运/领地/持久化/能源）触及后**已无可利用缺陷**，本轮零代码改动。全量 **3098 项测试基线复核通过、0 失败**（Java 21）。

**下轮候选（更少审计的富缺陷区）**：护身符系统（Talisman 消耗/概率/冷却）、自动合成器（AutoCrafter 配料匹配 dup 面）、GEO 资源生成（区块储量播种溢出）、强化熔炉、堆肥机/坩埚/淘金盘转化、发射器行为（BlockDispenseHandler）、史莱姆盔甲套装效果。

## 第 18 轮（2026-08-14）：自动合成器系统（AutoCrafter）

**范围**：AbstractAutoCrafter（craft 消耗/leftover/dropLeftover）、VanillaAutoCrafter/SlimefunAutoCrafter/EnhancedAutoCrafter/ArmorAutoCrafter 的 getSelectedRecipe 解析、SlimefunItemRecipe、VanillaRecipe、AbstractRecipe、AutoCrafterListener、AsyncRecipeChoiceTask。子代理 547s 全量核查（5 项发现）+ 逐条读码复核。

**确认无问题（守恒正确）**：
- craft() 物品守恒：Phase1 matchesAny 每谓词恰好消耗 1 单位（早 return 前递减 itemQuantities），per-slot 剩余计数正确防重复扣；Phase2 setAmount(remaining)；产出恰好一个 result.clone()；leftover 数量 = item.getAmount()-remaining（setAmount 前取原量）；getLeftoverItem 每次返回 new ItemStack 无别名——**无 dup 路径**
- AsyncRecipeChoiceTask：名称误导，start() 实为 runTaskTimer 同步任务，只改预览 ChestMenu 库存不动箱子，与 craft() 无竞态；观众空时自取消
- AutoCrafterListener：onRightClick 包 try/catch(Exception|LinkageError)（:84-88）、canUse 权限门、doLimitedCrafting 配方解锁检查齐全
- 事件 async 声明：AbstractAutoCrafter BlockTicker isSynchronized=true（主线程 tick），AutoCrafterCraftEvent/CompleteEvent 的 super(!isPrimaryThread()) 正确评估为 sync（仅 javadoc 误称 async，文案错误非运行时 bug）

**发现并修复（4 项）**：
- `fix(autocrafter)`（ac0a674）：VanillaAutoCrafter.getSelectedRecipe 的 `new NamespacedKey(values[0],values[1])` 对非法字符（腐化 PDC，如 `a@b:c`）抛 IAE 未捕获 → 传播 ticker → 4 错摧毁机器。包 try/catch(IAE) 失败关闭返回 null（与第 7 轮 Assembler offset/BlockPlayer owner 同 doctrine）
- `fix(autocrafter)`（68d2f0e）：**dropLeftover 双联箱余量虚空（HIGH）**——inv.getHolder() 对双联箱返回 `org.bukkit.block.DoubleChest`（非 BlockState/Block），原 loc=null 静默 return，满箱时装不回的余量（桶/瓶）被虚空；单箱正常落地。新增 DoubleChest 分支取 getLocation()。双联箱是自动化最常见配置，契合高负载场景
- `fix(autocrafter)`（68d2f0e）：**SlimefunAutoCrafter.getSelectedRecipe NPE（MEDIUM）**——`AbstractRecipe.of(item,targetRecipeType)` 在 RecipeType 不匹配时返回 null（addon 更新迁移配方 / 存储损坏），紧接 `recipe.setEnabled` NPE → ticker 4 错摧毁；updateRecipe 已有同款 null 检查、getSelectedRecipe 遗漏。补 null 守卫
- `fix(autocrafter)`（68d2f0e）：SlimefunItemRecipe.getInputs/show 对 `item.getRecipe()[i]` 循环 9 次无长度检查——畸形 addon 注册 <9 配方数组抛 AIOOBE 崩 ticker（同 F2 崩溃类）。加 `Math.min(recipe.length,9)` + null 守卫

**记录在案（不改，LOW）**：
- craft() Phase2 先消耗后 addItem（F3）：firstEmpty 预检使合法配方不可达，仅 addon setResult 畸形结果（超非堆叠上限/AIR）可触发——addon-trust、latent，记录
- matchesAny 贪心匹配假阴性（F5）：重叠谓词（如 [any_plank, oak_plank] + 仅 1 橡 1 桦）可能误拒合法合成，但**仅假阴性、无 dup/void**（守恒已证），上游贪心算法行为，记录

**测试**：TestAutoCrafter +2（损坏 NamespacedKey 返回 null 不抛、RecipeType 不匹配返回 null 不 NPE；含 MockBukkit Skull PDC 自校准断言）。F1 双联箱分支因 MockBukkit 不模拟双联箱配对（getHolder 永返单箱）不可端到端测，按第 9/10 轮惯例以代码检视验证（instanceof 分支镜像既有两分支 + 标准 DoubleChest.getLocation() API）。

**回归**：全量 **3100 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）；较第 16/17 轮基线 3098 +2（损坏 NamespacedKey、RecipeType 不匹配）。

## 第 19 轮（2026-08-14）：护身符系统（Talisman）

**范围**：Talisman.trigger/activateTalisman/consumeItem 核心流程、EnderTalisman、MagicianTalisman、TalismanActivateEvent/EffectApplyEvent、TalismanListener 全部 trigger 调用点（矿工/农夫/铁砧/法师/巫师/智者/猎人/旋风/骑士/战士/洞穴人/ lava/water/angel/fire/traveller）。子代理 711s 全量核查（7 项发现）+ 逐条读码复核。

**核心问题裁定（consumeItem 是否持久化到 live 库存）**：经三重验证（dough 字节码 ItemUtils.consumeItem 对传入引用直接 setAmount、CraftBukkit CraftItemStack.asCraftMirror live mirror 契约、全代码库 25+ 处同模式佐证）确认**持久化、核心流程无 dup**。新增 testConsumableTalismanIsConsumed 实证：激活后 slot 9 的护身符 amount 1→0（消耗生效）。MockBukkit 留 0-amount 幽灵栈（真实 CraftBukkit 把 count-0 视为 isEmpty→AIR，containsSimilarItem 不匹配、存盘跳过），故生产无幽灵复激活问题。

**发现并修复（5 项）**：
- `fix(talisman)`（7b84e38）：**Miner/Farmer 护身符掉落公式坏致 2-8x 过度复制**——`dropAmount=getAmountWithFortune(...)` 系独立重投 fortune、`max(1, dropAmount*2 - droppedItem.getAmount())` 与实际掉落无关（Fortune III 煤矿：实际掉 X∈{1..4}，重投 ∈{2..4}，extra=max(1,4..8−X)→总计 2-8× 真实掉落）。Miner/Farmer 非消耗=永久水龙头。改为干净翻倍（掉落一份 droppedItem.getAmount() 等量副本，BlockDropItemEvent 已含 fortune），移除死代码 getAmountWithFortune 及未用 meta 参数。**属掉落率行为修正（原"翻倍"护身符实为 2-8×，现统一 2×），发版须提示**
- `fix(talisman)`（7b84e38）：**铁砧护身符 setItem 1 tick 后无条件写回覆盖他物**——`Slimefun.runSync(()->inv.setItem(itemSlot,item),1L)` 在原版清除断工具后写回，期间玩家放入的物品被静默覆盖/虚空。写回前复查槽位为空
- `fix(talisman)`（7b84e38）：**铁砧护身符 trigger(消耗)在 slot 定位之前**——slot<0（.equals() 严格匹配miss：meta 分歧/非标准断裂源）时护身符已消耗未修复。先定位 slot 再 trigger
- `fix(talisman)`（7b84e38）：**EnderTalisman canUse 绕过**——末影箱分支查的是 `talisman.canUse`（base 护身符）而非 `enderTalisman.canUse`，ender_talismans 研究/权限从未评估（有 base 研究但无 ender 研究的玩家可经交易/give 获得的末影护身符照常生效）。改查实际使用的 enderTalisman
- `fix(talisman)`（7b84e38）：getPlayerByEventType 对 EntityEvent 实体未检 instanceof Player 直接强转（当前调用方预过滤不可达，但 addon API 隐患）——加 instanceof 守卫

**确认无问题（子代理核实）**：MagicianTalisman 附魔由 getRandomEnchantment 服务端配置过滤（canEnchantItem/冲突/启用），玩家无法注入或抬等级；EnderTalisman 仅访问自身末影箱无跨玩家泄漏；Hunter 外层 !getCanPickupItems 排除可持玩家装备的 mob；Whirlwind 反射投射物原样 remove+净计数；触发均同步事件处理无异步路径；TalismanActivateEvent/EffectApplyEvent preventConsumption/cancel 语义正确；Knight+Warrior 各自 containsSimilarItem+consumeItem 无同栈双消耗。

**记录在案（LOW，不改）**：cancelEvent 在 EffectApplyEvent 取消后仍执行（文档化契约，玩家劣势非 exploit）；getChance final 不可运行时篡改（构造器传 ≤0/>100 仅静默行为，Javadoc"1-100"未强制）；Hunter 内层装备复检注释声明的潜在问题被外层门掩盖（客户端不可设 mob NBT，不可利用）。

**测试**：TestTalismanActivateEvent +1（可消耗护身符激活后被实际消耗——佐证 consumeItem live mirror 持久化）。F1-F5 其余（掉落/铁砧时序/末影研究门）MockBukkit 限制定（BlockDropItemEvent+fortune、PlayerItemBreakEvent 时序、研究门控难端到端），按第 9/18 轮惯例以代码检视验证。

**回归**：全量 **3101 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）；较第 18 轮 3100 +1（可消耗护身符激活后被消耗）。

## 第 20 轮（2026-08-14）：强化熔炉系统（EnhancedFurnace）

**范围**：EnhancedFurnace（speed/efficiency/fortune + BlockTicker setProgress）、EnhancedFurnaceListener（onFuelBurn 乘子/onItemSmelt fortune）、EnhancedFurnaceBurnEvent/SmeltEvent。自读全量核查（子系统小、已有 3 个测试文件覆盖）。

**确认无问题（核心机制）**：
- setProgress：cookTime += speed*10 钳 cookTimeTotal-1（让原版在 cookTimeTotal 完成触发单次 FurnaceSmeltEvent），无双重冶炼；cookTimeTotal<=0 守卫防 setCookTime(-1)
- 燃料效率乘子默认小（0-10），fortune（getRandomOutputAmount）1..fortuneLevel+2 按 maxStackSize-previous 钳防溢栈、amount>0 守卫防 setAmount(0) IAE
- 玩家不可控 speed/efficiency/fortune（构造期配置）；玩家放入的燃料/原料经原版配方类型校验；无直接玩家 dup 路径
- 仅作用于原版 FURNACE（排 Smoker/BlastFurnace）；isDisabledIn 世界门控

**发现并修复（2 项，均 addon-trust 数值/契约加固）**：
- `fix(furnace)`（5472ef7）：**onFuelBurn 燃料效率 int 溢出**——`event.getFuelEfficiency() * burnTime`（int×int）在 `Math.min(..., Short.MAX_VALUE-1)` clamp **之前**溢出；addon 经 setFuelEfficiency（仅校验 ≥0 无上限）设大值时溢出为负/0 致非预期 burnTime（如 65536×65536 溢出为 0）。改 long 乘法再钳（与第 16 轮 flowSafeAddition 同 doctrine）
- `fix(furnace)`（5472ef7）：**onItemSmelt setAmount 读回绕过防溢栈钳制**——先前算好 `amount=min(maxStackSize-previous, amount)` 传入事件构造器，但 `event.getAmount()` 读回后直接 setAmount 未重钳；addon 经 setAmount（仅校验 ≥1，javadoc 称"至多 max stack"但代码未强制）设大值致结果超栈。读回后重 `Math.min(maxStackSize-previous, event.getAmount())` + >0 守卫

**测试**：TestEnhancedFurnaceEvents +2（addon 设 fuelEfficiency=65536+burnTime=65536→钳 Short.MAX-1 而非溢出 0；addon setAmount(100)→重钳至 maxStackSize 而非超栈）。两测在修复前会失败（溢出 0 / 超栈 100），修复后绿，证实非假通过。

**回归**：全量 **3103 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）；较第 19 轮 3101 +2（燃料效率溢出钳制、冶炼 amount 重钳）。

## 第 21 轮（2026-08-14）：GEO 资源系统（区块储量）

**范围**：ResourceManager（getSupplies/setSupplies/generate/scan）、GEOMiner（start 消耗/onCancel 返还）、GEOMiningOperation、GEOMiner endOperation 守恒、OilPump 衔接、GEOResourceGenerationEvent/GEOMiningStartEvent。自读全量核查。

**确认无问题（核心守恒正确）**：
- **dup 疑点排除**：MachineProcessor.endOperation 正确区分——`isFinished()`→fire FinishEvent（**不**调 onCancel），仅未完成（中止/破坏）才 onCancel 返还 consumedSupplies。故正常完成产出物品且 supply 保持扣减（无 dup），破坏中止返还（无丢失）
- getSupplies：NFE→OptionalInt.empty（腐化非数值 fail-safe，不致每个 GEO 机器 tick 崩）
- generate：`Math.max(0, getDefaultSupply)` 钳≥0 + getMaxDeviation>0 守卫
- GEOResourceGenerationEvent.setValue 校验≥0（防负生成）
- GEOMiningStartEvent.setConsumedSupplies 校验 `[0, supplies]`（防负→supply 增加 dup、防超→supply 变负）；GEOMiner.start `supplies>0` 守卫 + `setSupplies(supplies-consumedSupplies)`∈[0,supplies]
- onCancel：`s + consumedSupplies` 双≥0，多机器人同 chunk 守恒逐项对账正确
- scan 显示 `Math.min(supplies, maxStackSize)` 防溢栈 + `supplies>1` 守卫防 setAmount(≤1) 问题；仅作用 FURNACE 类型
- OilPump（第 16 轮）`supplies>0` 守卫 + suppliesCost∈[0,supplies] 校验
- GEOMiningCompleteEvent 取消 void 产物系第 6 轮已记录的设计选择

**发现并修复（1 项，第 7 轮 doctrine 一致性加固）**：
- `fix(geo)`（8188538）：**ResourceManager.setSupplies 公共 API 无校验**——内部调用方（generate 经 setValue≥0、GEOMiner/onCancel/OilPump 均传 supplies-consumedSupplies∈[0,supplies]）均传≥0，但 setSupplies 是公开 API，外部 addon 误用可写负 supply 致该 chunk 资源显示为负、不可挖（消费者 >0 守卫使其惰性但不恢复）。加 `Validate.isTrue(value>=0)`。不改 getSupplies 读行为（负→仍 of(负)，消费者 >0 守卫处理）以避免 GEOMiner.start 遇负资源提前 "scan required" 早退的行为变更

**记录在案（addon-trust，不改）**：GEOResourceGenerationEvent.setValue 无上限（addon 可设巨大值给海量资源，非玩家可利用，scan 显示已钳 maxStackSize）；GEOResourceGenerationEvent 无监听器守卫（每 chunk 首扫每资源触发一次，非每 tick，可接受）。

**测试**：TestResourceRegistration +1（setSupplies 往返/0 存储/负值拒绝 IAE/腐化非数值 getSupplies→empty 容错）。OilPump（8）、GEOMiningStart（10）回归确认 setSupplies 校验未破坏内部≥0 写入。

**回归**：全量 **3104 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）；较第 20 轮 3103 +1（setSupplies 校验）。

**观察（预存 flaky，待查）**：全量首轮 TestPiglinBarterDropEvent.testBarterWithoutListenersStillReplaces 偶发失败（"vanilla drop 未被替换"），但隔离重跑 6/6、全量重跑 0 失败——系测试顺序依赖/barter loot 共享静态状态的 flaky（非本轮 GEO 改动所致，setSupplies 不影响 PiglinBarter）。属测试隔离问题，留待后续轮次专项排查（若复发）。

## 第 22 轮（2026-08-14）：猪灵交易系统（PiglinBarter）+ 第 21 轮 flake 专项排查

**范围**：PiglinListener（onPickup/onInteract/onPiglinDropItem）、PiglinBarterDropEvent、PiglinBarterPreventEvent、PiglinBarterDrop 属性、SlimefunItem.getRecipeOutput。自读全量核查 + 第 21 轮 flake 根因排查。

**第 21 轮 flake 排查结论（根因不明，禁止臆断）**：
- 随机掷骰排除：5 个 chance=99 物品全失败概率 (1/100)⁵=10⁻¹⁰，天文级不可能
- 静态注册表泄漏排除：`SlimefunRegistry` 为实例字段、`getRegistry()` 经 instance 返回，MockBukkit 重载后 fresh per test class，无跨类泄漏
- 共享实例别名排除：`getRecipeOutput()` 内部已 `recipeOutput.clone()`（SlimefunItem.java:259），PiglinBarterDropEvent.drop 每事件独立克隆，无别名/dup
- 结论：单次失败无法可靠复现/解释（隔离 6/6、多轮全量仅此一次），按禁止编造不臆断根因；留作观察，若复发再专项深挖

**确认无问题（代码正确）**：
- onPiglinDropItem 替换单次（loop 首胜即 return，无多次替换/dup）；getRecipeOutput clone 故每次掉落独立；prevention 事件（onPickup/onInteract）有 getRegisteredListeners 守卫；非猪灵掉落正确跳过
- PiglinBarterDropEvent/PreventEvent 字段封装良好、setDrop/取消语义正确

**发现并修复（1 项，第 5 轮日志风暴 doctrine）**：
- `fix(piglin)`（569b766）：**配错 barter 几率的 warn 未限流**——`chance<1||chance>=100` 时原每次 EntityDropItemEvent 都 `sfi.warn`，高负载猪灵交易农场（每秒多次交易 × N 猪）会刷爆日志。改静态 `ConcurrentHashMap.newKeySet()` 按 itemId 去重，每物品仅 warn 一次

**记录在案（不改）**：PiglinBarterDropEvent 无监听器守卫（每交易触发，非每 tick，可接受）；flake 单次偶发根因不明。

**测试**：PiglinBarter 三测试类（BarterDrop 6/BarterPrevent 8/PiglinListener 6）回归通过（测试用 chance=99 有效值不走 warn 路径，warn 限流无测试影响）。warn 限流属日志降噪、无功能断言可加，按既有惯例不单设测试。

**回归**：全量 **3104 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）；第 21 轮 flake 本轮未复发。

## 第 23 轮（2026-08-14）：转化机器（Composter/Crucible）

**范围**：Composter（findOutput/consumeInput/pushItem）、Crucible（matches/consumeInput/generateLiquid）、ComposterProcessEvent/CrucibleLiquidGenerateEvent、ElectrifiedCrucible/FoodComposter（电变体）。自读全量核查。

**发现并修复（1 项 HIGH，玩家可利用资源作弊）**：
- `fix(blocks)`（f87abc2）：**Composter/Crucible 输入欠付作弊**——`findOutput`/`matches` 用 `isItemSimilar(input, convert, true)`（3 参形式**不查 amount**），故 1 片叶子即匹配"8 叶→泥土"配方；`consumeInput` 设 `removing.setAmount(convert.getAmount()=8)` 后 `removeItem`——`removeItem` 仅移除**实际可用**数量（1 片，返回余量被忽略），但 `pushItem`/`generateLiquid` 仍产出**完整** output（1 泥土/熔岩桶）。玩家拆栈单点：8 叶分 8 次→8 泥土 vs 一次→1 泥土 = **8× 作弊**；Crucible 圆石 16×。修复：新增 `hasEnoughInput(p, input)` 用 `Inventory.containsAtLeast(input, convert.getAmount())` 在事件触发前校验库存足量，不足走 wrong-item 不消耗不产出。BlockUseHandler 同步无 TOCTOU

**确认无问题**：ComposterProcessEvent/CrucibleLiquidGenerateEvent 有监听器守卫、setOutput/setWater 校验、取消语义正确（取消不消耗不产出）；Crucible generateLiquid 的下界禁水/水岩浆反应/水log 物理更新为既有 #2877/#2903 修复；输出经 OutputChest.findOutputChestFor 或自然掉落（满箱不掉落属设计）；权限 INTERACT_BLOCK 门控

**测试**：TestComposterProcessEvent +1（1 叶→不处理不消耗）、TestCrucibleLiquidGenerateEvent +1（1 圆石→不处理不消耗）。修复前两测会失败（事件触发+消耗），修复后绿。满量（8 叶/12 石）既有测试不受影响。

**回归**：全量 **3106 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）；较第 22 轮 3104 +2（Composter/Crucible 欠付拒绝）。

## 第 24 轮（2026-08-14）：欠付 bug 类全量清扫（多块机器 removeItem）

**背景**：第 23 轮发现 Composter/Crucible 的"金额校验缺失"漏洞（isItemSimilar 不查 amount + removeItem 部分移除 + 完整产出）。本轮全量 grep 所有 `removeItem` 用法清扫同型 bug 类。

**清扫结果（逐个读码核实）**：
- 已有守卫（安全）：Compressor（line 73）、OreCrusher（line 196）、PressureChamber（line 57）、AbstractSmeltery（needed 循环校验）、BlockPlacer（检查 removeItem 返回值）、Juice.removeGlassBottle（净 0 瓶无 dup/void）
- 1:1 配方 setAmount(1)（默认安全）：GrindStone、Juicer、OreWasher 的 SIFTED_ORE/PULVERIZED_ORE 分支

**发现并修复（1 项 HIGH 默认作弊 + 2 项 latent 加固）**：
- `fix(blocks)`（本轮）：**OreWasher SALT 分支默认欠付作弊**——`isItemSimilar(input, new ItemStack(SAND, 2), false)`（忽略 amount，1 沙匹配）+ `removeItem(..., 2)`（移除可用 1）+ 产出完整 SALT。**1 沙 → 1 盐**（配方 2:1），默认配置可利用。加 `inv.containsAtLeast(input, 2)` 整库守卫（removeItem 从整库移除，故守卫须整库非单槽）
- `fix(blocks)`（本轮）：GrindStone/Juicer 的 `setAmount(1)` 改 `setAmount(convert.getAmount())` + `inv.containsAtLeast(current, convert.getAmount())` 整库守卫——默认 1:1 配方为 no-op，闭合 addon 自定义 amount>1 配方的同型欠付（与 Compressor/Composter/Crucible 守卫模式统一）

**确认无问题**：removeItem 从整库移除故守卫须用 `Inventory.containsAtLeast`（整库）而非单槽 `getAmount`（多槽配方会误拒）；多块机器 onInteract 同步无 TOCTOU；MultiBlockCraftEvent 取消/替换语义正确

**测试**：无现成多块机器 dispenser 交互测试骨架（TestMultiBlocks/TestSmelteryFireConsumeEvent 不覆盖），OreWasher SALT 守卫按第 18/22 轮 MockBukkit 限制惯例以代码检视验证（3 行 containsAtLeast 守卫，清晰）。Composter(6)/Crucible(7)/SmelteryFireConsume(4) 回归通过。

**回归**：全量 **3106 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）；守卫以代码检视验证（无现成多块机器 dispenser 测试骨架）。

## 第 25 轮（2026-08-14）：AContainer 异步 ticker 输入消耗竞态（findNextRecipe bug 类）

**背景**：审计 GoldPan 时发现 ElectricGoldPan.findNextRecipe 异步消耗输入缺少 live 重验证。基类 AContainer.scanForRecipe 已显式守卫此竞态（AContainer.java:542-589 + 详注：异步 ticker 期间玩家可取走/换掉输入，consume 不重验可免费产出或消耗错物品）。9 个 override findNextRecipe 的子类绕过该守卫。子代理并行核查 8 个剩余（ElectricGoldPan 已修），逐条 file:line 复核。

**确认安全**：ProduceCollector（显式 isSynchronized=true 主线程 tick，无异步窗口，:113-116）。

**发现并修复（1 项 bug 类，8 台机器）**：
- `fix(machines)`（本轮）：**8 台异步 ticker AContainer override 缺 live 输入重验证**——read(getItemInSlot 快照) → [事件派发/计算，AutoEnchanter/AutoDisenchanter 各 2 事件窗口最大] → consume(读 live 槽但不校验是否仍匹配)。玩家在窗口内取走输入→consume 空→配方仍产出 = **免费产出**；换掉→消耗错物品。修复：各 consume 前重读 input 槽、isItemSimilar 验证仍匹配原读取项、不匹配则 return null 不消耗（镜像基类 scanForRecipe:551-559）：
  - ElectricGoldPan（findNextRecipe，1 槽）
  - AutoAnvil（findNextRecipe，2 槽 item/ductTape）
  - AutoBrewer（findNextRecipe，2 槽 input1/input2）
  - BookBinder（findNextRecipe，2 槽 item/target）
  - ElectricDustWasher（findNextRecipe，1 槽、两消耗点 SIFTED_ORE/通用）
  - OilPump（findNextRecipe，1 槽 bucket）
  - AutoEnchanter（enchant 子方法，2 槽 target/book 任一序）
  - AutoDisenchanter（disenchant 子方法，2 槽 item/book 任一序）

**利用性评估（诚实）**：竞态真实但利用性低——异步 ticker 时序非确定性（bot 难可靠命中）、人类反应不可及（~200ms vs 毫秒级窗口）；但窗口含事件派发（附魔双机 2 事件+计算最宽），加载服务器上事件派发变慢会加宽窗口。基类作者已视为值得守卫（防御纵深），故 override 应一致。修复为加法性（输入未变时守卫恒通过），不改默认行为

**确认无问题**：GoldPan（工具，b.setType(AIR) 消耗整块→1 物品，1:1 正确，BREAK_BLOCK 权限校验）；AContainer.tick 不再消耗输入（消耗只在 findNextRecipe/scanForRecipe 一次，ElectricGoldPan 双重消耗疑点经读码排除）；AContainer.scanForRecipe 自身守卫完备（live 重验证+consume 全部 or 不 consume）

**测试**：守卫为加法性、输入未变恒通过，全量回归验证 0 失败。竞态时序性、MockBukkit 无异步 ticker 交互模拟，按惯例以代码检视 + 基类同款守卫镜像验证。

**回归**：全量 **3106 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）；8 台守卫加法性、输入未变恒通过，全量 0 回归。

## 第 26 轮（2026-08-14）：流体泵（FluidPump）

**范围**：FluidPump（tick/桶/瓶路径、findNextFluid、getFilledBucket/Bottle、isSource）、FluidPumpCollectEvent。自读全量核查。

**确认无问题（干净）**：
- **无第 25 轮异步竞态**：FluidPump 自有 BlockTicker isSynchronized=true（:299-301，主线程 tick），read→consume 在单主线程无玩家交错窗口（同 ProduceCollector 安全原因）
- **守恒正确**：桶路径 1 空桶→consumeItem(slot) 恒消耗 1→1 满桶 + nextFluid.setType(AIR) 移除 1 源块（1:1:1）；瓶路径 1 空瓶→1 水瓶 + 30% 源块移除（设计，水瓶高效如原版）。无欠付（恒消耗 1，非 removeItem(配方量) 模式）
- **仅源块可抽**：findNextFluid 水仅源块返回、岩浆 Vein.find 末位源块；非源/无源→null 不操作
- **fits→push 同步无间隙**（主线程）；默认 filled 经 fits 校验、pushItem 成功无余量
- 能量仅成功时扣除（removeCharge 在 fits/event 之后）；输入须空桶/空瓶（满桶/他物 isItemSimilar 不匹配）
- FluidPumpCollectEvent 取消语义正确（不消耗不产出不扣能）；setFilledContainer addon-trust 已 javadoc 文档化（不重 fits 检查）

**修复（1 项文案）**：
- `fix(events)`（本轮）：FluidPumpCollectEvent 构造器注释误称 "Fired from the async ticker thread"，实际 FluidPump isSynchronized=true 主线程 tick——改正注释（adaptive 声明运行时本就正确，仅文案错）

**记录在案（设计观察，不改）**：getFilledBottle 对非水流体（如岩浆）default 返回 GLASS_BOTTLE（"新液体回退"），故瓶+岩浆会消耗能量循环空瓶并 30% 移除岩浆源——无物品 dup/void（瓶净 0 变化）、需近距放机+领地、原版海绵/桶本可排水，非安全问题，保留为有意回退行为

**回归**：全量 **3106 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）；本轮仅注释文案改动，无行为变更。

## 第 27 轮（2026-08-14）：经验收集器（ExpCollector）

**范围**：ExpCollector（tick 收集/produceFlasks 产瓶/getStoredExperience 容错）、ExpCollectorCollectEvent/ProduceEvent。自读全量核查。

**确认无问题**：isSynchronized=true 主线程 tick（无第 25 轮异步竞态）；getNearbyEntities 领地保护（owner INTERACT_BLOCK，第 6 轮 doctrine，无跨领地吸 orb）；getStoredExperience NFE→0 容错；每 tick 处理一 orb（experiencePoints==0 退出）；能量仅成功扣；ExpCollectorCollectEvent/ProduceEvent 监听器守卫 + 取消/调整语义正确

**发现并修复（1 项数据缺陷）**：
- `fix(exp)`（2d84d5a）：**produceFlasks 向上取整产瓶致存储变负**——循环 `level < getStoredExperience` 对存储 XP **向上取整**（存 25 时产 3 瓶=30XP，但仅 25 可用，最后一瓶为"部分"瓶）。`store = experiencePoints - withdrawn` 可变**负**（存 25+收 1=26，withdrawn=30，存 -4）。FlaskOfKnowledge 是离散 10XP 物品应**向下取整**。改 `level + 10 <= stored`（floor，withdrawn <= stored，新存储恒 >= 0）。绝对守恒经"债务机制"（负存由后续 orb 偿还）本成立，但**破坏机器时债务抹除 → 微小 XP dup**（<9XP，且耗一台机器，实用性极低）；更关键是负存储为腐化态

**测试**：TestExpCollectorProduceEvent +1（存 25 + 1XP orb → 存储不变负、==6；修复前会 -4）。既有测试（存 10 倍数）ceil/floor 同结果不受影响。

**回归**：全量 **3107 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）；较第 26 轮 3106 +1（ExpCollector 非 10 倍数存储不变负）。

## 第 28 轮（2026-08-14）：产物收集器（ProduceCollector）+ 同步机器事件注释纠偏

**范围**：ProduceCollector（findNextRecipe 守恒/isAnimalNearby）、ProduceCollectorCollectEvent。自读全量核查 + 同步机器事件注释清扫。

**确认无问题（ProduceCollector 干净）**：
- **守恒正确**：AnimalProduce 输入均 amount=1（BUCKET/BOWL），`consumeItem(slot)` 恒消耗 1 → 1:1（无第 23/24 轮欠付，配方量=1）；AnimalProduce 输出 1 物品，无离散量 floor 问题（异于第 27 轮 ExpCollector）
- recipes 空 → findNextRecipeCached 路由自身 findNextRecipe（消耗 1），scanForRecipe 不调用（无双重消耗，AContainer.tick 不再消耗，round 25 验证）
- **同步 tick**（isSynchronized=true）无第 25 轮异步竞态
- 输出经 AContainer.tick pushItem + 余量掉落（非虚空）
- claim 保护（owner INTERACT_ENTITY，getOwner null→legacy）；动物不受伤（仅 nearby）
- setResult addon-trust（notNull，默认 1 物品）；取消语义正确

**修复（5 项文案，同步机器事件构造器注释纠偏）**：
- `docs(events)`（本轮）：第 4 轮（b362d6a）为 45 个事件加 adaptive 异步声明时统一复制了 "Fired from the async ticker thread" 注释模板，但**同步机器（isSynchronized=true）触发的事件注释不准**（运行时 adaptive 声明本就正确，纯文案）。本轮修正我直接审计确认同步的 5 个：ProduceCollectorCollectEvent、ExpCollectorCollectEvent、ExpCollectorProduceEvent、AutoCrafterCraftEvent、AutoCrafterCraftCompleteEvent（FluidPumpCollectEvent 第 26 轮已修）
- **记录（低优先纯文案，不追）**：其余含该注释的事件多为异步机器触发（注释准）；少数未逐一核实 sync/async 的（android/accelerator 等）留待后续若审计到再纠。adaptive 声明运行时正确，无功能影响

**回归**：全量 **3107 项测试通过、0 失败、7 项跳过**（BUILD SUCCESS，Java 21）；本轮仅注释文案改动，无行为变更。
