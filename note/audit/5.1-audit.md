# SlimeFun4.1 v5.1.x 审计追踪

延续 [5.0.0-audit.md](5.0.0-audit.md) 的 /loop 审计循环，覆盖 5.1.0（全面汉化）与 5.1.1 之后的新改动面。每轮覆盖不同方面，本文件记录已覆盖范围、发现与处置。

## 第 1 轮（2026-08-27）：5.1.0 汉化的逻辑闭合——双语兼容机制 + 字符串匹配契约全量核查

**背景**：5.0.0 审计循环（r1-r170）结束于汉化之前，5.1.0 是此后最大的一批改动（显示层/资源层全量替换）。本轮核查发布说明声称的兼容机制是否真实成立（"表述与实现相符"检查），以及汉化是否引入新的字符串匹配失配。

**4 处双语兼容机制逐条核实（全部成立）**：
1. **灵魂绑定行**（`SlimefunUtils:74-81`）：`isSoulbound` PDC+lore 双因子，lore 侧 `contains(SOULBOUND_LORE) || contains(SOULBOUND_LORE_LOCALIZED)` 新旧两行都认（整行 equals，符文 lore 里的句子不会误匹配）；`setSoulbound(false)` 两版都删；`setSoulbound(true)` 只写中文行 ✓
2. **剩余次数正则**（`PatternUtils.USES_LEFT_LORE`）：`§e[0-9]+ (Uses? §7left|次 §7剩余)` 与写入侧 `LoreBuilder.usesLeft`（`&e<N> 次 &7剩余`）格式精确一致，`matches()` 全行匹配成立；英文 `Uses?` 兼容单复数 ✓
3. **多功能工具模式行**（`MultiTool`）：`LORE_PREFIX`（中文）/`LORE_PREFIX_LEGACY`（英文）经 `Pattern.quote` 交替匹配，写入中文前缀 + stripColor 物品名，下次匹配闭环；越界回退与 `add(Math.min(2, size))` 守卫在位 ✓
4. **MOB_DROP 反解析**（`RecipeType.resolveEntityType`）：全项目 MOB_DROP 配方仅 1 处（铁傀儡，在别名表内）；猪灵图标实际走 `BARTER_DROP`（不解析名字），别名属防御性冗余；`toUpperCase` 对中文无操作，别名键稳定匹配 ✓

**其它字符串匹配契约核查（全部同源自洽）**：
- 充能行（`ChargeUtils`）：`LORE_PREFIX` 与 `REGEX` 同源常量，语言无关格式 `N / M J` ✓
- 背包 ID 行：模板 `&7ID: <ID>` 与 `AbstractCraftingTable:93/104/121`、`PlayerProfile:795` 匹配侧全部保留英文 ✓
- 刷怪笼 `Type: ` 前缀：模板保留英文，`getEntityType` 匹配不受影响 ✓
- 祭坛探针前缀 `§dALTAR §3Probe - §e`：保留英文， AncientPedestal/ItemPickupListener 两侧同源 ✓
- 研究烟花名 `"Slimefun Research"`：`FireworkUtils:49`（发射）与 `FireworksListener:42`（免伤比较）同源保留英文 ✓
- 固定索引 lore 读取：全项目仅 `KnowledgeTome` get(0)/get(1)，模板行结构与读写索引自洽，老英文存档同结构兼容 ✓
- 无任何 `getTitle()` 字符串比较（GUI 标题纯显示层，6 处硬编码标题已汉化无副作用）✓

**lore-phrases 动态翻译基础设施（显示层安全性核实）**：
- `translateLore` 返回新列表、`getLocalizedItem` 经 `CustomItemStack.create` 构造克隆——只作用于指南显示，实际物品 lore 不被污染（`Type: `/`ID: ` 存储契约不受"类型：/编号："显示翻译影响）
- 空键守卫（防 `replace("", v)` 逐字符插入）、长短语优先排序、按语言缓存（ConcurrentHashMap）均在位
- 作弊模式 shift 点击给的是 `sfitem.getItem().clone()`（真实模板），显示克隆不流入玩家背包 ✓
- 全部 5 个调用点均在 SurvivalSlimefunGuide 显示层 ✓

**本地化资源完整性（脚本核对）**：
- 代码引用的 105 个消息键在 zh-CN（337 键）与 en（310 键）双侧全部存在，无缺键
- config.yml 默认 `options.language: zh-CN` ✓

**红线 1（老存档兼容）核实**：
- SF 物品经 PDC id 识别（r61 已深审），改名不影响
- 配方数组中仅有的 2 个 `CustomItemStack`（铁傀儡/猪灵）均为 4 号槽展示图标而非输入——**无任何靠显示名匹配的非 SF 配方输入**，不存在"汉化改名致老物品无法参与配方"的失配面

**发现并修复（1 项，表述与实现不符）**：
- `fix(i18n)`（0ff8acccc）：多功能工具模式切换消息的 `%device%` 硬编码英文 `"Multi Tool"`——与 5.1.0"运行时状态文本全部中文"表述不符，且无法区分 I/II/III 档位。改用 `getItemName()` 本地化名（stripColor），与 `%mode%` 处理一致

**回归**：全量 **3153 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21，33.6s）。

## 第 2 轮（2026-08-27）：新 API 可用性——研究系统数字 id 的持久化碰撞（2 项修复，其中 1 项 HIGH）

**背景**：目标要求把 5.0.0 新增 API 的可用性/稳定性作为审查重点。本轮从 `ResearchBuilder`（5.0.0 新增流式构建器）切入，核实其与存储层的交互。

**关键事实核实**：本 fork 的活跃存储实现仅有 `LegacyStorage`——研究解锁状态以**数字 id 为持久化键**（`researches.<id>`，读写均在 LegacyStorage:44/167）。因此数字 id 的唯一性是数据完整性的硬约束，而不仅是"遗留兼容字段"。

**发现并修复（2 项）**：

1. `fix(api)`（35cba9c72，HIGH）：**ResearchBuilder 默认 `id=0` 与持久化键碰撞**——内置首个研究 walking_sticks 正是 id 0（ResearchSetup:37），任何用 builder 不调 `id()` 的附属研究都会与它（以及彼此）共享 `researches.0`：解锁 walking_sticks（几乎人人解锁的第一个研究）即免费解锁所有默认 id 的附属研究，反向亦然。修复：未显式指定 id 时从 `NamespacedKey` 确定性派生（`floorMod(hashCode, 2^30)`，跨重启稳定、避开内置 0-8800 密集段）；`id()` Javadoc 更新。既有测试全部显式传 id，此默认路径此前零覆盖（漏检原因）
2. `fix(storage)`（e2d5f3d7b，HIGH，上游既有）：**内置 coal_generator 与 bio_reactor 共用 id 173**（upstream/experimental 同款，上游保存侧注释自认 hack 绕过）——解锁其一即双双永久解锁（14 级研究白送 18 级研究）。修复三件套：
   - bio_reactor 重编号 173→1730（静态核验 ResearchSetup 全部 id 唯一，仅此一处重复）
   - LegacyStorage 加载侧兼容：老存档 `researches.173` 存在时同时恢复两个研究（保留玩家既有进度，不丢解锁）；下次保存起各自独立落键
   - `Research.register()` 新增重复 id 告警——把静默数据污染变为启动可见（防附属再引入）
   - 保存侧防御分支保留（防未来任意重复 id），过时注释更新

**确认无问题**：
- `ResearchBuilder` 其余面：必填字段校验（key/name notNull、cost≥0）、LinkedHashSet 保序、build/register 分离语义正确；既有 10 项测试覆盖
- 研究依赖系统（addDependency/meetsDependencies/unlock 强制）：r9/r11 已深审，本轮无复发
- `SlimefunWeapon`/`SlimefunFood` 标记接口：纯标记无状态，TestSlimefunCombatEvents/TestSlimefunFoodEvents 已覆盖
- 保存侧清理分支（`anyMatch(getID()==)`）在重编号后对新旧数据均正确（测试第二段验证：解锁状态可独立撤销）

**测试**：新增 3 项（builder 派生 id 稳定且非 0/显式 id 保留、legacy 173 双恢复 + 独立撤销往返）；全量 **3156 项通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21，基线 3153→3156）。

**行为变更提示（发版须知）**：5.0.0-5.1.1 期间若有附属用 ResearchBuilder 默认 id 创建过研究，升级后其解锁状态按新派生 id 重新计算（旧 `researches.0` 状态本就与 walking_sticks 混淆，属污染数据）；coal_generator/bio_reactor 的既有解锁经兼容逻辑无损保留。

## 第 3 轮（2026-08-27）："数字 id 作持久化键"bug 类全项目推广清扫

**背景**：第 2 轮的两个 HIGH 均属"标识符默认值/重复值在持久化键上静默碰撞"。本轮把该 bug 类推广到全项目所有按数字 id 持久化的体系逐一核验。

**清扫结果（干净，第 2 轮两处为仅有实例）**：
- **背包 id**（PlayerProfile.createBackpack:397）：`max-key+1`（非 map.size，r68 已修），per-player 命名空间，lore 携带 `ID: <hash>#<id>` 读写两侧同源（第 1 轮已核）✓
- **机器人脚本 id**（ProgrammableAndroid.uploadScript:478）：`作者已传脚本数+1`——脚本体系 **append-only**（全代码库无删除路径），id 严格单调不碰撞；文件名嵌入作者 UUID（r14 已修路径穿越），跨作者命名空间隔离；同码防重传在 id 分配之前 ✓
- **传送点 id**（GPSNetwork）：字符串键、净化+空 id 拒绝（r12/r58 已审），非数字 id 体系 ✓

**回归**：本轮无代码改动；全量基线维持 **3156 项通过**（第 2 轮回归即本类闭合验证）。

## 第 4 轮（2026-08-27）：高频新事件 API 附属开发者视角端到端契约演练 + 1.21.11 兼容面复查

**范围**：目标要求把新 API 可用性作为审查重点。本轮对三族最高频的新事件（EnergyGenerate / CargoItemWithdraw|Insert / AndroidMine）以"附属注册真实监听器 → 生产触发路径 → 修改/取消路径落地"的方式演练既有覆盖与缺口，随后复查 1.21.11 符号兼容面与本循环修复的热路径归属。

**三族覆盖盘点与补齐**：
- **Cargo 族（既有覆盖完备，无缺口）**：TestCargoNetworkTaskItemFlow 8 场景全部经真实 routeItems 驱动——无监听器传输/取消退回源槽/setItem 替换分发物/setItem 空气=销毁/插入侧对称三场景/监听器事件中途拆源容器不吞没
- **Energy 族（补齐 setValue 修改路径，新增 3 项测试全绿）**：既有覆盖为契约测试 + 结算 cancel/异常/池化/回存（TestEnergyNetSettlement），缺"修改值流经真实结算"。新增 `TestEnergyGenerateModificationDrill`（经 pluginManager 注册真实监听器 + 反射驱动真实 tickAllGenerators/storeRemainingEnergy）：
  - 倍增监听器：可见值（发电 10 + 池化存储 50 = 60）×2 → 真实 supply=120；余量回存与重写语义在修改后依然成立
  - 归零监听器（setValue(0) 非取消）：supply=0 且发电机仍属被结算集合（区别于 cancel 的 unpooled 保留语义）——与 Javadoc"缩放贡献量"契约一致
  - setEnergy(-1) 拒绝（Validate ≥0）
- **AndroidMine 族（MockBukkit 限制，代码检视验证）**：MockBukkit 3.133.2 的 `BlockMock.getDrops(ItemStack)` 抛 UnimplementedOperationException（javap 实证），真实 dig() 演练不可行（与第 9/18 轮同类限制，按惯例转代码检视）。检视确认：MinerAndroid.dig/moveAndDig 触发事件后消费侧 `breakBlock(menu, event.getDrops(), block)` 直读监听器可变列表——修改必然生效；TestAndroidEvents 已覆盖字段暴露/可变性/分发可见性（dispatch 层）

**1.21.11 符号兼容面复查（干净）**：
- **版本门控**：`MINECRAFT_1_21(21, 0, "1.21.x")` 走三参构造 → `maxMinorVersion=-1`（无上界）；服务端 "1.21.11"（version=21, patch=11）经 `isMinecraftVersion` 正确匹配 MINECRAFT_1_21，所有 `isAtLeast(MINECRAFT_1_21)` 门控行为正确，**不会**降级 UNKNOWN（isAtLeast 对 UNKNOWN 恒 false 的隐患不适用）
- **compatibility 层**：7 个 Versioned* 助手全部为 `Registry.X.get(NamespacedKey.minecraft(key))` 键查找——1.21.x 稳定 API、条目缺失返回 null 由调用方处理
- 遗留已知项（README 已声明）：shaded dough/paperlib 内部符号的实机表现需上真实服务器观察，环境级风险不变

**热路径归属评估（免基准依据）**：本审计循环（第 1-4 轮）全部修复位于启动/加档/玩家消息路径——ResearchBuilder.build（附属启动期）、Research.register 重复扫描（启动期一次性 O(n²)，n≈250 即数万次 int 比较）、LegacyStorage.loadPlayerData（每次档案加载多一次已加载 Config 的 contains 查询）、MultiTool 消息（交互路径）。均不在 benchmark/ 覆盖的 tick 热路径（ticker-run/charge 写入/放置破坏/电容/全息）上，**无需重跑基准**。

**回归**：全量 **3159 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21，基线 3156→3159）。

## 第 5 轮（2026-08-27）：中频事件族（Research 解锁链 / Talisman 激活 / Backpack 开关-扩容）附属视角覆盖核验 + SlimefunLocalization 并发专项

**三族端到端覆盖盘点（全部完备，无需补测）**：
- **Research 解锁链**：TestResearchCostEvent 9 场景（扣费触发/取消=免费/setCost 调整落地/附加费超额拒解不扣/零费用=取消/解锁事件取消退还已扣等级/无监听器原行为）+ TestResearchUnlocking（时长调整端到端完成解锁）+ 依赖强制 3 场景（r11）+ 知识典籍分享（含失败退款）。全链闭合
- **Talisman 激活**：TestTalismanActivateEvent 5 场景——真实铁砧触发路径、字段正确性、取消、preventConsumption、可消耗护身符激活后真实库存移除（r19 实证 live mirror 持久化）
- **Backpack 开关-扩容**：TestBackpackEvents（PlayerBackpackOpen 经真实打开路径触发/取消阻止视图/关闭事件）+ TestBackpackResizeEvent 9 场景（扩容生效/取消保原/无监听器原行为/内容保留/观看者重定向/越界与破坏性缩小抛异常不触发）+ TestBackpackCreateEvent 6 场景（含修改尺寸落地）

**SlimefunLocalization 并发专项（结论：干净，无代码改动）**：
- **`languages` LinkedHashMap（非并发表）**：构造期（主线程 onEnable）经 loadEmbeddedLanguages 填充后**无任何运行时写入路径**（addLanguage 为 protected 且仅构造器调用；setLanguage 私有仅构造器）——启动后等效不可变；异步 ticker 线程经任务提交获得 happens-before 可见性。与 5.0.0 审计接受的 BlockStorage Config 并发读同一 doctrine
- **`lorePhraseCache`（5.1.0 新增，CHM<Language, List<String[]>>）**：短语列表在 `put` 前完成构建与排序——CHM put 即安全发布，读侧仅迭代不可变列表；并发首次访问的重复计算为良性（两侧等价，一方胜出）；键为 Language 身份语义（无 equals 重写），同一逻辑语言最多两个实例（构造器 defaultLanguage 与 languages 表项）各持等价缓存——条目数 ≤2×语言数，有界正确；短语源自 jar 内流加载、运行时不可变，无需失效机制
- **多语言切换路径**：主线程菜单点击 → PlayerLanguageChangeEvent（可否决/可重定向，r51 构造器三重校验在位）→ `setSelectedOption` 写玩家 PDC——共享本地化状态零写入；`getLanguage(Player)` 每次调用读 PDC，无缓存一致性窗口
- **记录（上游继承，不改）**：异步线程（如异步 ticker 事件监听器收 sendMessage）读玩家 PDC 与主线程语言切换写入存在理论竞态，最坏影响为单条消息使用旧语言——上游一贯设计，非本 fork 引入

**回归**：本轮无代码改动；全量基线维持 **3159 项通过**（本会话第 4 轮全量回归，工作树自该点仅新增测试与文档）。

## 第 6 轮（2026-08-27）：DirtyChestMenu/BlockMenu 多写入者槽位竞争专项 + GEO 供给并发复验（2 项修复，含 1 项实证并发丢更新）

**范围**：菜单体系的多写入者现实——异步机器 ticker（AContainer 等输出 pushItem/consumeItem）与主线程（玩家点击/cargo runSync/方块破坏 dropItems）操作同一 BlockMenu；GEO 供给 map 跨区块写入复验 r64 结论。

**发现并修复（2 项，提交 58061ef53）**：

1. **pushItem/consumeItem 丢更新窗口（并发实证）**：两个方法均为"读槽→改量→写回"的 check-then-act，无同步。8 线程同槽推入探针（200 轮）实证 **3 轮丢更新、共 11 个物品凭空消失**（两线程同时读到旧 amount，后写覆盖先写）。生产对应面：异步 ticker 推产出 vs 主线程 cargo/玩家交互同一机器菜单。修复：两方法加菜单实例级 `synchronized`——合并分支与 consumeItem 为纯菜单内操作零死锁面；null 槽分支的 `preset.onItemStackChange` 回调在锁内运行属已文档化取舍（默认实现纯函数；addon 不得在其内重入其它菜单，注释明示）。修复后探针 **0/200**。`replaceExistingItem` 保持不同步：其丢失窗口仅存于 null↔非 null 槽态跃迁的精确交错，且它是 addon 回调面，全面加锁的死锁/行为风险大于收益——记录为残留理论窗口
2. **pushItem 合并分支持久化缺失（数据丢失）**：合并分支原地 `stack.setAmount` 后**从不 markDirty**（r42"pushItem→replaceExistingItem→markDirty 链式完整"的结论只对空槽分支成立，予以纠正）——`BlockMenu.save` 以 `changes>0` 门控，菜单唯一改动为合并时永不重存，**重启丢物品**。受影响面：GEOMiner/收割机器人/砍树机器人等"只推产出不消耗输入"的机器（消耗型机器被同 tick 的 consumeItem markDirty 掩蔽）。修复：合并发生时补 `markDirty()`。新增确定性回归测试（模拟 save 后 changes=0，合并推入后必须 isDirty）

**GEO 供给并发专项（复验 r64 结论，成立）**：供给存储于 `BlockStorage` chunk-info——`SlimefunRegistry.chunks` 为 ConcurrentHashMap、`setChunkInfo` 经 get+putIfAbsent 原子建块（代码注释自证）、同区块多机器的读-改-写在单异步 ticker 线程序列化、跨区块为独立键无共享结构。无需改动。

**测试**：TestPushItem +2（合并置脏确定性断言、并发推入不腐蚀槽位弱不变量守卫）；探针为临时取证文件未入库（时序断言不可入库，r76 教训）。全量 **3161 项通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21，基线 3159→3161）。

## 第 7 轮（2026-08-27）：me.mrCookieSlime 遗留包系统性 synchronized 面复核 + 菜单同步化的基准回归（1 项修复 + 零性能回归实证）

**范围**：验证器指定的第 6 轮同类竞态推广——遗留包（BlockMenu/BlockMenuPreset/UniversalBlockMenu/BlockStorage.inventories）全部跨线程读写面复核；并因第 6 轮同步化触及 tick 热路径重跑 benchmark。

**复核结论（逐面）**：
- **发现并修复（1 项，提交 00f58ddd5）**：`BlockMenu.dropItems` 的读槽→掉落→清槽序列无同步——与异步 ticker 同槽 pushItem 构成 r6 同类竞态，窗口内推入的物品被清槽 `replaceExistingItem(null)` 静默虚空。修复：加菜单监视器同步（dropItems 全部调用面均主线程——破坏/爆炸/4 错终结均经 sync 调度，无死锁面；锁内含 world 掉落与 onItemStackChange 回调，取舍同 r6 注释）
- **其余面核验（干净）**：`UniversalBlockMenu` 继承已同步的 pushItem/consumeItem；块数据删除路径 r13 已加固（persistenceLock 下 markForFileDeletion/remove/dirty 三步原子，save 同锁排水）；`inventories` 为 CHM、moveLocationInfoUnsafely 的 put 新键原子；`BlockMenu.save` 异步快照撕裂为良性瞬态（changes 只减已写量，并发改动保持 dirty 下周期重存——r13 语义）；`ChestMenu.getItemInSlot` 单引用读原子；`BlockMenuPreset.newInstance` 仅主线程（放置事件）
- **记录（teardown 竞态，理论）**：异步 ticker 正在 tick 的机器被主线程注销（clearInventory→disableTicker 之间）——最后一个 tick 的推入落在已脱离表的对象上被 GC，单次微量产出损失，上游单循环删除语义既有行为，不改

**benchmark 基准回归（3 轮，JDK 21，对照 5.0.0-r73 基线）**——第 6/7 轮菜单同步化的直接受热场景为 `machine-processing`（活跃机器每 tick pushItem/consumeItem）：
- machine-processing active：929/795 ns（基线）→ **820/622/663 ns——零回归，反略优**（无竞争监视器获取 ~10ns 级，在 600-900ns 场景不可见）
- ticker-run 5000 块：2.28/1.20 ms → 0.81/0.83/0.81 ms（偏好）
- place/break-block、capacitor、hologram、energy-settlement：全部持平或偏好
- charge-write：196/225 ns（基线）→ 201/**317**/206 ns——r2 中位数为环境方差（r3 回落 206；该场景完全不触菜单代码，历史波带 ±30%，r74 期 4.9.4→5.0.0 亦有 245/212 先例）
- 结论：**全部场景零性能回归**。结果文件 `benchmark/report/results-r7-menu-sync-r{1,2,3}.txt`

**回归**：全量 **3161 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 19 轮（2026-08-27）：第十七大清扫——Optional 裸解包守卫完备性 + bStats 禁用面（结论：全部干净，零代码改动）

**Optional.get() 清扫（全量）**：
- `SlimefunUtils:403/436`（isItemSimilar 的 DistinctiveItem 双分支）——两处 `.get()` 均在 `isPresent()` 守卫内 ✓
- `GitHubTask:156` `uuid.get()`——`if (uuid.isPresent())` 门控 ✓；`:164` 的 `future.get()` 为 CompletableFuture.get（非 Optional.get），且整个 grabTextures 已在 r18 的 try/catch(Exception|LinkageError) 内——NoSuchElementException/异常 JSON 路径均已兜住 ✓（验证器指出的裸 get 风险实际已被 r18 修复间接闭合）
- StatusEffect/Contributor 的 Optional 用法无裸解包匹配 ✓

**bStats/指标上报禁用面**：pom 无 bStats 依赖、源码无 Metrics 类（唯一 "metrics" 匹配为 :284 无关注释）——README"移除匿名数据上报"**表述与实现相符**，禁用行为面不存在 ✓

**第十七大清扫确立**：Optional 裸解包守卫面。

**回归**：本轮零代码改动；全量基线维持 **3161 项通过**（第 18 轮全量回归）。

## 第 20 轮（2026-08-27）：第十八大清扫（启动）——静默吞没点 + debug/exceptions 收官

**扫面界定**：两类人群——"ignored" 捕获 11 处、catch-带-return 19 处。抽样核对与既往轮次遭遇的位点一致：ignored 捕获均带解释注释或可观测回退（电容驱逐 r4.9.5、DebugFishListener r53、matchesBackupName 本会话 r14 等）；return 型捕获遵循 fail-closed 语义（resolveEntityType 重抛而非吞、StatusEffect 损坏返回 false 为安全侧——4.9.4 已记录）。**边界如实声明**：本会话上下文不足以产出全新逐位点表格，闭合依据为既往 19 轮的逐点遭遇记录；若循环继续，可对 19 处 return 型做完整逐点表。

**core/debug + api/exceptions 收官**：Debug.java 全部输出经 Slimefun.logger（可观测）；TestCase 为纯枚举。api/exceptions 八个类型化异常载体（IdConflict/TagMisconfiguration 等），构造即抛、无吞没面 ✓

**第十八大清扫确立（部分闭合，边界如上）**：静默吞没点。

**回归**：本轮零代码改动；全量基线维持 **3161 项通过**。

## 第 21 轮（2026-08-27）：第十八大清扫收尾——catch-return 23 处逐点全表（全部闭合，零代码改动）

| 类别 | 位点 | 可观测信号/fail-closed 语义 |
|---|---|---|
| 日志+重试（8） | BlockStorage save/temp/serialize ×4、BackupService、GitHubConnector、LegacyStorage、DirtyChestMenu.saveAtomically | SEVERE/WARNING + "will retry next cycle"（r13/r16/r77 链）|
| fail-closed 安全默认（8） | StatusEffect NFE→false、GiveCommand 溢出→拒绝、HologramProjector/CargoNode/Assembler offset·channel NFE→默认值、ChargeUtils NFE→未充电、ProgrammableAndroid fuel·index、GPSTransmitter IAE | 玩家可见 no-op/拒绝 + r7/r54/5.0.0 各轮 doctrine 记录 |
| 版本兼容 null 回退（5） | VersionedAttribute/ItemFlag/Particle/PotionEffectType/PotionType | registry 未命中→null→调用方跳过效果（版本化降级设计）|
| 中断传播（1） | PlayerProfile InterruptedException | 恢复中断标志 + return null → failureHandler 补偿路径 |
| 遗留 fail-open（1） | SlimefunUtils IAE | r17 已裁定为有意的遗留兼容（javadoc 注明）|

**结论**：23 处全部具备可观测失败信号或已记录的 fail-closed/fail-open 语义，无需修改。第十八大清扫完整闭合。

**1.21.11 实机回归（第 24/25 轮实质推进）**：
- 第 24 轮：本地拉起真实 Paper 1.21.11-132（Java 21.0.10）部署 SlimeFun4.1-5.1.1.jar——runbook 第 1 节判据全过（必须 3/3、禁止 0 命中，**shaded 依赖残留风险实证排除**），日志存档 note/report/1.21.11-boot-log.txt（提交 8aced34fa）
- 第 25 轮：**7 分钟真实服务器浸泡**——插件全程在线，Slimefun 相关 ERROR/SEVERE/Exception **零条**（全部错误仅为原版 flat 世界生成噪音），日志存档 note/report/1.21.11-soak-log.txt
- **第 26 轮（mineflayer 协议机器人驱动真实客户端交互）**：
  - ✅ **实机验证**：/sf research all 与 /sf give 全链路（中文输出）；SF 方块经真实客户端协议放置成功且 autosave 持久化（.sfb 含 ENERGY_REGULATOR/CONNECTOR×2/SOLAR_GENERATOR/ELECTRIC_GOLD_PAN/CARGO_MANAGER 正确坐标）；**机器菜单 GUI 真实打开**（右键 81 槽窗口、中文 customName 断言通过）；vanilla 箱子开合与煤炭存入（containerItems 精确 16 coal）
  - ⚠️ 未完成：货运输入/输出节点放置（mineflayer 对该方块型的放置交互受限，连放其余 SF 方块均成功唯节点失败，根因未定）；光伏充电未观测到（网络五件套齐备/天光通/正午/区块 forceload/autosave 已刷盘，但 .sfb 无 energy-charge 键——根因未定，疑服务端 ticker/光照门控差异，需真人客户端复核）
  - 证据：note/report/1.21.11-smoke-bot.js（机器人脚本）+ 1.21.11-smoke-server.log；测试环境已清理
- **第 27 轮（疑点定性反转与移交）**：
  - **纠正**：`updateBlockInfo` 会置脏块（dirtyBlocks.add）——充能变化**本应**随 autosave 刷盘；故 ".sfb 无 energy-charge 键"= 真实零发电，**非**此前猜测的"内存瞬态设计"（该假说被代码否定）
  - **可观测性突破**：`/forceload` + `/data get entity @e[type=minecraft:armor_stand]` 可无客户端读取调节器全息（CustomName `+ N J ⚡`）——实测取得 `+ 0 J ⚡`（调节器 ticker 运行、网络在线、零发电）；该次运行的太阳能布局因脚本中断而在位存疑，零发电不能归因于插件缺陷
  - **新发现的环境变量**：①天气（雨/雷压天光→光伏 0 输出，time set noon 不清天气）；②`/fill` 清 SF 方块不触发数据清理→幽灵数据干扰网络解析（此前多轮部分放置的残留可能是干扰源）——均已写入 runbook 供真人复测遵循
  - 移交：§2 太阳能充电的最终确认需真人客户端（或按 runbook 新增的无客户端观测法 + 新世界重摆）

**回归**：本轮零代码改动；全量基线维持 **3161 项通过**。

## 第 8 轮（2026-08-27）：菜单监视器锁交互时序推演 + ChestMenu 基类写入面审查（结论：无锁序倒置，零代码改动）

**背景**：第 6/7 轮引入的 pushItem/consumeItem/dropItems 菜单监视器是全新锁面，须证明与既有锁体系（persistenceLock、ticker 调度）无环、与回调/事件无锁序倒置。

**锁交互推演（逐向核验，全部干净）**：
- **persistenceLock → 菜单监视器方向：不存在**。BlockStorage.save 的排水模式（:402）在 persistenceLock 内仅做快照并清空，随后**释放锁再序列化**；menu.save()（:499）在锁外调用且本身非 synchronized——该方向嵌套为零，环不成立
- **菜单监视器 → persistenceLock 方向：不存在**。监视器持有期间的调用面：getItemInSlot（live Inventory 读）、ItemStackWrapper/ItemUtils/CustomItemStack（纯物品操作）、markDirty（AtomicInteger）、replaceExistingItem → `preset.onItemStackChange`（**全项目零覆写**，基类默认纯返回 next——grep 实证仅 cargo 注释引用该钩子）、dropItems 的 world.dropItemNaturally（Bukkit 调用，见下）。无任何 BlockStorage 锁获取
- **TickerTask：跨机器 tick 不持锁**（全文件无 synchronized 块，"synchronized tick" 仅为 javadoc 措辞指主线程 tick）——机器 ticker→菜单操作链上无外层锁
- **EnergyNet 结算：零菜单引用**（grep 实证）——结算只读写 Config 充电数据，与菜单监视器无交集
- **锁内事件/回调面（已文档化取舍，复核成立）**：dropItems 监视器内的 dropItemNaturally 触发 ItemSpawnEvent、replaceExistingItem 调用 onItemStackChange——监听器/覆写代码在锁内运行。死锁需两线程构成 A→B 与 B→A 的跨菜单环：B→A 只能经"锁内回调再触其它菜单"，内置代码无此路径（零覆写+无菜单内触发事件再入菜单），仅理论存在于自设环的 addon——代码注释已警示
- **BlockMenu 构造即物化**：两个构造器尾部 `getContents()`（:29/:48）——inv 在构造线程建好后经 inventories CHM 发布，异步读者恒见非 null inv，基类 setup() 惰性创建竞态对 BlockMenu 不可达

**ChestMenu 基类（me.mrCookieSlime.CSCoreLibPlugin，fork 内源码非 shaded）写入面审查（记录，不改）**：
- `setup()` 惰性建库存的 check-then-act 无同步——BlockMenu 不可达（上述构造即物化）；裸 ChestMenu 由 addon 直接跨线程构造+首次触碰理论上可触发（一个库存被丢弃）——基类遗留行为，记录
- `items` ArrayList / `handlers` HashMap 仅构造期（preset init/clone）与主线程（MenuListener 点击分发）触碰，无跨线程运行时写
- 点击处理面（MenuListener→handler→replaceExistingItem 未同步）与异步 ticker pushItem 的交错为 r6 已记录的残留理论窗口（null↔非 null 槽态跃迁），本轮无新证据升级其风险等级

**回归**：本轮零代码改动；全量基线维持 **3161 项通过**（第 7 轮全量回归）。

## 第 9 轮（2026-08-27）：MenuListener.menus 与观看者生命周期并发面 + 命令层输入面复检（1 项修复）

**观看者生命周期逐面核查**：
- **断线清理 ✓**：玩家断线时服务端关闭其打开的库存并触发 InventoryCloseEvent → `onClose` 即时 `menus.remove`（r5 已证"关界面即清"，无泄漏）
- **连续开菜单次序 ✓**：`ChestMenu.open` 先 `openInventory`（同步触发旧菜单 close → remove）后 `put` 新菜单——新条目不会被旧 close 误删（r9 背包两段式同型正确次序）
- **发现并修复（1 项，提交 a48082b91）**：**UniversalBlockMenu 异步关闭观看者**——异步 ticker 删除队列 → `deleteLocationInfoUnsafely` destroy 分支 → `universalInventory.close()` → `DirtyChestMenu.close()` **直接在异步线程调 `human.closeInventory()`**。双重危害：① 异步触碰实体/库存状态（同文件 `clearInventory` :1253 早有 `runSync` 包裹并注释"Asynchronous entity add"原因，此路径漏包裹）；② 异步线程触发 InventoryCloseEvent → `MenuListener.onClose` 在异步线程 `menus.remove`，与主线程 `menus.put/get`（点击/开菜单）构成 **HashMap 跨线程访问**（结构损坏/幽灵条目——玩家后续点击被误路由到已死菜单）。修复：close 的观看者关闭改经 `Slimefun.runSync`（对齐 clearInventory 既有语义，destroy 路径的关闭延迟一 tick 属可接受统一）；`menus` 换 ConcurrentHashMap 作纵深防御（防任意第三方异步 close 再入）
- **记录（上游语义，不改）**：close handler 内同步再开菜单的重入递归为 addon 行为面；关服时表随 JVM 消亡非问题

**命令层不可信输入面复检（干净，引用既有轮次 + 变更史证明）**：自受审提交 ad5cd1277（r11 依赖多趟解锁）以来命令目录仅 `VersionsCommand` 12 行显示文本变更（git diff 证明），解析/门控逻辑零改动——r48（NUMERIC 正则、命令方块排除门、cheat 权限门、结果上限/crop）与 r81（GiveCommand 命令方块排除、溢出捕获、excess 落地配置）结论继续成立

**回归**：全量 **3161 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 10 轮（2026-08-27）：destroy 路径时序复核（第 9 轮 close 改动影响面）+ lore-phrases 相关解析边界清扫扩展（结论：全部干净，零代码改动）

**destroy 路径时序（第 9 轮 close 延迟一 tick 的新交互，逐路径推演）**：
- **保存/计数器一致性**：`BlockMenu.save` 与 `UniversalBlockMenu.save` 同款门控——`changes.get()` 快照 → 写槽 → `addAndGet(-快照)` 只减已计数写入；窗口内并发 markDirty（含 r6 新增的合并分支置脏）的增量在减法后存活 → 菜单保持 dirty 下周期重存。保存槽位快照与监视器持有者的交错撕裂为已接受的良性瞬态（r7/r13 doctrine，下周期覆盖）
- **universal 菜单 destroy 窗口**：`deleteLocationInfoUnsafely` destroy 分支 close（现延迟一 tick）→ save（异步立即）。universal 菜单按 preset id 驻留注册表、不随方块删除移除——观看者在窗口内的点击 markDirty 存活计数 → 下一保存周期落盘，**零丢失**
- **clearInventory 对照**：其"延迟关闭（r13 起既有 runSync 模式）→ 删文件 → 移表"次序未变；第 9 轮改动只是让 universal 路径对齐这一既有模式。窗口内观看者点击取出物品属合法取回（等价于破坏掉落语义）
- **监视器交互**：save 不获取菜单监视器（r8 已证双向嵌套不存在），新时序未引入锁面

**lore-phrases 解析边界清扫扩展（r55 第十一个清扫在 5.1.0 后的扩展）**：
- **模板不可污染（关键证明）**：`SlimefunItem.getItem()` 返回 `itemStackTemplate.clone()`——`getLocalizedItem` 的 `CustomItemStack.create(item.getItem(), meta->...)` 操作新鲜克隆，显示翻译（含 "Type: "→"类型： "）**不可能**写入共享模板，契约行不可能经显示层进入真实物品；作弊模式发放 `getItem().clone()` 同为模板克隆（r1 已证）
- **翻译 lore 无回流解析面**：`translateLore`/`getLocalizedItem` 全部 5 个调用点在指南显示层（r1 盘点），无任何代码解析翻译后的 lore
- **5.1.0 新增解析面复核**：灵魂绑定双行/USES_LEFT 双语正则/MultiTool 双前缀（r1 已证同源）；`getLorePhrases` 空键守卫 + 长短语优先 + 按语言缓存（r5 并发专项已证安全发布）；管理员误配 phrases 仅影响显示层
- KnowledgeTome 固定索引读取（唯一 index-based lore 读）r1 已证模板自洽

**回归**：本轮零代码改动；全量基线维持 **3161 项通过**（第 9 轮全量回归）。

## 第 11 轮（2026-08-27）：integrations/ 软依赖适配层 1.21.11 符号面 + guide 搜索/历史内存面（结论：全部干净，零代码改动）

**integrations/ 适配层核查（r43"fork 零改动"结论换符号兼容视角复验）**：
- **注册门控结构天然韧化**：`IntegrationsManager.load()` 对每个软依赖做"插件在场 + isEnabled"检查，回调整体 `catch (Exception | LinkageError)` 并带版本号记日志——第三方插件 API 变更（NoSuchMethodError/NoClassDefFoundError）最坏降级为一条 WARNING + 跳过该集成，**不会崩插件**；缺失即回调不执行（干净降级）。ProtectionManager 装载同样 try/catch 包裹
- **逐适配类调用面**：PlaceholderAPI（expansion 注册与 placeholder 解析，PAPI 自身 API 与 MC 版本解耦）；WorldEdit（构造器 `Class.forName("...extent.Extent")` 显式版本守卫，缺失时转为 IllegalStateException 交由 load() 捕获——设计良好）；mcMMO（事件监听 + SkillUtils 调用）；ClearLag（EntityRemoveEvent 监听保展示物品）；Orebfuscator（ServicesManager 服务查找——`getRegistration(...).getProvider()` 对"插件在场但服务未注册"会 NPE，但发生在 load() 的 catch 内，降级为警告，记录为上游可接受行为）；ItemsAdder 仅标记位
- **1.21.11 面**：适配类的 Bukkit 侧符号（事件/ServicesManager/PluginManager）均为 1.21.x 稳定 API；第三方符号与 MC 版本解耦（随各插件版本演进，运行时不兼容由 LinkageError 兜底捕获）
- **反射面**：全层仅 WorldEditIntegration 一处 Class.forName，且为守卫用途（上述）

**guide 搜索/GuideHistory 内存面（高频使用视角）**：
- **GuideHistory 有界**：`MAX_HISTORY_SIZE=200` 逐出最旧（r48 引入）；条目多为 SlimefunItem/ItemGroup **单例引用**（零拷贝）与搜索词（≤聊天协议 256 字符）；最坏情形全为配方展示 ItemStack 克隆约 200KB/玩家，且随 PlayerProfile 卸载（quit → markedForDeletion → auto-save 收口）整体释放——有界 + 生命周期受限
- **搜索路径**：结果 44 条上限 + 标题 crop 截断 + 字面 contains（r48 已证无 ReDoS/OOM 面），`refresh` 对同对象连续翻页仅更新页码不新增条目

**回归**：本轮零代码改动；全量基线维持 **3161 项通过**（第 9 轮全量回归）。

## 第 12 轮（2026-08-27）：SlimefunProfiler 按需收集开销 + 盔甲周期异步任务时序/取消语义（结论：全部干净，零代码改动）

**Profiler 专项（长时高负载自身开销视角）**：
- **空闲路径成本**：`newEntry()` 一次 volatile 读短路返回 0；`closeEntry` 两次 notNull + timestamp==0 短路；`endTick` volatile 写——该成本已含在基准 ticker-run（0.8ms/5000 块）内（4.9.5 优化产物）
- **字段并发正确**：`isProfiling`/`lastTickTotalElapsed` volatile、`queued` AtomicInteger、`timings` CHM、`requests` ConcurrentLinkedQueue；`start()` 仅在有请求时开启逐块收集并清表
- **finishReport 有界**：等待循环上限 4000×1ms（4s），超时向请求者发送诚实的超时消息并排水请求——请求不会滞留堆积；下一周期已开始（isProfiling && queued>0）时中止防跨周期污染；重复 /sf timings 每次新建请求、服务即移除
- **closeEntry 合并**：无人查看时在调用线程就地 merge（免 lambda 分配与 executor 往返）；有人查看时经 executor（多线程池，阻塞其一可接受——代码注释自证）
- **kill()** 关停 executor；DebugFish 查单块耗时需先 /sf timings 的诊断工作流变化在 4.9.5 发布说明中已声明——表述相符

**盔甲三任务 + SolarHelmet（调度/取消/重入语义）**：
- **间隔钳制**：`tickInterval < 1` 钳 1 并 WARNING（0/负配置不产生零周期任务风暴）
- **逐玩家隔离**：`run()` 对每玩家 `catch (Exception | LinkageError)`——注释明示未捕获异常会取消重复任务并永久静默禁用全部盔甲处理（此防线在位）
- **关服取消**：onDisable `cancelTasks` 全量覆盖
- **条件门控**：radiation 嵌套于 enable-armor-effects 之下，冲突时 WARNING 提示（表述与行为一致）
- **SolarHelmet 细粒度间隔（记录，上游同款）**：与盔甲任务共用 `armor-update-interval` 但不带 ×20（10 tick vs 200 tick）——upstream/experimental 逐字相同，充电速率按此间隔标定，上游有意设计非缺陷

**回归**：本轮零代码改动；全量基线维持 **3161 项通过**（第 9 轮全量回归）。

## 第 13 轮（2026-08-27）：AutoSavingService 周期/峰值面 + Network discoverStep 参数级复核（结论：全部干净，零代码改动）

**AutoSavingService（保存间隔边界 + 单周期全量刷盘峰值）**：
- **间隔边界**：`interval <= 0` 回退默认 10 分钟并 WARNING（r13 加固在位）；分钟单位 ×60×20 换算正确
- **峰值行为**：玩家与方块两个异步任务数据域不相交（PlayerProfile saveLock per-profile / BlockStorage persistenceLock 仅排水段持有），互不争锁；Bukkit 调度器对同一异步重复任务不自重叠（运行中不下发下一次）——不会出现保存风暴叠加
- **刷盘形态**：单线程顺序逐档案/逐世界（非并行 I/O），每单元异常隔离 + 失败保持 dirty 下周期重试（r13/r16/r76 加固链在位）；profile 卸载仅在保存成功后执行（数据安全）
- **事件**：SlimefunAutoSave/BlockDataSave/PlayerProfileUnload 均自适应异步声明（r13 827059a 在位）

**Network discoverStep 参数级复核（节点数逼近/超过 maxSize 场景）**：
- **语义核实（上游逐字一致）**：`maxSteps = manager.getMaxSize()`（config `networks.max-size`，默认 200）是**每 tick 发现预算而非网络规模硬上限**——超过 200 节点的网络经 ceil(n/200) 个 tick 完成发现，队列跨 tick 保留（r66 摊销结论复验）；终止由队列排空保证
- **限幅计数**：steps 对分类未变节点同样计数——markDirty 洪峰（大规模放/破坏）以 ≤200 节点/tick 有界消化
- **分类变更拆建**：REGULATOR/CONNECTOR 分类变化 → 整网注销重建，收敛式（重建网络经调节器身份解析重新注册，5.1.1 修复语义在位）
- **记录（上游命名语义，不改）**：config 键名 "max-size" 实际语义为"每 tick 发现预算"（网络可超此规模且无碍）——上游同名同行为，命名误导属上游文档层面；发现中途的部分分类网络结算为瞬态欠供（下一 tick 自愈，电量按方块存储无损）
- **fork 改动归属**：相对 upstream 的 diff 仅为并发加固（CHM/并发队列/registered 标志，r66 已审），预算逻辑零改动

**回归**：本轮零代码改动；全量基线维持 **3161 项通过**（第 9 轮全量回归）。

## 第 14 轮（2026-08-27）：BackupService/GitHubService 失败累积面 + cargo-ticker-delay 边界（1 项修复）

**BackupService（备份轮换失败累积）**：purgeBackups 仅参与时间戳格式文件（杂散文件不炸 DateTimeParseException）、新→旧排序删尾部（4.9.4 方向修复在位）；Files.delete 失败抛 IOException 由 run() 的 catch(Exception) 接住——**无失败累积机制**（每周期对同一批最旧文件重试 + 一条 WARNING，文件数有界）；目录被清理后 mkdirs 自愈（r77）；同名分钟幂等跳过。✓

**GitHubService（长时间断网）**：
- 每连接器 download() 内部捕获全部预期网络失败模式（IOException/InterruptedException/JsonParseException/URISyntaxException）并写缓存；缓存目录缺失自愈 + 补建后重试一次（r77）
- **发现并修复（1 项，提交 24a02629e）**：`GitHubTask.connectAndCache` 的 forEach 无外层隔离——单个连接器逃逸的**意外 RuntimeException** 会中止整个 forEach 且使小时级重复任务**自取消**（Bukkit 异步重复任务对未捕获异常的语义），贡献者更新静默停摆至重启（与盔甲任务教训同型，r30 的 per-player 隔离缺失于此处）。修复：逐连接器 try/catch(Exception|LinkageError) + WARNING
- 断网行为：每小时每连接器一条失败日志（logging 可关）+ 缓存不更新——有界、不累积、恢复后自动重建 ✓

**cargo-ticker-delay 边界（参数级）**：阈值计数逻辑为"每 TICK_DELAY+1 tick 运行一次"（守卫-递增-运行后复位 `tickDelayThreshold=0` 三段完整）；**0 → 每 tick 运行**（0<0 恒假）；**负值 → 每 tick 运行**（同理由，安全退化无永久停摆）；配置损坏 getInt→0 同上。✓

**回归**：全量 **3161 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 15 轮（2026-08-27）：第十三大清扫——全部周期任务 run() 异常防护完备性（1 项修复）

**方法**：全量 grep `runTaskTimer*` 调用点（9 处），逐任务核对 run() 主体的未捕获异常路径（Bukkit 重复任务对未捕获异常自取消——r30 盔甲/r14 GitHubTask 教训的 bug 类系统清扫）。

**逐点结论**：
- GitHubTask（异步/小时）——r14 已修 ✓
- clearStaleCapacitorTextures（异步/5min）——removeIf lambda 逐条目 try/catch ✓
- saveAllPlayers/saveAllBlocks（异步）——逐档案/逐世界隔离 + 外层结构无可抛出面 ✓
- 盔甲四任务——run() 逐玩家 catch(Exception|LinkageError)；onTick 尾部全部为无抛出面简单语句 ✓
- evictDeadProjectiles（同步/30s）——无抛出面 ✓
- TickerTask（异步/主循环）——逐 chunk/逐位置隔离久经多轮审计（5.0.0 r4/r5/r14 等）✓
- **发现并修复（1 项，提交 bfde7b6a2）**：`AsyncRecipeChoiceTask.run`（同步）——try-finally 只保证解锁，`inventory.setItem`/`iterator.next()` 的异常仍会在解锁后传播并取消任务。修复：catch(Exception|LinkageError) 内优雅自取消（预览动画损坏不值得重试）

**第十三大清扫确立**（前十二见 5.0.0-audit）：周期任务自取消面。

**回归**：全量 **3161 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 16 轮（2026-08-27）：第十四大清扫——一次性同步任务异常面 + PlayerProfile 孤儿档案（1 项修复）

**runSync/一次性延迟任务清扫**：
- **咽喉点证明**：`Slimefun.runSync` 两个重载是全部 ~50 处一次性同步任务的唯一汇聚点（r41 清点）；直接 `scheduleSyncDelayedTask` 仅 2 处——AbstractPlayerTask（玩家任务一次性 by design，单次动作丢失有界）与 TickerTask:349（4 错终结掉落 lambda，dropItems 异常面为异国世界操作，记录不改）
- **发现并修复（1 项，提交 fb339e4a8）**：runSync 调度分支无兜底——未捕获异常使延迟动作静默失败且仅留匿名调度器日志、不可归因。修复：两个重载的调度分支包 try/catch(Exception|LinkageError) + SEVERE 归因（动作本身不可恢复，此修复只使失败可诊断）；**UNIT_TEST 分支保持直抛**以免吞测试断言
- r41 的"过期状态"清扫（引用过期方块/实体的守卫）与本轮"异常面"清扫互补闭合

**PlayerProfile 加载队列孤儿处理（复查干净）**：MAX_LOAD_ATTEMPTS=3 退避重试后终局返回 null（必终止）；排队回调经 `deliver` 必获解决——成功回调或经 runSync 主线程补偿的 failureHandler，且**逐回调隔离**（单个回调异常不阻断其余）；加载中退出的档案由 `markForDeletionIfOffline` 收口（quit-监听器看不到加载中档案的泄漏路径已闭合，r68 设计复验）

**第十四大清扫确立**：一次性调度任务异常面（经由咽喉点单点闭合）。

**回归**：全量 **3161 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 17 轮（2026-08-27）：第十五大清扫——一次性异步任务异常面 + PatternUtils 回溯风险（结论：全部干净，零代码改动）

**一次性异步任务清扫（调度三分类最后一块）**：非咽喉点调用仅 2 处——
- `PlayerSkin.fromPlayerUUID`（fork 自写 skins 层）：异步 lambda 全路径 complete/completeExceptionally（r72 已证 future 不悬挂），异常不可逃逸 ✓
- `IntegrationsManager.onServerStart`（runTask 一次性）：唯一风险段 ProtectionManager 装载有 try/catch(Exception|LinkageError)，load() 内部自捕获 ✓
- 其余一次性同步任务经 r16 runSync 咽喉点兜底覆盖 ✓

**PatternUtils 正则族回溯风险（不可信输入面）**：全部模式为线性结构——离散字符类 + 单层量词（`[a-z0-9_-]+:.*`、`minecraft:[a-z0-9/._-]+`、`\w{3,16}` 有界、充能/多功能工具前缀+`(.+)`）——**无嵌套无界量词，不存在灾难性回溯**；最坏情形（`[0-9.]+` 后需字面 " / " 的局部回溯）为 O(n²)，在 lore 行（聊天协议 ~256 字符上限）量级上仅数千次字符比较，不可利用。MultiTool/ChargeUtils 模式经 `Pattern.quote` 固定前缀（r1 已核）✓

**第十五大清扫确立**：一次性异步任务异常面。调度三分类（周期/一次性同步/一次性异步）异常防护至此系统性闭合。

**回归**：本轮零代码改动；全量基线维持 **3161 项通过**（第 16 轮全量回归）。

## 第 18 轮（2026-08-27）：第十六大清扫——CompletableFuture 异常完成面 + UniversalBlockMenu 共享语义（2 项修复）

**CompletableFuture 使用点全量核查（4 处）**：
- PlayerSkin/UUIDLookup（r72 已证全路径 complete/completeExceptionally）✓
- **发现并修复（2 项，提交 a57553d37）**：
  1. `PlayerBackpack.closeForAll`——future 完成语句在 runSync 体内，异常时 future 永不完成（**孤儿 future**，AbstractCraftingTable 的 thenRun 重建链条静默死寂）。修复：catch 内 completeExceptionally
  2. `GitHubTask.grabTextures`——`future.get(30, SECONDS)` 的 TimeoutException（慢 Mojang API 的**预期事件**）/ExecutionException 逃逸无防护的 run()，使小时任务自取消（r15 清扫只覆盖了 connectAndCache 的 forEach，此为漏点）。修复：调用点包 try/catch(Exception|LinkageError) + WARNING

**UniversalBlockMenu 共享语义收官（干净）**：按 preset id 注册于 universalInventories（CHM，加载置位防重复）；所有同型方块（如全部强化工作台）共享同一菜单与 .sfi 文件——写路径经 r6/r7 同步化的 pushItem/consumeItem/dropItems 菜单监视器串行化，跨方块一致性由单实例保证；保存经统一 changes 门控（r10 时序复核）；destroy 路径不注销注册表项（设计如此，同型其它方块仍用）——r9/r10 已覆盖，本轮确认共享单例模型下无遗漏的一致性面

**第十六大清扫确立**：future 异常完成面（孤儿 future/异常吞没）。

**回归**：全量 **3161 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 28 轮（2026-08-27）：太阳能链实机闭环——三链路冒烟收束与 5.1.2 发布

- **决定性实验**：新世界 + `/weather clear` + `/time set noon` + 机器人五件套放置 → 调节器全息稳定 **`+ 2 J ⚡`**；淘金盘 `.sfb` 落盘 **`energy-charge: 128`**——发电→传输→充电→持久化全链在真实 Paper 1.21.11 闭环（日志 note/report/1.21.11-solar-success.log）
- **根因定案**：r26 的零发电 = 雨天天光被压（/time set noon 不清天气）——r27 天气假说实证成立，非插件缺陷
- **冒烟总账**：§1 加载 ✅ / §2 太阳能 ✅ / §3 菜单（81 槽中文 GUI）✅ / §4 货运 A 侧 ✅（节点放置为机器人交互限制，传输逻辑 MockBukkit 8 场景覆盖）/ 长时浸泡零错误 ✅
- 发布：pom bump 5.1.2、发布说明转正（含实机回归摘要）、构建 `SlimeFun4.1-5.1.2.jar`

## 第 29 轮（2026-08-27）：持久化写入的崩溃一致性与 IO 异常面——Config#save 双咽喉点原子化（2 项修复）

**既有原子面复核（干净，既往轮次成果在位）**：`.sfb`（BlockStorage.writeBlockFile + 失败 requeue）、`.sfi`（DirtyChestMenu）、chunks.sfc（saveChunks tmp+move）、玩家档案/航点（LegacyStorage.saveAtomically，失败抛 UncheckedIOException 留脏重试）；AutoSavingService 逐档案/逐世界 try/catch 隔离（单点失败不取消周期任务）；PlayerProfile.save() 成功落盘后才清 dirty（epoch 方案）✓

**发现并修复（2 项）**：两个 `Config#save` 咽喉点均为"直写 + 非原子"——字节级证据：`FileConfiguration.save(File)`（paper-api）= `new FileOutputStream(file)` 直接截断在用文件再流写（1.19.4 jar 反汇编核实；1.21.11 实机行为同源）。崩溃/断电/磁盘满中途必留半截文件，且：
1. **CS-CoreLib `Config#save/save(File)`**：`catch (IOException e) {}` **完全静默**——磁盘满时旧文件已被截断、无任何日志。调用方含 `Script.java` ×3（玩家编写的机器人脚本=真实用户数据，截断即丢）与 `BlockStorage.saveChunks`（后者目标是 tmp 文件，本身安全）。修复：原地原子化（`FileUtils.saveAtomically`：tmp+`ATOMIC_MOVE`，回退 `REPLACE_EXISTING`，父目录创建，失败删除半截 tmp + SEVERE 归因日志"旧文件完好但新状态未落盘"）；错误报告路径加 `Slimefun.instance()` 空守卫（`validateInstance()` 在报错路径抛 IllegalStateException 会掩盖原始 IO 失败，测试环境亦需）。
2. **dough `Config#save`（外部依赖 jar，无法原地改）**：反汇编确认其 header 处理后直调 `FileConfiguration.save(file)`，IOException 仅 SEVERE——**截断已发生**，日志无济于事。修复：新建 `ConfigUtils.saveAtomically(Config)`（逐字复刻 dough header 语义保证字节级输出一致 + 委托 `FileUtils.saveAtomically`），替换全部 12 处调用：PostSetup（**Items.yml/Researches.yml——管理员定制配置，截断后下次加载变空档被默认值覆盖=整档丢失**）、PerWorldSettingsService ×2（会话中世界加载即写入）、CustomTextureService、SoundService、PermissionsService ×2、SlimefunLocalization、GitHubService 缓存 ×2、ResourceManager。行为零变化（含 PerWorldSettingsService 直接设 options().header 的既有语义——dough 原实现同样经 getHeader() 判空走 copyHeader(false)，输出逐字节一致），仅写入方式变原子。

**复用的 tmp 命名与并发性**：`.tmp` 与仓内 4 处原子写器同约定；同一 Config 对象跨线程并发 save 本身不受 FileConfiguration 线程安全保护（上游前置缺陷），tmp 撞名不引入新面。

**清扫收尾**：全仓无绕行两咽喉点直调 `FileConfiguration.save` 的残余（grep 证明）；`Config#createFile` 的静默吞 IOException 为既有遗留（仅建空文件信号，无数据面），记录不改。

**第十九大清扫确立**：配置/用户数据写入的崩溃一致性（tmp+原子移入全覆盖：方块/菜单/玩家/配置/脚本五类持久化文件）。

**新增测试（11 项）**：`TestAtomicConfigSave`（7：目标写入/无 tmp 残留/父目录缺失创建/完全替换无残留/父路径为文件时失败且不产生 tmp/dough 落盘/合并-移除语义/头保留）+ `TestLegacyConfigSave`（4：落盘/合并-移除语义/save(File) 重载/失败路径旧文件完好且不抛）。测试过程发现并按真实契约修正假设：`Config` 为**加载-合并-保存**语义（第二次构造加载旧键），移除须 `setValue(path, null)`——字节级断言确认移除键消失（排除追加式残留）。

**回归**：全量 **3172 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 30 轮（2026-08-27）：GPS/GEO 网络、远古祭坛多方块并发、村民/实体交互不可信输入面（2 项修复）

**GPS/GEO 面复核（大体干净，既往轮次加固在位）**：`GPSNetwork.updateTransmitter` 原子 compute（异步 ticker 注册 vs 主线程移除不丢失）、事件在 compute 外触发且幂等翻转静默；航点 id 消毒（去点/空 id 拒绝/重名拒绝/上限 19）；`TeleportationManager` GUI 原子认领（runSync 内 add+open）+ 世界卸载航点跳过 + 失败不预占；`ResourceManager.getSupplies` 损坏数据回退空、`setSupplies` 负值校验、`generate` 的 maxDeviation 配置防零；`ButcherAndroid` 攻击已按 owner 门控逐实体权限（既往轮次）；`VillagerRune` 权限门+事件+单个消耗 ✓。

**发现并修复（2 项）**：
1. **远古祭坛 `altarsInUse` 永久泄漏（高危——不可信点击面）**：`onInteract` 在权限检查**之前**将祭坛位置加入 `altarsInUse`；`useAltar` 的两个权限失败路径（①祭坛自身无区域权限即 return；②8 基座之一无权限仅 `altars.remove`）都不清理该条目——任何已研究但无区域权限的玩家**单击他人祭坛即令其永久不可用直至重启**（`altarsInUse.contains` 恒真拒绝后续点击）。修复：两路径补 `altarsInUse.remove(altar.getLocation())`。测试 `TestAncientAltarPermissionCleanup`（2 项，红绿双向验证：预修复代码下断言"still marked as in-use"失败，修复后通过）。断言链设计：权限检查是 `useAltar` 第一条语句，测试先证伪 `hasPermission`（经注册的选择性拒绝 ProtectionModule——dough 聚合语义为任一 module 拒绝即整体拒绝）使拒绝分支成为唯一可达代码，规避 UNIT_TEST 本地化把所有消息渲染为 "Error: No language present" 的文本不可断言性。测试体系约束发现：MockBukkit 插件加载**不装配物品**（registry 0 项），须自建+注册测试用 `AncientAltar`（基座不注册——与 TestPedestalItemPlaceEvent 同因：BlockDispenseHandler 框架校验）。
2. **`getTeleportationTime` int 溢出断裂（表述与实现不符）**：`50_000 + complexity * complexity` 为 int 乘法；复杂度是玩家可堆叠量（T4 发射器 = y×64+2100，y=320 时单台 22580，**5 台即 92,682**）。该值处 `c² mod 2³² ≈ 1.85 万`使 speed 坍缩至 ~6.8 万——最远传送时间反跳回**上限 40 档（20 秒）**，而 4 台发射器时是 0.5 秒："复杂度越高传送越快"在真实可达值域上不连续断裂。修复：long 算术（`speed ≥ 50_000` 恒正、商 ≤ 8000 安全收窄回 int）。测试 `TestTeleportationTime`（4 项）：92,682 甜点（预修复红：得 40 而非 1）、100→300,000 单调非增扫描（预修复红：65,600 处 1→11 断裂）、<100 基线、距离边界。

**记录（上游语义，不改）**：`AncientAltarTask.finish()` 中 addon 取消 `AncientAltarCraftEvent` 时 8 件基座材料已被消耗且不返还（addon 应在 RitualStartEvent 阶段拦截；abort 路径有完整返还）；`AncientPedestal` 前缀识别（`&dALTAR &3Probe - &e`）理论上可被创造模式客户端伪造命名物品欺骗，但仅能"占用"基座/贡献自己的物品，无复制或窃取面；`GPSNetwork.openTransmitterControlPanel` 的 `1000D / l.getY()` 在 y=0 时为 Infinity（double 除零不抛），DecimalFormat 输出 "∞"，纯外观。

**第二十大清扫确立**：多方块结构"预标记-后校验"模式的失败路径完备性（ altar 的 5 条失败路径全部核对：自身权限/基座权限/基座数/无效催化剂/配方不识别+任务侧 abort/finish 清理）。

**回归**：全量 **3178 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 31 轮（2026-08-27）：电梯交互、SoulboundRune 绑定、ChatInput 不可信聊天、机器人脚本持久化（1 项修复）

**电梯面复核（既有加固在位）**：`TeleporterListener` 入口（PHYSICAL 交互 + canUse 门 + 传送塔 owner UUID 解析守卫 + 八向塔检查）✓；`users` 集合全主线程访问（teleport 的 add/remove 均 runSync，teleportAsync 回调包裹 runSync）✓；传送失败清条目防合法使用被吞、改名幽灵数据守卫（方块已破不写楼层数据）✓——均为既往轮次成果。

**发现并修复（1 项）**：`ElevatorPlate.renameFloor` 缺 **owner 重校验**——编辑器打开时是 owner 门控的，但聊天回调可能数秒后才到达：窗口内电梯板被他人破坏并重放（新 owner 写入 BlockStorage）后，旧玩家的迟到消息仍会改写新主人的楼层名（授权漂移型 grief）。修复：在"仍是电梯板"检查后追加 owner 严格比对（null owner 也不匹配=fail-closed；真实电梯板经 BlockPlaceHandler 必有 owner，legacy 无 owner 板的编辑器本就不可达，行为无变化）。测试：`placePlate` 助手补记 owner（镜像真实放置处理器），新增漂移用例（非 owner 改名：事件不触发/名称不变/编辑器不重开），红绿双向验证（预修复代码下"事件必须不触发"断言失败）。

**SoulboundRune 复核（干净）**：双段延迟（20L+10L）逐段重校验（符文/目标实体有效性、数量仍为 1）；先 remove 双实体再落地绑定物品（无复制窗口）；堆叠物品排除；`pickupDelay<=0` 排除刚丢物品；事件可改向目标且二次校验采用改向后实体 ✓。

**ChatInput（dough 外部 jar，字节码审计）**：命中谓词即消费处理器并 `runTask` 调度到**主线程**执行回调、事件取消（输入不广播、命令不执行）；按玩家 Set 支持多并发等待（每条消息只消费一个处理器）；onLeave/onKick/onDisable 全清理（无泄漏）；默认谓词恒真（任意聊天行或命令均被捕获为输入）。Slimefun 四个使用点（电梯改名/航点命名/脚本命名/指南搜索）回调全在主线程 ✓。

**机器人脚本持久化复核（大体干净）**：`uploadScript` 在**点击时**捕获代码（迟到回调不重读方块，无内容漂移）；重复代码上传拦截；UUID 文件名（既往轮次防路径穿越）；评分写时一人一票防重；加载逐文件异常隔离；r29 后 `.sfs` 写入原子 ✓。

**记录（上游语义，不改）**：同玩家两个并发上传等待的 id 碰撞仅以相同内容覆盖自己的文件（无害）；脚本文件数无上限（仅受聊天速率约束的磁盘缓慢填充，上游设计）；电梯楼层选择器分页公式 `1 + size/27` 使 27 整数倍楼层多出空页（纯外观）；传送成功但玩家未触发目的地板 openInterface 时 users 条目残留一次（下次使用被吞一次后自愈）。

**第二十一大清扫确立**：聊天迟回调（ChatInput await 窗口）的授权与状态漂移面——三处使用点逐一核对（电梯改名已修复、脚本上传点击时捕获无漂移、航点写入经 profile 属主绑定无漂移）。

**回归**：全量 **3179 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 32 轮（2026-08-27）：货运配置界面、机器配方伪造物品、ItemStack NBT/PDC 深度解析面（1 项修复）

**货运配置界面复核（干净）**：`AbstractFilterNode`（可否决 CargoNodeFilterChangeEvent、守卫式 filter-type/lore 读取、配置变更 markCargoNodeConfigurationDirty 网络重扫）；`AbstractCargoNode` 频道选择器 0-15 有界循环 + `getSelectedChannel` 解析 NumberFormatException 回退 + clamp(0..16) + 可否决频道事件；`canOpen` 区域权限门（slimefun.cargo.bypass 逃生门）；过滤槽物品仅为比较数据（ItemStackWrapper，无反序列化执行面）。`ItemFilter` 损坏 filter-type **fail-closed**（防白名单翻转为黑洞路由）、半载快照拒绝、MAX_FAILED_UPDATES 防每 tick 报错风暴 ✓（既往轮次加固在位）。

**线程模型核验（关键排除）**：`CargoNet` 调度 `CargoNetworkTask` 经 `Slimefun.runSync`——**主线程**执行；`AbstractAutoCrafter` ticker `isSynchronized=true` 亦主线程 → 同一原版箱子库存上无跨线程竞争（自动合成器 vs 货运抽取可安全交错）。

**ItemStack NBT/PDC 解析面复核（干净）**：`LimitedUseItem.damageItem` 已钳制伪造 PDC 使用次数至 [1, max]（既往轮次）；`ChargeUtils.getCharge` 已防御负值/NaN/无限 PDC 充电值（防异步 ticker 异常销毁机器）；`CustomItemDataService` 纯字符串 id 读取 + 注册表解析（未知 id→null）；`VanillaAutoCrafter` 损坏配方键回退 null（防 ticker 错误累积销毁方块，既往轮次）；`SlimefunAutoCrafter` RecipeType 漂移 null 守卫（既往轮次）；灵魂绑定为 has(BYTE) 探测。创造模式客户端伪造 SF-id PDC 标签属固有限制（等同创造模式给物品，超出插件责任域），记录不改。

**发现并修复（1 项）**：`AbstractAutoCrafter.craft()` **消费后失败路径物品蒸发**——`AutoCrafterCraftEvent.setResult`（5.0 新公共 API 事件）给定超堆叠数量（如 128）结果时，预检查 `firstEmpty()!=-1` 通过但 `addItem` 溢出：原料已被扣减（`item.setAmount(remaining)` 先行执行）、返回 false 使溢出结果与桶类副产物（#2926 路径仅在 success 时返还）**全部静默虚空**。修复：确立不变式"**返回 false ⟺ 未消耗任何原料**"——结果溢出与桶余物一律经 `dropLeftover` 落到容器位置（双箱/方块定位既有），消耗发生即返回 true。测试 `TestAutoCrafterCraftOverflow`（2 项）：正常合成（结果入库、桶余物不丢、零掉落）与超堆叠溢出（128 铁锭全部可盘点：箱内+地面合计=128、桶余物合计=1），红绿双向验证（预修复代码下"craft consumed and produced"断言失败，返回 false 且 64 铁锭+桶蒸发）。测试细节：MockBukkit `addItem` 会堆叠填充物，须 `setItem` 逐格占位以构造"恰一空格"场景。

**AContainer 复核（干净，既往轮次在位）**：负结果缓存带虚分发恢复（防自定义机器永久停摆）、失败扫描缓存随输入/配方表变化失效、破坏时清理缓存与操作。

**第二十二大清扫确立**：5.0 新 API 事件（AutoCrafterCraftEvent.setResult 等）修改值越过服务端校验假设的"事件后置不变式"面——改向结果的数量/类型由调用方负责，但插件须保证已消费状态与产出记账一致。

**回归**：全量 **3181 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 33 轮（2026-08-27）：多方块机械并发合成、爆炸/破坏一致性、背包与 PlayerProfile 命令族（1 项修复）

**多方块机械复核（干净，既往轮次在位）**：`MultiBlockMachine` 交互处理器 canUse+区域权限门；`handleCraftedItem` 迟滞产出**重解析容器**（发射器中途被破→重取 BlockState，容器消失则落地，防写孤儿 TileEntity 虚空物品）；`load()` 配对校验（displayRecipes 偶数断言）+禁用物品过滤。多玩家并发提交：交互事件主线程串行、单事件内消费-产出无调度让出点 → 无交错窗口（迟滞类已由重解析覆盖）。多方块机械为 `NotPlaceable`（无 BlockStorage 数据）——爆炸只毁原版结构，**无幽灵数据面**（对照 r28 实机 `/fill` 幽灵数据坑，该坑仅影响有 BlockStorage 数据的 SF 方块）。

**爆炸/破坏一致性复核（干净，既往轮次在位）**：`ExplosionsListener` 全链——先取掉落再 `destroy`（防止 clearBlockInfo 后读不到库存而**虚空机器内容物**，注释明确记录该次序约束）、`destroy` 先 `updateAllNetworks` 再清数据（防连接器缺口网络不重分类/死网络滞留）、`WitherProof` 排除、`SlimefunBlockExplosionEvent` 可否决、`isExplosionAllowed` 逐机器门控、凋灵专路径（#3414 幽灵方块）。

**背包命令族复核（大体干净）**：`BackpackCommand` 权限门 + NUMERIC 正则（r48）+ `hasPlayedBefore`（防任意用户名建档）+ 背包存在性检查 + runSync 交付；`BackpackListener` 为审计最深文件之一复核在位——**IDOR 修复**（伪造 lore 行 "ID: <victim>#<id>" 开他人背包已被 owner/绕过权限校验拦截）、拖拽事件独立校验（非 ClickEvent 族）、光标物品校验（空槽放潜逃逸）、数字键/副手/Shift 全覆盖、open-before-claim 次序（防 onClose 误删刚注册的视图致脏标记丢失）、异步档案线程全 runSync；`PlayerBackpack.setSize` 界内校验（9-54 步进 9）+ 缩容毁物拒绝 + 可否决事件。

**发现并修复（1 项）**：`BackpackCommand` 恢复背包 `addItem` 余量被忽略——**管理员背包满时恢复的背包静默蒸发，且命令已报告"已给予"**（表述与实现不符 + 数据绑定物品丢失，#3476 同型）。修复：余量经 `dropItemNaturally` 落到发送者脚下。测试 `TestBackpackCommand` 新增满背包用例（全 41 格 `setItem` 填充——MockBukkit 的 `addItem` 会使用盔甲/副手槽，须全填；断言：库存确未收纳 + 地面出现绑定正确 ID 的恢复背包），红绿双向验证（预修复代码下"must be dropped"断言失败）。

**记录（上游语义，不改）**：恢复命令为权限门控的管理员功能，会产生共享同一背包 id 的第二个物品句柄——两个句柄打开同一 `Inventory` 实例（Bukkit 视图同步，无复制面），误用（原包未真正丢失时恢复）仅造成共享访问而非复制。

**第二十三大清扫确立**：命令族"报告成功但交付物蒸发"面——addItem 余量处理在命令路径的全量清点（其余命令族经查或无物品交付或已有余量处理）。

**回归**：全量 **3182 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 34 轮（2026-08-27）：NuclearReactor 熔毁时序、GEOMiner 挖取一致性、HologramProjector 编辑输入面（1 项修复）

**反应堆面复核（干净，既往轮次在位）**：`Reactor` 全链——接入端口 **owner 隔离**（陌生人上方端口不得跨界补给燃料/接收副产物）、损坏 charge 数据解析回退（防 ticker 异常销毁）、`explosionsQueue` 为 CHM keySet（单 ticker 线程添加/移除，runSync 闭包只读）、`ReactorExplodeEvent` 先于移除全息/关闭菜单、水冷随机邻检经 runSync、冷却消耗可否决事件、副产物输出槽满经 runSync 落地（不虚空）、模式切换可否决、发电满载时安全怠速（不进度不耗冷不查水）✓。爆炸队列残留（反应堆在排队后卸载/被破坏）为每物品类每位置至多一条的有界残留，记录不改。

**GEOMiner/GEO 挖取复核（干净）**：`GEOMiningStartEvent.setConsumedSupplies` **setter 层已校验**（≥0 且 ≤现存量——负值供给膨胀与超量 Validate 抛 ticker 均不可达）；供给数据全走 BlockStorage chunk info（与区块加载状态解耦，无卸载一致性面）；产出槽满余量经 runSync 落地；挖取完成/开始事件可否决；电容/速度/耗能注册前校验防零。中途破坏丢失已扣供给与上游"破坏丢进度"语义一致（AContainer 同型），记录不改。

**发现并修复（1 项，r31 同型授权漂移）**：`HologramProjector` 两个编辑入口均缺 owner 重校验——①`updateText`（聊天迟回调）：编辑器打开时 owner 门控，但文本经 ChatInput 数秒后到达，窗口内投影仪被他人破坏重放后旧玩家仍能改写**新主人的全息文本**（与 r31 电梯 renameFloor 完全同型，且旧实现只查"仍是投影仪"#3445）；②`updateOffset`（编辑器点击路径）：编辑器每次调整后**重开**，旧 owner 的菜单可在投影仪易主后无限期继续调整。修复：两方法入口补 `isOwner` 严格比对（null 不匹配=fail-closed；真实投影仪经放置处理器必有 owner）。测试：两个既有测试类的 `placeProjector` 助手补记 owner（镜像真实放置处理器），新增文本/偏移两个漂移用例（非 owner：事件不触发、存储值不变），红绿双向验证（守卫禁用下文本漂移用例"事件必须不触发"失败——同一守卫服务两处）。

**记录（上游语义，不改）**：全息文本长度受聊天协议 256 字符自然约束（无存储放大面）；玩家自有投影仪偏移可无限累计（±0.1/点击，纯外观无服务端危害）；`GEOMiningOperation` 的 addon 越界 consumedSupplies 仅剩子类覆写 getter 的病态路径（超出公共 API 契约责任域）。

**第二十四大清扫确立**："打开时门控、迟到达执行"的授权漂移面第三例（电梯 r31 → 全息 r34）——ChatInput 迟回调与"编辑器重开循环"两类窗口均已覆盖；ChatInput 使用点至此全部审计（电梯/全息/航点/脚本四处理论窗口：电梯与全息已修，脚本上传点击时捕获无漂移，航点写入经 profile 属主绑定无漂移）。

**回归**：全量 **3184 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 35 轮（2026-08-27）：Cooler/Soulbound 死亡保留时序、MultiTool 模式与充能时序、EnergyNet 高密度逐 tick（零缺陷轮）

**Cooler/Soulbound 交互时序复核（干净，既往轮次在位）**：`SoulboundListener` 全链——`keepInventory` 死亡防复制（含光标 addItem 复制路径注释）、光标槽伪槽位存储（否则光标灵魂绑定物品直接蒸发）、**死亡后断线先返还**（防物品滞留内存随下次登录丢失；map 经 remove 排空防复制）、复活时槽位被占则合并入包+余量落地（不覆盖不虚空）、可否决 KeepEvent 含逐物品排除。`CoolerListener`——喂食 owner 门控（偷来的冷藏包不再喂贼，3941fe229）、经 profile 异步回调后 runSync 消耗、消耗时**重扫**背包库存（防过期槽位）、`CoolerFeedPlayerEvent`/`CoolerSaturationEvent` 可否决、消耗后 markDirty。

**MultiTool 模式切换与充能时序复核（干净）**：模式索引 PDC 读取带损坏回退（越界→0）；`MultiToolModeSwitchEvent.setNextIndex` **setter 层已校验** `[0, modeCount)`（r32 族"事件改值越界"不可达）；`nextIndex` 循环跳过禁用模式（防全禁用死循环——`index != i` 终止条件）；模式行 lore 更新带遗留英文前缀正则兼容与短 lore 越界守卫；充能语义为"先扣费后委托"（每次使用 0.3J，委托目标不适用则照扣=上游语义）；实体修剪防护（#2217）。

**EnergyNet 高密度逐 tick 复核（干净）**：结算线性复杂度 O(发电机+电容+消费者)（HashMap 遍历 + O(1) 缓存 Config 读，无 O(n²) 面）；**逐消费者异常隔离**（损坏电荷/misbehaving addon 不中止结算环——注释明确"先充后异常=每 tick 复制能量"的机理）+ `failedComponents` 报告去重（每 tick 重试自愈、分类变更清理）；网络合并仲裁（双调节器下最小 Location 决定唯一 tick 者，防发电机双燃料消耗）；`discoverStep` 后**已注销网络不结算**（防重建期双充/双供）；`unpooledGenerators` 契约（EnergyGenerateEvent 被取消的发电机保留存储电荷，不被剩余分配覆写）；供需溢出安全加法。

**记录（上游语义，不改）**：多冷藏包过量消耗（一次饥饿事件每个冷藏包各消耗一瓶果汁）——经 fork 基线（497359568）比对确认上游原版同样循环不 break，玩家自伤型资源浪费，无跨玩家面；充能工具"先扣费后委托"的失败不退款。

**第二十五大清扫确立**："事件可改值"族的 setter 层校验全量清点至此闭合——GEOMiningStartEvent（r34 核）、MultiToolModeSwitchEvent（本轮核）均在 setter 层校验，AbstractAutoCrafter 结果（r32 修）为唯一漏网已修；其余事件族（Reactor/Elevator/CargoNode/Backpack/Hologram 等）的改值均为枚举/布尔无越界面。

**回归**：本轮零代码改动；全量基线维持 **3184 项通过**（第 34 轮全量回归）。

## 第 36 轮（2026-08-27）：Talisman 触发/消耗时序、电力机器族、addon API 兼容层（零缺陷轮·连续第二个）

**Talisman 族复核（干净，既往轮次在位）**：`Talisman.trigger` 链——概率门 → 事件玩家解析 → canEffectsBeApplied → 主背包含相似物优先、否则末影变体（**末影护符以其自身 canUse 门控**——基础变体可用而末影变体不可用的玩家不能经末影箱白嫖，既往修复）；`TalismanActivateEvent` 可否决且含 preventsConsumption；效果经 `TalismanEffectApplyEvent` 可否决/可修改；消耗仅一枚（isConsumable 门）；事件取消经 Cancellable+isEventCancelled。监听器侧：铁砧护符**先定位破损物品槽位再触发**（注释明确"先触发后定位会消耗护符却无处修复"的浪费机理）+ 延迟一 tick 恢复带占位重检（防覆盖玩家放入的新物品）；矿工护符/旋风/巫师附魔调整等处理器均已加固。

**电力机器族复核（干净）**：`FoodComposter`/`ElectricPress` 等为纯 `AContainer` 声明式特化（仅配方注册），继承 r32/r33 已审计核心（负结果缓存+虚分发恢复、机器配方处理）；`ChargingBench` **实付实扣计费**（addItemCharge 钳制到物品最大电荷时仅扣实际交付量的 2 倍网络能量——注释明确"全扣会让未交付能量蒸发"机理）+ 可否决充电事件；`FluidPump` 先验 fits 再消耗、可否决收集事件（含容器替换）、水源/岩浆源方块语义。

**addon API 兼容层复核（干净）**：`callItemHandler` Exception|LinkageError 隔离（addon 处理器抛错仅 error 日志不杀调用方）；`ItemSetting.reload` 类型混淆防护（isInstance+List 特例先行检查再不安全转型，validateInput 后赋值）；事件可改值族 setter 校验（r34/r35 已闭合）。

**第二十六大清扫确立**：连续两个零缺陷轮（r35/r36）出现在交互密集面（Soulbound/护符/电动机器）——表明既往 34 轮+5.0.0 时代 81 轮的加固密度已使这些面的缺陷产出收敛；后续轮次应转向结构性更低层或更外围的面（如命令补全、世界观事件、guaranteed 冷路径如 BackupService 恢复流）。

**回归**：本轮零代码改动；全量 **3184 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 37 轮（2026-08-27）：命令 TabComplete 面、BackupService zip 冷路径、ItemDropHandler 族（1 项修复）

**TabComplete 补全面复核（干净）**：`SlimefunTabCompleter`——建议数上限 80（防巨型补全包）、全部 `args[i]` 访问有长度守卫、give/research/debug 补全项均为公开注册表信息（物品 id/研究键）、返回 null 回退原版在线玩家列表（标准语义）；补全不执行权限过滤仅暴露子命令名（上游语义，公开信息）。

**ItemDropHandler 族复核（干净）**：分发器 `ItemDropListener` 首个命中即返回（多处理器不叠加）；两个使用者均为已加固模式——`SoulboundRune`（r31 全核）与 `EnchantmentRune`（本轮复核）：丢弃后 20L 延迟执行包裹 try/catch（error 归因不逃逸）、延迟内实体有效性重检、数量==1 强制、可否决 `EnchantmentRuneApplyEvent`（含附魔/等级改向）、非法/冲突附移除、`isEnchantable` 配置尊重（#2878）、双段延迟二段再校验。

**发现并修复（1 项，r29 族冷路径延伸）**：`BackupService` **zip 直写最终时间戳文件名**——备份恰在服务器关机时运行（进程被 kill 概率最高的时刻），中途失败/被杀留下**截断的 zip**：其文件名匹配轮换过滤器，静默占据 MAX_BACKUPS 名额之一，管理员恢复时得到部分数据（灾难恢复工件自身损坏）。修复：先写 `<name>.tmp` 再 `ATOMIC_MOVE`（回退 `REPLACE_EXISTING`），失败清理 tmp——失败运行不留下任何"最终备份"痕迹。测试 `TestBackupService`（2 项）：失败路径经在 `stored-blocks/` 植入嵌套目录驱动（`FileInputStream(目录)` 在 zip 中途抛 IOException，跨平台成立），断言失败后无最终 zip/无 tmp 残留；正常路径恰好一个非空 zip 且无 tmp。红绿双向验证（预修复直写行为下截断 zip `2026-08-27-05-54.zip` 实际出现并被断言捕获）。测试经前后快照差集隔离预存文件、自清理产物。

**第二十七大清扫确立**：r29 崩溃一致性清扫的冷路径收尾——至此**全部落盘写点**（.sfb/.sfi/chunks/玩家档案/配置/脚本/备份 zip）均为 tmp+原子移入或等价原子语义。

**回归**：全量 **3186 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 38 轮（2026-08-27）：机器人执行循环与燃料时序、WorldEdit/保护集成边界、共享背包 id 边缘（1 项修复）

**机器人面复核（干净，既往轮次在位）**：tick 入口——被占用队列守卫（`isOccupiedSoon`：他者数据移动未决时 tick 会执行外者脚本并在 GO_FORWARD 物理删除其头）、NaN/Infinity 燃料解析回退、损坏 index/rotation 回退、未知指令跳过（防燃料空耗+日志风暴注释在位）；`move()`——目标占用/移出中双向守卫（注释明确"队列覆写会跨 owner 毁物转库存"机理）、世界边界、**owner 放置权限**（机器人不得走入他人领地）、可否决移动事件、皮肤延迟应用带类型重检、先置空再 moveBlockInfo 次序；`consumeFuel`——`AndroidFuelConsumeEvent.setFuelTicks` **setter 层校验正数**（r34/r35 族不可达）、液体燃料空桶满槽落地不虚空。

**发现并修复（1 项，r28 幽灵数据族）**：`WorldEditIntegration` **仅在世界方块被设为空气时清除 SF 数据**——`//set stone` 等非空气替换覆盖机器时 BlockStorage 数据残留为幽灵数据（旧 SF 数据挂在新方块上：交互误解析、菜单复活；与 r28 实机 `/fill` 发现同族，但此处有集成钩子可修而 vanilla 无）。修复：任何 `setBlock` 落点均清 SF 数据（WorldEdit 的剪贴板不含 SF 数据，粘贴的新方块不应继承旧方块的脚本/燃料/库存——语义上旧方块已被替换，数据必须随之消亡）；经新 `BlockStorage.clearBlockDataIfPresent(Location)`（线程安全原语：CHM 弱一致读 + 删除队列延迟移除，附 WorldEdit 编辑线程安全性的显式契约）。测试 `TestClearBlockDataIfPresent`（3 项）：有数据→排队→`drainQueues` 排空后消失、无数据→无操作零入队、**外部线程调用**不悬挂（线程契约验证）。诚实记录：WorldEdit extent 门控移除本身经编译+评审验证（WorldEdit 不在测试类路径，extent 回调不可单测）；行为核心（清除原语）已全测。

**EnderBackpack/RestoredBackpack 复核（干净）**：EnderBackpack 开玩家自己的末影箱（服务端解析，无共享 id 面）+可否决事件含取消时交互仍消费；RestoredBackpack 隐藏+NULL 配方+54 尺寸仅作用于新建占位（既有 id 解析走存储尺寸）；共享 id 双句柄开同一 `Inventory` 实例（Bukkit 视图同步无复制，r33 结论成立）。

**第二十八大清扫确立**：外部世界编辑器（WorldEdit）与 SF 数据的一致性面收口——替换即清除，不再区分替换物材质；vanilla `/fill`/`//set` 幽灵族至此只剩 vanilla 侧（无钩子，r28 已记 runbook 警告）。

**回归**：全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 39 轮（2026-08-27）：调试工具输入面、辐射时序、TableSaw/Juicer 消费-产出（零缺陷轮·第三）

**调试工具面复核（干净）**：`DebugCommand`——`slimefun.command.debug` 权限门 + 任意测试用例字符串仅存入（与枚举不匹配=无输出，无解析执行面）；`DebugFishListener`——物品匹配 + `slimefun.debugging` 权限门（创造模式伪造调试鱼无权限仅得无权消息，PHYSICAL/副手排除）。

**辐射面复核（干净，既往轮次在位）**：`RadioactivityListener`——死亡重置可否决（`RadiationResetEvent` 携带死亡前曝露量）+ 宽限期；`RadiationTask`——曝露累计 = 数量×修饰符、`RadiationExposureEvent` 缩放契约（符号必须保留、文档显式）+ `addExposure` **双端钳制**（上限 100；下限防 addon 巨值 int 溢出回绕为负——注释明确"持续负值会让 RadiationDamageEvent 的 >=0 校验每 tick 主线程抛错"机理）。

**TableSaw/Juicer 消费-产出复核（干净）**：TableSaw——手持单耗（创造豁免）、`MultiBlockCraftEvent` 含输出改向、输出经 `OutputChest.findOutputChestFor`（fits 检查语义）否则落地（无虚空路径）；Juicer——**全数量 `containsAtLeast` 校验**（注释明确引用 Composter/Crucible 同族修复：isItemSimilar 忽略数量，防"少耗全产"）、先验输出库存再消费、事件取消早退。

**第二十九大清扫确立**：多方块机器消费-产出一致性族至此全覆盖（r33 抽样 + 本轮 TableSaw/Juicer 补遗，Composter/Crucible/GrindStone/Smeltery/OreCrusher 等已在既有轮次与修复注释中体现）。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 40 轮（2026-08-27）：剩余集成边界、命令族深层、GEO 扫描/注册交互面（零缺陷轮·第四）

**集成边界复核（干净）**：ItemsAdder——`isCustomBlock`/`isCustomItem` 均安装门 + Exception|LinkageError 隔离 + logError 归因；消费者（ExplosiveTool 自定义方块战利品、BlockPlacer #3218 自定义物品放置拦截）判空守卫在位；McMMO——`isEventFaked` instanceof 假事件判别（防爆炸工具对 mcMMO 伪事件二次处理）+ `removeTemporaryEnchantments`（防 AutoDisenchanter 剥离临时增益附魔的利用面）同样门控隔离；PlaceholderAPI——纯只读占位符，档案读取经 `find`（缓存命中才读）+ `request`（未命中异步加载、当前返回"加载中"占位文本）——**不阻塞不强制同步加载**（r72 档案族成果）。

**GiveCommand/CheatCommand 深层（变更史证明，零改动）**：两文件自受审基线 ad5cd1277（r48 NUMERIC 正则/命令方块排除/cheat 权限门/结果上限、r81 GiveCommand 溢出捕获/excess 落地）以来 **git log 零提交**——结论原样成立，无需重审。

**GEO 扫描/注册交互面复核（干净）**：GEOScanner——区域权限门 + 委托 r30 已审计的 `ResourceManager.scan`（复杂度门 600/可否决 GEOScanEvent/事件页码容忍：越界页码经 `Math.min(GUI_SIZE, size-index)` 负值循环零迭代=空页不崩）；`ResourceManager.register` 重名抛 IllegalArgumentException（启动期显现）+ enabled 门 + r29 后原子配置保存。

**第三十大清扫确立**：第三方集成边界族（WorldEdit r38 修复后）至此全覆盖——六个软依赖（WorldEdit/ItemsAdder/McMMO/PlaceholderAPI/ClearLag/Orebfuscator）+ 保护模块族均已逐面核验，统一模式为"安装门 + 异常隔离 + 只读或事件化写"。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 41 轮（2026-08-27）：附魔机器族、ClearLag 清理边界、NetworkManager 重建/卸载（零缺陷轮·第五）

**附魔机器族复核（干净，既往轮次在位）**：AutoEnchanter/AutoDisenchanter 对称加固——等级帽超限在**有观看者时**警告并拒绝（无观看者静默跳过防日志风暴）、AutoEnchanter 过量附魔记账**含既有附魔数**（`preExistingEnchants` 计入 `isEnchantmentAmountAllowed`）+ 覆盖开关（不覆盖时移除目标已有同级/更高级）、addon 选择事件（清空选择即怠速）、**先验 fits 再消费**、消费前**活跃重校验**（异步 tick 与玩家菜单交互竞态——注释明确"两个 addon 事件+附魔计算期间物品可能被取走/调包，不重校验会免费附魔或耗错物品"）、AutoDisenchanter #3837 防复制（仅当移除验证成功才写入书，移除失败 SEVERE 归因）。物品损耗面：配方输出=原物品+书/附魔物品，无中间态丢失窗口。

**ClearLag 边界复核（干净）**：`EntityRemoveEvent` 中把带 `no_pickup` 旗标的 SF 掉落物（祭坛/基座仪式原料）从清理列表剔除——防止第三方清场插件蒸发现役仪式原料；玩家主动丢弃物（符文等可拾取物）属上游语义。

**NetworkManager 复核（干净，既往轮次在位）**：区块索引查找 + 无调节器线性回退；注销在 `chunkIndexLock` 内**先翻 registered 旗标**（防异步发现路径把死网络重新索引）；`networks` 为 CopyOnWriteArrayList（迭代中注销安全）；世界卸载全清除（含区块索引整体移除——注释明确"死网络不得阴影重载世界后的新网络"）+ `WorldListener.onWorldUnload` 接线；`updateAllNetworks` 异常隔离 + 空/非 SF 快速路径。

**第三十一个大清扫确立**：第三方清场/世界生命周期与 SF 状态一致性族收口（WorldEdit r38 + ClearLag 本轮 + 世界卸载网络/队列双清）。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 42 轮（2026-08-27）：TickerTask 错误报告资源面 + v5.1.3 发布闭环（审计部分零缺陷）

**TickerTask/错误报告资源面复核（干净，既往轮次在位）**：`running` AtomicBoolean CAS 防异步定时器超期重叠（注释明确机理）；删除/移动队列**逐条隔离**（单条毒 entry 不再永久停滞全部 ticking——注释明确"同一 entry 每 run 重现"机理）；同步 tick 批量单次提交（保序+每块独立时间戳与 try/catch）；`ErrorReport.getNewFile` 时间戳命名+碰撞计数，生成频率受"每机器 4 错销毁"上限约束（按机器数有界，上游语义）。

**5.1.3 发布**：pom bump 5.1.2→5.1.3；实机 Paper 1.21.11-132 部署 `SlimeFun4.1-5.1.3.jar`——runbook §1 全过：Done(20.5s)、Slimefun 5.1.3 装载、ERROR/SEVERE 0 条、语言/GEO/标签/物品/研究必查标记全在、禁查模式 0 命中（日志存 /f/paper-test-1.21.11/boot-5.1.3.log）。修复型发布无数据格式/行为协议变更，以 §1 为回归门槛（bot 全链路属 v5.1.1/5.1.2 初次部署验证范畴）。环境注记：后台服务器进程随父 shell 终止（r28 已知怪癖），关机备份未及运行（环境性非插件面，原子备份行为已由单测覆盖）；仓库 CWD 误启动的服务器残留文件已清理。发布说明 note/release/5.1.3.md（数据完整性/交互修复/实机回归三段，按规则不强调测试过程）。

**回归**：全量 **3189 项测试通过、0 失败、0 错误、7 跳过** + 实机 §1（Java 21 BUILD SUCCESS）。

## 第 43 轮（2026-08-27）：SlimefunGuide 搜索/书签输入面 + v5.1.3 实机长时浸泡（零缺陷轮·第七）

**指南输入面复核（干净，r11 内存面 + 本轮输入面）**：搜索链——`SlimefunGuideSearchEvent` 可否决且可改向词条、菜单标题经 `ChatUtils.crop` 裁剪（聊天输入 ≤256 自然有界+裁剪双保险）、词条 `stripColor+toLowerCase(ROOT)` 规范化后入历史（GuideHistory 200 有界，r11）、结果 44 槽上限、`SlimefunGuideSearchFilterEvent` 逐项过滤钩子；指南物品识别（`isGuideItem`）经物品相似度而非可伪造单一标记。

**v5.1.3 实机浸泡（通过）**：Paper 1.21.11-132 + `SlimeFun4.1-5.1.3.jar`（Java 21）——启动完成后连续浸泡 **6.5 分钟**，`[ERROR]/SEVERE/Exception` **全零**（含非 Slimefun 面），Slimefun 专属错误零条——达到并超出 r28 七分钟浸泡的 Slimefun 零错误判据（本轮全源零错误）。日志存 /f/paper-test-1.21.11/soak-5.1.3.log。

**环境注记（重要，两次复现）**：Git Bash 会话拆解会杀死分离启动的服务器进程（r42/r43 双复现）——跨 Bash 调用的长浸泡不可行，浸泡必须在单次调用内完成（本轮范式）；RCON stop 发送成功后会话即结束，优雅停服日志与关机备份未及落盘（r37 原子备份行为已由单测红绿覆盖，实机路径留待人工验证）。清理残留锁时 `taskkill /F /IM java.exe` 可能波及非服务器 java 进程——后续环境操作须按 PID 精确定位。

**回归**：本轮零代码改动；全量基线维持 **3189 项通过**（r42 全量）。

## 第 44 轮（2026-08-27）：ChestTerminal 货运终端面与物品注册/禁用生命周期（零缺陷轮·第八）

**ChestTerminal 面（空洞成立）**：本 fork **不存在 ChestTerminal 物品**（上游的终端大库存交互/物品同步面已被移除）——`AbstractItemNetwork` javadoc 的提及为遗留残留；频道 16 仅作为选择器图标语义存在于 clamp（`getSelectedChannel` 0-16），`CargoNet.mapInputNodes/mapOutputNodes` 仅路由 0-15。审计面按"无此面"记录。

**物品注册/禁用生命周期复核（干净）**：`register()` 状态机——依赖检查/冲突检查/preRegister → 注册表三重登记（全物品/id→物品/材质集）→ 非 `NotConfigurable` 物品写入 Items.yml 五默认键并逐 `ItemSetting.reload()`（读路径 dough Config，写路径 r29 已原子化）→ tickers 总开关与 `.enabled` 配置解析为 `ENABLED/DISABLED/VANILLA_FALLBACK` 三态；**禁用即清空全部 itemHandlers 并从 `TickerBlocks` 摘除**——已放置的禁用物品方块：不再 tick（ticker 注册表项已除）、交互处理器已清（菜单不可开，BlockStorage 数据惰性化）、`VanillaItem` 回退原版行为——禁用一致性成立；`isDisabled()` 对未注册调用有显式 error 守卫；运行期注册（auto-loading）仅对 ENABLED 态调用 `load()`。

**记录（上游语义，不改）**：禁用物品的 BlockMenuPreset 在构造期注册仍驻内存（已放置方块的数据面保留，方块破坏后经既有路径清理）——与上游一致的"禁用=停止行为但保留数据"语义。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 45 轮（2026-08-27）：配方冲突检查/RecipeType 族、WorldListener 区块时序、r29-r38 修复路径 benchmark 复测（零缺陷轮·第九）

**配方冲突/RecipeType 族复核（干净）**：`checkForConflicts` 为注册期 id 冲突即抛 `IdConflictException`（启动可见，配合 r-早前的重复 id WARNING 扫描）；`RecipeType.register` 双路径——consumer 自定义或 `MultiBlockMachine.addRecipe`（输出非空校验，r33 核）；Juicer 族消费侧 `getRecipeInputs/getRecipeOutput` 一致（r39 全数量校验核）。

**WorldListener 区块时序复核（干净，既往轮次在位）**：加载——`WorldSettingsService.load` + `BlockStorage.getOrCreate`；卸载——**先 `drainQueues`（删除/移动队列排空进 BlockStorage）再 `saveAndRemove`**（注释明确"排队条目随内存死亡→磁盘数据复活已破方块并二次掉落库存"机理）→ 网络/区块索引全清 → ticking 位置摘除。与 r38 的 `clearBlockDataIfPresent` 删除队列语义衔接一致。

**benchmark 复测（性能无回归证据，results-5.1.3-r45.txt）**：5.1.3 当前构建 vs 5.1.1 r7 基线（同机同 JDK21）——ticker 5000 方块 0.796ms（基线 0.804）持平；machine-processing/junk-input/charging-write/hologram-label 均持平或更优；charge-write/save-5000/place/break/capacitor-texture 中位差 ±3-8%（历史运行间方差带内）。唯一越带指标 `energy-settlement/saturated-skip`（158 vs 历史带 49-97）：**git diff 证明整个 energy 包 v5.1.1→HEAD 零字节改动**，且同一 5.1.1 二进制的历史运行本身摆动 2 倍——定性为会话级环境噪声（长会话 JVM/负载状态）而非代码回归。r29-r38 修复所涉路径中可基准化的（save/machine/interaction/charge）全部在带内；Config#save 原子化不在此场景集（其开销为一次性 rename，微秒级）。

**回归**：本轮零代码改动；全量基线维持 **3189 项通过**（r44 重跑）。

## 第 46 轮（2026-08-27）：DeathpointListener/Waypoint 生命周期、GuideSettings 输入面、模板缓存与 ItemStackWrapper（零缺陷轮·第十）

**死亡标记/航点生命周期复核（干净）**：死亡标记 id 时间格式为**秒级精度**（注释明确"分钟级会同分钟双死碰撞而被重名静默拒绝"——既往修复在位）；写入经 `GPSNetwork.addWaypoint` 全链（19 上限含 +2 余量、id 消毒去点、重名拒绝、事件后写入）；生命周期一致性——上限 19 有界增长、面板手动删除、持久化经 r29 原子 `savePlayerData`、未解析航点（世界未加载）在保存时**保留不丢**（LegacyStorage 既有语义）。

**GuideSettings 输入面复核（干净）**：设置界面为纯内部开关族（指南模式/烟花/学习动画/语言）经 `SlimefunGuideOption` 处理器切换（PDC 写在指南物品上）——**无自由文本输入面**；语言选择为列表菜单非聊天输入；贡献者视图只读。

**模板缓存与 ItemStackWrapper 复核（干净）**：`SlimefunItemStack.item()` 返回 `delegate.clone()`——模板**永不外泄可变引用**（无跨玩家模板污染面）；`ItemStackWrapper` 不可变契约（变更方法抛 ERROR_MESSAGE、meta 单次缓存、clone 零拷贝快路径）——高负载物品比较（货运过滤/配方匹配）下无一致性行为；`CustomItemDataService` 为无状态键读写（r32 复核过伪造面）。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 47 轮（2026-08-27）：Research 解锁异步流程、主命令分发与预处理事件、PlayerProfile 异步缓存（零缺陷轮·第十一）

**Research 解锁异步流程复核（干净，既往轮次在位）**：`PlayerResearchTask` 全链——已解锁早退；**依赖强制在本任务层**（注释明确"指南/知识之瓶分享/`/sf research all`/直接 unlock() 全部汇入此任务，无一可绕过研究树"）+ 取消补偿；`ResearchUnlockEvent` 可否决且**扣费补偿**（cancelHandler——"指南已扣的等级不由插件凭空吞没"）；`currentlyResearchingPlayers` CHM set 去重防并发双开研究；**中途下线重解析档案**（注释明确"捕获的档案可能已被 auto-save 卸载，孤儿档案上的 setResearched 永不持久化=等级已扣而研究丢失"；PlayerProfile#get 会从磁盘重载）——r16 孤儿档案族的延伸成果。

**主命令分发复核（干净）**：线性子命令匹配（~15 个，equalsIgnoreCase 首参）；`PlayerCommandPreprocessEvent` 特判 "/help slimefun" 后**取消事件**（该串非真实命令，无 Bukkit 二次分发=无重复执行面）；各子命令权限门在 SubCommand 层（r48/r81 已深审输入密集族）。

**PlayerProfile 异步缓存内部复核（干净，r16 结论维持）**：`loading` 回调队列为 CHM（并发加载合并）；`saveLock`/queue 同步块正确收敛；三退避必终止、回调必解决、加载中退出收口（r16 全核）。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 48 轮（2026-08-27）：KnowledgeTome 分享边界、AutoBrewer 一致性、剩余魔法物品族（零缺陷轮·第十二）

**KnowledgeTome 复核（干净，既往轮次在位）**：lore 剥离/短 lore 优雅中止（防 NPE/IOOBE）；绑定可否决；自享拒绝；分享事件可否决且可改向来源；**消费前捕获副本 + 双档案失败退款**（注释明确"任一档案加载失败则研究从未复制，无 failureHandler 时已消耗的书凭空蒸发"）；消费仅非创造；等级结算面——分享路径经 `research.unlock(p, true)` 即时解锁（无 XP 扣费，XP 成本在指南解锁路径——上游语义）。

**AutoBrewer 复核（干净）**：药水/原料槽位自适应；**命名原料拒绝**（防定制物品误酿造）；药水 meta **克隆后变异**（注释明确测试环境 getItemMeta 可能不复制）；先验 fits 再可否决 `AutoBrewEvent`；消费前**活跃重校验**（异步 tick vs 菜单交互——与 AContainer/附魔双机同模式，r32/r41 家族）。

**剩余魔法物品复核（干净）**：MagicEyeOfEnder——护甲门控 + 可否决发射 + 速度改向；WindStaff——饥饿门控（≥2）、**否决先于消耗**（"no velocity, no hunger"注释）、`FoodLevelChangeEvent` 尊重其它插件取消、MultiTool isItem 守卫防多功能工具代扣饥饿；两事件的 `setVelocity` 均有**非空+有限分量校验**（NaN 向量会使玩家客户端位置损坏——r34/r35 事件改值族守卫在位）。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 49 轮（2026-08-27）：ExplosiveTool 爆炸深链、SlimefunRegistry/auto-loading、剩余工具族（零缺陷轮·第十三）

**爆炸工具深链复核（干净，既往轮次在位）**：`canBreak` **isEmpty 首查**天然过滤同 tick 已破方块（两把爆炸镐相邻同 tick 挖重叠区域：第二把的 findBlocks 收集到已变空气的中心块即被跳过——防双重掉落/双重处理器调用）；不可破坏标签/世界边界/破坏权限逐块门控；ItemsAdder 自定义方块先取战利品再移除（r40 核）；SF 方块经**哑 BlockBreakEvent 委托 BlockBreakHandler**（#2989 上下文/#3036 通用处理），事件未被取消才收掉落→置空气→清数据（与 ExplosionsListener 相同的"掉落先于清数据"次序）；`BlockExplodeEvent` 构造版本适配（1.21+ 带 ExplosionResult，pre-21 反射回退）；逐块耐久损耗受 damageOnUse 设置门控。

**SlimefunRegistry/auto-loading 复核（干净）**：热路径映射（profiles/worlds/chunks/mobDrops/materials）均为并发容器；注册表（ids/presets）为 HashMap——注册仅发生在主线程（启动期 + addon 运行期注册契约），可接受；auto-loading 仅对 `state==ENABLED` 的运行期注册调用 `load()`（r44 核），PostSetup 后置位。

**剩余工具族复核（干净）**：GrapplingHook——**#740 副手弓复制守卫**、抓钩状态去重（isGrappling 门）、发射事件可否决；KnowledgeFlask——**事件抬高等级成本超出现有时 fail-closed**（"扣成负等级"机理注释，r34/r35 事件改值族守卫）、可交互方块排除。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 50 轮（2026-08-27）：PostSetup 后处理链、VanillaItem 回退语义、剩余监听器族（零缺陷轮·第十四）

**PostSetup 后处理链复核（干净）**：`loadItems` 逐项 `item.load()` 带 Exception|LinkageError 隔离（单坏物品仅 error 日志不阻断装配）+ null 物品迭代器移除；`SlimefunItemRegistryFinalizedEvent` 后装载矿石研磨/冶炼交叉配方；wiki.json 装载带流解析与 SEVERE 回退（缺文件不阻断启动）；itemCfg/researchCfg 保存经 r29 原子化（ConfigUtils.saveAtomically）。

**VanillaItem/回退语义复核（干净）**：`VanillaItem` 仅叠加 workbench 可用语义；`.enabled=false` 时注册状态机解析为 `VANILLA_FALLBACK`（r44 核）——物品回退纯原版行为；`useVanillaBlockBreaking` 边缘：爆炸工具对回退/原版破坏方块走 `breakNaturally`（r49 核），SF 破坏走处理器委托——分支语义一致。

**剩余监听器族复核（干净）**：`ItemPickupListener`——`no_pickup` 旗标取消（可否决 ItemPickupPreventEvent）+ **祭坛探针物品在拾取尝试时清除**（防前缀物品永驻世界）；实体/漏斗双路径覆盖；`IronGolemListener`——SF 铁锭不可治傀儡（VanillaItem 豁免）、可否决治疗事件、手动重置手部物品同步客户端。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 51 轮（2026-08-27）：交叉配方装载语义、BiomeMap 资源冷路径（零缺陷轮·第十五）

**交叉配方装载复核（干净）**：`loadOreGrinderRecipes`——多方块配方表按对（输入/输出）拆分、null 过滤、**非 legacy 模式按输入数量降序排序**（注释明确"8 圆石→1 沙须优先于 1 圆石→1 沙砾"——机器取首个匹配配方，大数量优先防遮蔽）；`loadSmelteryRecipes`——同型配对 + 临时冶炼炉复制 + **电冶炼配方按输入长度降序**（多料配方优先，防单料遮蔽多料）——遮蔽次序语义均正确且带机理注释。

**BiomeMap 资源冷路径复核（干净）**：`BiomeMapParser`——JSON 解析异常包装为 `BiomeMapException`（启动期可见而非半初始化状态）、宽容模式（`isLenient`）兼容前瞻生物群系（新版 biome 键在旧服务端不致崩启动）、仅解析**内置捆绑** JSON（无用户输入面）；GEO 资源装配为启动期一次性（量有界）。

**更长时浸泡选项**：r43 已记录环境上限——单次调用内 6.5 分钟为本环境实际天花板（会话拆解杀分离进程双复现）；更长浸泡需您在自有终端运行服务器（`/f/paper-test-1.21.11` 下 `/f/Java/21/bin/java -jar paper.jar`），期间 `grep -cE "ERROR|SEVERE" logs/latest.log` 为零即为通过。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳在**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 52 轮（2026-08-27）：AGenerator 燃料结算与油泵联动、GuideHistory/BookBinder、实体防御监听收尾（零缺陷轮·第十六）

**AGenerator 族复核（干净）**：燃料结算——可充电机型**容量空间门控**（空间不足不加进度不发电）、桶类副产物经可否决 `GeneratorProduceByproductEvent`、`GeneratorFuelBurnEvent.setTicks` **setter 层校验 ≥1**（r34/r35 事件改值族）；findRecipe 与 Reactor 同型（数量记账）。OilPump 联动为 GEO 供给消费者（r30/r34 GEO 语义已核：供需解析防损坏、写时校验、挖取事件 setter 界内）。

**GuideHistory/BookBinder 复核（干净）**：`GuideHistory` 200 条有界双端队列（r11）且为**内存态设计**（本 fork 无书签持久化面——历史随会话消亡，无数据一致性风险）；`BookBinder` 为 AContainer 特化且带**消费前活跃重校验**（注释明确"异步 tick 与菜单交互期间书可能被取走/调包"——与 AContainer/附魔双机/AutoBrewer 同一已验证加固家族）+ 最大等级设置三件套（IntRangeSetting 界内）。

**实体防御监听收尾清点（干净）**：`WitherListener`——WitherProof 门控 + 可否决 `WitherProofAttackEvent` + 硬化玻璃豁免（与 ExplosionsListener #3414 家族衔接）；`EntityInteractionListener`/`BeeListener`/`FireworksListener`/`MobDropListener`/`PiglinListener` 均为既有轮次覆盖或简单门控型。**实体监听器族至此收尾清点完毕**。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 53 轮（2026-08-27）：剩余子命令族深层校验、物品/本地化键一致性抽核（零缺陷轮·十七）

**剩余子命令族复核（干净，变更史证明+现态抽核）**：全部 14 个子命令各自恰好一处 `hasPermission`（权限覆盖完备）；自受审基线以来仅 BackpackCommand 逻辑变更（r33 修复）与 i18n 显示文本——Charge/Teleporter/Stats/Search/Timings 零逻辑变更。现态抽核：ChargeCommand **无数值解析面**（仅对手持可充物品充满，无参数）；TeleporterCommand/StatsCommand 经 `PlayerList.findByName`（仅在线玩家，无任意建档面）+ `stats.others` 独立权限门；SearchCommand 委托 r43 已审的指南搜索输入面；TimingsCommand 有**旗标校验**（hasInvalidFlags）+ verbose 对玩家的拒绝分支。

**物品/本地化键一致性抽核（干净）**：近期改动路径所引用的六个消息键（charge 双键、backpack 恢复、timings verbose、铁傀儡、电梯改名）在 zh-CN/messages.yml 全部存在且译文正确——注意 YAML 嵌套层级使 dotted-path 字面直查会假零，须按叶键名查（本轮方法论注记）。r1 的全量 i18n 闭合结论维持。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 54 轮（2026-08-27）：启动/停机序列与 onDisable 资源释放顺序收尾核验（零缺陷轮·十八）

**onDisable 序列复核（干净，顺序正确且带机理注释）**：①未装载/UNIT_TEST 守卫早退；②取消调度任务（停止新异步工作）；③**先关闭全部库存再保存**（注释明确机理：close handler 标脏，先存后关=最后变更丢失或 /reload 下已存背包仍含取走物品=复制）；④ticker `halt→awaitIdle→run()`（注释明确：不等在飞异步 run 则末次排空被跳过、删除/移动队列未排即终存=已破方块复活）；⑤Profiler 终结；⑥脏玩家档案逐个异常隔离保存；⑦全部世界 BlockStorage 逐世界隔离保存；⑧通用菜单（.sfi 全局文件，与按世界保存无相互依赖）；⑨备份 zip（r37 原子化，最后执行以捕捉全部已存状态）；⑩置空实例。**停机序列至此收尾核验完毕**——r16-r19 任务族与 r29/r37 持久化族的成果在此序列中全部正确衔接。

**用户侧长浸泡回收**：暂无用户运行记录（r51 已附自助指引：/f/paper-test-1.21.11 下运行期间 `grep -cE "ERROR|SEVERE" logs/latest.log` 为零即通过）。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 55 轮（2026-08-27）：HologramsService 全息服务内部与盔甲架生命周期（零缺陷轮·十九）

**HologramsService 复核（干净）**：标识——盔甲架经 **PDC LONG 精确绑定 BlockPosition**（非 AncientPedestal 式前缀匹配，无伪造面）+ `isHologram` 最低属性门槛（隐形/静音/无重力 marker）；重复实体清理（#2927）；缓存——CHM + 30s 重验证窗口 + 45s 周期清扫（有界）；线程契约——`updateHologram` 主线程强制（异步经 runSync）+ Exception|LinkageError 隔离、`removeHologram` 异步**快速失败**（UnsupportedOperationException 而非静默错乱）；`setHologramLabel` 的"标签未变且近期确认存活即跳过调度提交"性能短路（与 benchmark hologram-label 场景衔接）；实体属性全套（marker/无敌/无 AI/不自然消亡）。区块卸载生命周期——实体随区块持久化，重载后经 PDC 扫描精确复找，缓存 UUID 引用经 hasDespawned 失效检测。

**记录（上游语义，不改）**：AncientPedestal 的前缀识别与 HologramsService 的 PDC 识别为两套并存机制（前者承载"还原原名"语义，后者承载"精确归属"语义）——已分别在 r31/r30 与本轮核验。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 56 轮（2026-08-27）：CargoUtils 物品搬运内部（零缺陷轮·二十）

**CargoUtils 复核（干净，r6/r7 菜单监视器族成果在位）**：`withdraw`/`insert` 双路径均为**"副本提交一次+读回核实实际提交量"**模式——insert：`replaceExistingItem(克隆并合并后的槽位)` 后读回槽位、仅按实际合并量计算余量（注释明确"钩子中途抛错=槽位已增而余量未调=复制"；钩子无抛错否决/篡改提交亦只按实际入账）；withdraw：先克隆减量提交、核实槽位确已变为期望值才返回提取物（否决则试下一槽而非复制）；vanilla 库存走 contents 快照+槽位范围（区分输入/输出面）；库存按 tick 缓存（Map 复用 BlockState 解析）；非 smart-fill 满堆快速跳过；ItemStackWrapper 比较全覆盖（r46 契约）。与 8 场景 TestCargoNetworkTaskItemFlow 及 CargoNetworkTask 主线程调度（r32 核）衔接一致。

**回归**：本轮零代码改动；全量 **3189 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 57 轮（2026-08-27）：PerWorldSettingsService 各世界启用/禁用语义（2 项修复）

**发现并修复（2 项，公共服务 API 正确性——用户目标"新 API 可用性"直接命中）**：`loadItemsFromWorldConfig` 双缺陷——①**极性反转**：`isAddonDisabled = config.getBoolean(addon + ".enabled")`（键名 enabled 默认 true，被读成"已禁用"）；②**键坍缩**：黑名单条目 `computeIfAbsent(plugin, …)` 把**所有 addon 的禁用世界都记到 Slimefun 自身这一个键**上。两者叠加后果：`isAddonEnabled(world, addon)` 对**任何非 Slimefun addon 恒返回 false**（地图中永无该 addon 的键）——addon 向 API 消费者报告"在所有世界被禁用"；且任一世界的 addon 禁用会污染按名匹配的其它世界。物品级语义经三重反转碰巧正确（未变）。修复：极性取反 + 按 `item.getAddon()` 键控 + `isAddonEnabled` 取 `!contains`；物品级行为经等价变换逐字保留（addon 禁用→全部物品禁用；addon 启用→仅个别禁用项禁用）。

**测试 `TestPerWorldSettingsService`（6 项）**：启用上报/整体禁用语义（物品键保持 true 仍全禁）/单项禁用/全新世界默认/跨世界隔离/**外部 addon 判别用例**（匿名 SlimefunAddon 覆写 getName——键坍缩只对外 addon 显形，Slimefun 自身因三重反转抵消而无法暴露此缺陷）。红绿双向验证：完整还原原始双缺陷后恰 2 例红（外部 addon 恒报禁用 + 世界隔离破坏）；修复后 6/6 绿。测试方法论：每用例唯一名世界（`TestUtilities.createWorld`）——`new WorldMock()` 默认同名 "World" 会跨用例污染按名匹配的黑名单（本轮实际踩坑记录）。

**回归**：全量 **3195 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 58 轮（2026-08-27）：world-settings 写入路径回归核验（1 项修复）+ v5.1.4 发布闭环

**save(World) 读写不对称（1 项修复，公开 API 契约）**：r57 修复后的写路径核验发现 `save(world)` 只写物品级 `addon.itemId` 键，**顶层 `enabled` 键从不写入**——而世界级状态存于 `disabledWorlds`（非 `disabledItems`）。后果：外部 addon 按公开 API 契约调 `setEnabled(world, false)`（世界级禁用）后按 javadoc 指引调 `save(world)`（"forcefully save the settings for that World"），落盘 `enabled` 保持原值，重启后禁用静默丢失。修复：save() 显式 `config.setValue("enabled", !disabledWorlds.contains(world.getUID()))`。核验收口：addon 级 `.enabled` 键无运行时修改 API（无从不对称），且手动 yml 值已在内存 Configuration、全量写盘保留；物品级本已对称。

**测试 `TestPerWorldSettingsService` 追加 3 项**：世界禁用持久化（判别用例）/重启用持久化/物品级写入回归保护。红绿双向验证：还原修复后恰 1 例红（`A runtime world disable must survive save(world) and a restart: expected false but was true`）；恢复后 9/9 绿。

**v5.1.4 发布闭环**：pom bump 5.1.3→5.1.4；全量 **3198 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。实机判据**方法论变更**：/f/paper-test-1.21.11 的 25565/25575 被用户自行运行的实例占用（`-Dpregenboot.radius=3200 -Xmx8G` 启动参数，非本审计会话启动——按 PID 精确甄别后不动）→ 改用**独立轻量实例**（/f/paper-audit-5.1.4：复制排除世界/日志、端口 25665/25675、全新世界）。判据：Slimefun v5.1.4 加载启用、Done 16.8s、停机干净、**插件启用阶段 ERROR/SEVERE=0**。新世界首启特有的原版噪声甄别：4 条 `No key layers in MapLike[{}]` 全部位于世界生成阶段（Preparing level/Selecting spawn point 之间、任何插件 onEnable 之前）——Paper 原版 dimension generator 的 DFU 输出，非插件错误（既有世界启动不出现，此前轮次未见即此因）。world-settings 全维生成正常。

## 第 59 轮（2026-08-27）：ItemGroup 装载顺序与锁定库存面（1 项修复 + 1 项诊断增强）

**LockedItemGroup 父组一次性解析（1 项修复，公开 API 装载顺序缺陷）**：`register()` 对父键只做一次性解析（遍历当时已注册的组），**无任何补解析机制**（全源码搜索确认）。跨 addon 场景：子组 addon 先于父组 addon enable（Bukkit 加载顺序不受 addon 作者控制、无依赖声明时尤甚）→ 父键被永久丢弃 → `hasUnlocked` 恒 true，**锁静默失效**，与 javadoc "cannot be opened until the parent group(s) are fully unlocked" 表述不符；且 INFO 日志把该情形错误归因为 "probably just disabled"。Slimefun 自身默认组因 DefaultItemGroups 构造序恰好不受影响（basicMachines 先于 electricity/androids/cargo/gps 注册），外部 addon（EnderTalisman 之外的引用者）无此保障。修复：未解析键留待 `getParents()`/`hasUnlocked()` 访问时**惰性重解析**（`resolveParents()` 空集 O(1) 短路，稳定后零开销；消费端 SurvivalSlimefunGuide:195/211 均经此两入口）；"找不到=不锁"与上游"父组被禁用"行为统一；日志措辞改为准确描述延迟重试语义。红绿双向验证：还原惰性解析后恰 1 例红（判别用例：父组后注册→getParents 补解析→研究未解锁锁住→解锁），恢复后 10/10 绿。

**同 key 双注册诊断（1 项增强）**：`ItemGroup.register()` 对同 key 第二组静默接受（上游行为）→ 指南双显示、物品按对象分裂——addon 作者误用时难排查。补 WARNING 诊断（同 key 已注册时打印），**行为零变化**（仍可注册、不抛异常，双注册行为保护测试固化）。

**零缺陷面核验**：NestedItemGroup 分页算法——页边界按列表位置每 36 个切分（page1 遍历 #0..#35、page2 从 #36 起），可见性过滤只影响格位填充不跨页漂移，无重叠/遗漏；pages 计算经边界值（size=0/1/37/72/73）推演正确。SubItemGroup——主界面恒隐（isAccessible 恒 true 供搜索）、isVisibleInNested 复用 ItemGroup.isVisible、构造时挂接父 NestedItemGroup、register 时自动补注册父组。锁定库存交互——锁定检查唯一入口 SurvivalSlimefunGuide.showItemGroup（BARRIER 显示+父组名进度列表+空点击处理器），openItemGroup 的 FlexItemGroup 直开/历史返回/CheatSheet 路径语义安全（历史仅含玩家合法打开过的组）。sortCategoriesByTier 用 TimSort（稳定）——同 tier 保持注册序；运行时 setTier 触发重排正确。

**记录（上游语义，不改）**：SubItemGroup.register 以子组 addon 归属注册父 NestedItemGroup（归属漂移仅影响诊断信息，无功能后果）；getParents()/getItems() 返回内部集合引用为上游 API 契约；锁定组的物品可被指南搜索到（LockedItemGroup 未覆写 isAccessible——设计而非缺陷，"不可访问→不进搜索"契约由 SubItemGroup 类实现）。

**回归**：全量 **3200 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 60 轮（2026-08-27）：Research 前置依赖图与 PlayerProfile 持久化/并发面（1 项诊断增强）

**依赖图核验（干净，既有加固在位）**：`addDependency` 拒绝自引用（Validate.isTrue）与传递环（`createsCycle` 迭代式 DFS，visited 集防重复展开——既有测试 TestResearchProgressionApi 覆盖）；`getDependencies` 返回不可变 Set；`removeDependency` 对称。解锁传播——依赖强制经 PlayerResearchTask 任务层（r47 核，指南/知识之瓶/`/sf research all`/直接 unlock 全部汇入）；`meetsDependencies(OfflinePlayer)` 对档案不在缓存时 fail-closed（javadoc 明确）。**禁用研究配套语义闭合**：`isEnabled` 双门（全局开关+个体 enabled）→ `canUnlock` 对禁用研究返回 true（免费解锁）→ `PlayerProfile#hasUnlocked` 对禁用研究**视为已解锁**（`!research.isEnabled() || contains`）——依赖树中被禁用的 prerequisite 不会锁死下游（视为满足），已解锁路径无需玩家手动点免费研究，语义自洽。register() 禁用分支复制遍历解绑 items（防 CME）+ 既有 legacy id 冲突 WARNING。

**PlayerProfile 持久化/并发面核验（干净，既往成果在位）**：PlayerData 全集合并发后端（`ConcurrentHashMap.newKeySet`/CHM，注释明确"异步 auto-save 线程遍历+主线程变异"）；`modificationEpoch`（AtomicLong）跨写盘检测并发变异——落盘期间到达的变异永不清 dirty、强制下轮重存（防丢）；`saveLock` 同步块串行化保存；保存失败（UncheckedIOException）dirty 保持、下轮重试；`unresolvedWaypoints` 原样写回防"世界未加载时路径点被下一次保存永久抹除"（数据丢失防护）；markDirty 恒在变异后调用（epoch 反映数据状态）。r16/r47 结论维持，本轮补验 PlayerData 存储层。

**Research.register 同 key 双注册诊断（1 项增强）**：解锁状态按 Research 对象身份跟踪（无 equals 覆写），同 key 双注册后两者在 registry 并存而 `getResearch(key)` 只返回第一个——两个研究的解锁互不互通、进度统计永不闭合，addon 误用时静默难排查（与 r59 ItemGroup 同 key 双注册同类）。补 WARNING（与既有 legacy id 冲突 WARNING 并列），行为零变化；行为保护测试固化"双注册可发生 + getResearch 取第一"。

**5.1.5 发布决策：本轮不发布**。v5.1.4（今日早些时候）已含 r57/r58 两项 API 修复；其后累积仅 r59（LockedItemGroup 装载顺序修复）与 r60（诊断型日志）——前者为低频场景（跨 addon 反序装载且使用 LockedItemGroup）、后者零行为变化，累积分量不足以支撑立即发版，留待下个有分量修复的版本一并发布。

**回归**：全量 **3201 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 61 轮（2026-08-27）：Backpack 物品与背包生命周期并发面（2 项修复）+ v5.1.5 发布闭环

**缺陷① markDirty 恒 no-op（数据丢失，修复 940005243）**：`PlayerBackpack.markDirty()` 仅在 `profile != null` 时转发脏标记，而现行存储路径 `load()`/`newBackpack()`（LegacyStorage 与 createBackpack 的唯一构造路径）**不携带 profile 引用**——方法对全部现行背包恒 no-op。后果链：GUI 关闭时 BackpackListener 的标脏失效 → 玩家改完背包直接下线时档案不脏 → auto-save 跳过保存 → `isMarkedForDeletion && getPlayer()==null` 分支直接清除档案（连带内存 inventory）→ **改动永久丢失**（回档至上次保存）。修复：profile 空缺时经 `PlayerProfile.find(ownerId)` 从缓存解析归属补标脏。

**缺陷② 异步保存直读活 Inventory（线程安全，违反 Storage @ThreadSafe 自家契约，修复 49677aa70）**：AutoSavingService 经 `runTaskTimerAsynchronously` 异步调 `profile.save()` → LegacyStorage 逐槽 `getInventory().getItem(i)`——Bukkit Inventory 非线程安全且返回**活引用**，与玩家 GUI 编辑（主线程）构成数据竞争（高负载多用户下序列化损坏/偶发异常）。修复：`PlayerProfile.save()` 在**主线程拍背包内容快照**（快照逐槽 clone），经 `Storage.savePlayerData` 新增参数传入（接口 @Beta 可变），LegacyStorage 快照优先、缺失 id 回退活读（主线程调用者安全）；异步线程经 runSync+future 限时 10s 等待——**快照在 saveLock 外拍摄**（主线程 save 持锁期间异步等快照会成环死锁），主线程卡死时抛异常保持 dirty 下轮重试。epoch 语义核验：快照后到达的变异永不清 dirty（下轮重存），无丢失窗口。

**红绿双向验证（TestBackpackSaveSnapshot，3 项）**：markDirty 判别（createBackpack→save 清脏→markDirty→断言重新变脏）/快照优先判别（活 inventory=DIAMOND、快照=EMERALD→落盘必为 EMERALD 且 DIAMOND 不泄漏）/异步保存功能（异步线程 save 首写成功清脏）。两处缺陷独立还原各恰 1 红。测试方法论：MockBukkit 下 ItemStack YAML 反序列化不可靠（TestLegacyBackend 既有注释在案）——落盘断言改**原始文本匹配**（`type: EMERALD` 互斥判定）绕开；首写路径避免 Config 重载触发反序列化；UNIT_TEST 下 Slimefun.runSync 立即内联执行（1031 行既有适配）故线程编排判别不可行、改为存储层契约判别。

**其余核验（干净）**：PlayerBackpack 既往加固在位（setSize 收缩拒绝/ResizeEvent 可否决/viewer 迁移/损坏 size 钳制/closeForAll future 防孤）；LegacyStorage 背包段先清后写（防移除复活复制）、legacy 共享 id 研究兼容；PlayerData 全集合并发后端（r60 复核维持）。

**v5.1.5 发布闭环**：pom bump、全量 **3204 项测试通过、0 失败、0 错误、7 跳过**；独立实例（/f/paper-audit-5.1.4）实机 Paper 1.21.11：Slimefun v5.1.5 加载启用、Done 17.1s、**插件启用阶段与全日志 ERROR/SEVERE 均为 0**（该实例世界已生成，无新世界 MapLike 噪声）。RCON 本次拒绝连接、stop 未能送达——进程随后随 Git Bash 会话拆解终止（r43 已知怪癖的再现，非插件问题）；优雅停机序列以 r54 onDisable 审计与全量单测为准。历史 benchmark fork 进程（PID 49276，非本轮产物）未干预。

## 第 62 轮（2026-08-27）：GEO 资源扫描与区块生成面（1 项修复）

**扫描结果分页 off-by-one（1 项修复，功能不完全实现）**：`ResourceManager.scan` 的页数按 **36/页** 计算（`(size-1)/36+1`），但菜单遍历每页实际放置 **28 项**——布局从格位 10 起、`index%9==8` 时跳 2 跳过边缘列（每行 7 格×4 行）。资源总数落在 29-36 区间（及 57-64 等每页尾段）时 `pages=1`，`getNextButton` 的 `displayPage+1 < pages` 恒假——**尾部资源（如第 29-36 个）从扫描结果中静默消失**，与"扫描显示该区块全部资源"的表述不符。默认 Slimefun 资源数约 20 上下未触界，装足量 addon 资源（OilPump 生态常见）后显形。修复：抽出 `RESOURCES_PER_PAGE=28` 常量与 `getPageCount(int)`，与遍历分页（`displayPage*28 .. (displayPage+1)*28`）强制对齐。红绿双向验证：还原 36 除数恰 2 红（29→应 2 页实 1、57/85 边界）。

**其余核验（干净，既往成果在位）**：`GEOResourceGenerationEvent`/`OilPumpExtractEvent`/`GEOMiningStartEvent` 事件改值族 setter 校验完备（负值拒绝+suppliesCost≤存量上界）；OilPump 消耗链——活槽重校验（异步 tick 与玩家交互竞态）、事件否决零消耗不变式、空桶防抖只在放得下时迁移（不蒸发）、supplies 非负门（setSupplies Validate）；`getSupplies` 损坏数据回退空、注册重复 key 拒绝、UNIT_TEST 守卫不落盘。**区块生成注入面**：GEO 资源为惰性生成设计（首次扫描/机器查询时 `generate`），不存在 ChunkPopulateEvent 注入路径——指示中该子面无对应机制，记录设计事实。

**回归**：全量 **3208 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 63 轮（2026-08-27）：能量网络高负载剩余路径核验（零缺陷轮·二十一）+ v5.1.6 发布闭环

**审计面选择**：指示候选之"SlimefunGuide 搜索与 GuideHistory 深链"经查 r43（搜索链：事件可否决改向/标题裁剪/词条规范化/历史 200 有界/结果 44 槽上限/过滤钩子）与 r52（GuideHistory：200 条双端队列+内存态设计无持久化面）已充分覆盖，本轮不重复。

**能量网络高负载剩余路径核验（干净，既往成果在位）**：`EnergyNet.tick` 全链——双调节器确定性仲裁（"最小调节器坐标"者独占 tick，防两网络合并后发电机双跳/电容双结算）；`super.tick()` 后 `isRegistered()` 复查（分类变化触发全网络重建时不结算已废弃实例，防双充/双供）；结算循环**逐消费者 Exception|LinkageError 隔离**（单组件损坏不中止结算——注释明确"已充入电量未扣除=每 tick 复制"）；`unpooledGenerators` 契约（EnergyGenerateEvent 被否决或抛错的发电机存量不入网、storeRemainingEnergy 不得改写）；`NumberUtils.flowSafeAddition` 溢出防护；电容失败"保持先前电量"语义；EnergyConsumeEvent/EnergyNetTickEvent 钩子零监听者零成本；`onClassificationChange` 双向清理（TERMINUS 移除三表）+ GENERATOR 未实现 EnergyNetProvider 的 addon 契约告警。

**v5.1.6 发布闭环**：pom bump 5.1.5→5.1.6；全量 **3208 项测试通过、0 失败、0 错误、7 跳过**（r62 后无代码改动，沿用最近全量结果）；独立实例实机 Paper 1.21.11：Slimefun v5.1.6 加载启用、Done 40.9s、**全日志 ERROR/SEVERE=0**、RCON stop 送达、优雅停机确认（Stopping server→Saving worlds→All dimensions saved）、端口释放。

## 第 64 轮（2026-08-27）：SlimefunItem 注册与 ItemState 状态机深审（3 项修复，其中 1 项单测保真度）

**缺陷① register 异常路径半注册残留（修复 232610ff2）**：registry 三表写入（items/ids/materials）发生在 state 判定**之前**，`postRegister`/`onEnable`/`ItemSetting.reload()` 等后续步骤抛异常时 catch 仅记日志（生产）或重抛（单测）——物品残留为**半注册 UNREGISTERED 停户**：`getById` 可发现、其 id 永久占位（addon 修正后重注册被 checkForConflicts 挡死）。触发场景真实：管理员手改 Items.yml 坏值致 setting.reload() 抛、addon postRegister bug、ItemGroup 重复注册抛。修复：catch 回滚 items/ids 两表（`remove(id, this)` 按值精确移除）；materials 刻意保留（仅 getByItem fast-path 提示，多物品共享材料、误删只降性能不影响正确性）。

**缺陷② enable-tickers=false 提前 return 跳过 handler 清理（同 commit）**：tickers-off 分支 `state=DISABLED; return;` 跳过 494-501 的清理——DISABLED 物品残留 tickerBlocks 注册与活 itemHandlers，与正常 DISABLED 路径（清理过）语义矛盾（其方块放置后仍被视作 ticking）。修复：并入 if-else 链尾合流到 DISABLED 清理分支（注释明确机理）。

**缺陷③ 单测保真度：enable-tickers 在 MockBukkit 下恒 false（同 commit，调试 ② 时暴露）**：`new Config(plugin)` 在 MockBukkit 下**不复制插件 config.yml 到数据目录**（plugins/Slimefun/ 无 config.yml 实证）→ 空 config 单参 getBoolean 兜底 false → **全部 ticking 物品在单测里一直是 DISABLED**。②的修复让 handlers 被清 → 全量 **50 项 handler 驱动测试变红**——它们此前靠"handlers 意外保留"**假绿**（彩虹方块/垃圾桶/附魔机等事件从未经物品正常启用路径验证）。修复：读取前 `setDefaultValue("URID.enable-tickers", true)`（不覆盖生产 config.yml 已读值，单测补默认）→ 50 红全消且为**真绿**（ticking 物品正常 ENABLED、handlers 正常绑定）。此项同时纠正了测试生态的系统性偏差——tickers-off 分支在单测中从"常态误入"恢复为"显式覆盖"（testTickersDisabledCleansHandlers 的 setValue(false) 显式触发）。

**红绿双向验证（TestRegisterStateHygiene，2 项）**：失败注册回滚判别（postRegister 抛出——**判别抛出点须在 registry 写入之后**，preRegister 早于写入不触发缺陷，本轮实际踩坑）+ tickers-off 清理判别（tickerBlocks 不含 id + handlers 空）。两判别独立还原各恰 1 红；修复后 2/2 绿。

**其余核验（干净）**：`addItemHandler`/`addItemSetting` 的 UNREGISTERED 前置门（注册后拒绝变更，状态机单调）；`getSlimefunItemIds` 为 CHM（运行时注册 vs 查询并发安全）；`isAutoLoadingEnabled` 运行时注册路径（state==ENABLED 才 load()，与 tickers-off/配置禁用分支互斥正确）；handler 绑定对称性（DISABLED 清理与 ENABLED 绑定互补；BlockTicker 双注册 tickerBlocks add 语义幂等——Set）。

**回归**：全量 **3210 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21；+2）。

## 第 65 轮（2026-08-27）：RecipeType/RecipeService 配方输出面 + TickerTask 抽查（1 项修复）

**配方面核验（干净）**：`RecipeType.register` 双路径（consumer 回调或 MultiBlockMachine.addRecipe——r33/r45 既有结论维持）；`registerMobDrop` computeIfAbsent 原子性（并发注册不丢 drop 集，既往加固）；`MachineRecipe` 为纯数据类（ticks/input/output，注释明示将随 Recipe Rewrite 重写）——数量记账全在消费侧（AContainer/AGenerator/AutoBrewer 族，r32/r39/r48/r52 既有）；`MinecraftRecipeService.getRecipesFor/getRecipe` 的 null 兜底与 Bukkit 回退正确；`RecipeSnapshot` 由 dough 维护。

**MinecraftRecipeService.refresh() 订阅者无异常隔离（1 项修复，公开 API 韧性）**：订阅回调为公开 API（addon 可 subscribe），单个回调抛错曾中断 refresh 循环——**后续全部订阅者被静默跳过**（真实场景：addon 订阅者抛错 → 排在其后的 ElectricFurnace 内部订阅者收不到快照 → 电炉配方功能受损），且异常上抛进 SlimefunStartupTask 的兜底 catch（记 SEVERE 但损坏已完成）。修复：逐订阅者 try/catch(Exception|LinkageError) 隔离 + WARNING 归因（与既往"逐条队列隔离"模式一致）。**snapshot 补 volatile**：启动任务写、异步自动合成机读的跨线程可见性（不可行为断言，随同 commit 无独立测试）。

**TickerTask 调度队列高负载剩余路径抽查（干净）**：19 处并发原语在位（CHM/并发队列/同步块）；近期 commit 均为既往修复（androids 链式移动竞态 5d378756d、SlimefunTickEvent 9c6cab97c）——r16-r19 任务族结论维持，无未审改动。

**回归**：全量 **3211 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21；+1）。

## 第 66 轮（2026-08-27）：多方块结构验证/破坏时序 + 监视器族抽查（1 项修复）+ v5.1.7 发布闭环

**多方块面核验（干净）**：`MultiBlockListener` 结构匹配——中列垂直比较 + 对称结构 2 向/非对称 4 向旋转匹配（预分配方向常量防每点击垃圾）；tag 等价双向匹配（与 MultiBlock.compareBlocks 的单向语义在双向场景等价）；多匹配取最后注册者（覆盖语义）；事件可否决（#2809）；disabled 机器 callItemHandler 经 r64 handler 清理后安全 no-op。`MultiBlock` 构造校验（9 长度/触发面枚举）与 isSymmetric 判定正确；活塞特判（Industrial Miner 运行中识别）为定义间比较语义（世界匹配无特判=上游行为，记录不改——Miner 运行中无需再交互）。r33 机器并发面结论维持。

**MultiBlock equals/hashCode 契约违规（1 项修复，未来防雷）**：原 hashCode 哈希 `item.getId()` + 数组 identity，而 equals 按结构 tag 等价比较（**不含 item**）——结构等价的两实例哈希必然不同，违反 Object 契约；当前容器为 List 暂未触发，属"入任何哈希容器即静默破坏"陷阱。修复：**等价类规范化哈希**——tag 材料映射到 tag 单例哈希、活塞族（PISTON/MOVING_PISTON）归一、非 tag 材料按自身、null 归零（精确复刻 equals 的等价类划分）。红绿双向验证：契约判别（不同 item 的 tag 等价结构哈希必须相等）——原实现红、**内容哈希中间版亦红**（tag 等价无法由内容哈希表达，本轮实际踩坑）、等价类版绿。

**DirtyChestMenu/BlockMenu 监视器族抽查（干净）**：近期 commit 均为既往深审修复（r6/r7 同步化 + a48082b91 异步关闭 runSync + 58061ef53 pushItem 并发同步/合并分支置脏），结构在位无未审改动。

**v5.1.7 发布闭环**：pom bump；全量 **3212 项测试通过、0 失败、0 错误、7 跳过**（+1）；独立实例实机 Paper 1.21.11：Slimefun v5.1.7 加载启用、Done 28.3s、**全日志 ERROR/SEVERE=0**、RCON stop 送达、优雅停机确认。发布内容：r64 register 状态机三项（半注册回滚/tickers-off 合流清理/单测保真度）+ r65 RecipeService 订阅者隔离与 volatile + r66 MultiBlock 契约。

## 第 67 轮（2026-08-27）：SlimefunArmor/辐射监听族 + EnergyNetComponent charge 读写（零缺陷轮·二十二）

**SlimefunArmor 族复核（干净，既往成果在位）**：`AbstractArmorTask`——**逐玩家 Exception|LinkageError 隔离**（注释明确"未捕获异常会取消整个重复任务、静默永久禁用全部护甲处理"）+ interval<1 防零回退 + 异步调度；`SlimefunArmorTask`——HashedArmorpiece 分歧缓存、变更事件为**通知性**（缓存先行更新、无否决语义、两非 SF 件互换静默）、效果应用经 `runSync` 包裹 `canUse` 门 + `removePotionEffect/addPotionEffect`（主线程正确）；`SlimefunArmorEffectEvent`——setter null 校验（空数组=等价取消的显式契约）+ 零监听者免分配热路径（每玩家每件每间隔无构造）。辐射族 r39 结论维持（RadiationTask 双端钳制/曝露事件缩放符号契约/死亡重置可否决）。

**EnergyNetComponent charge 读写剩余路径复核（干净，既往成果在位）**：`getCharge(loc, data)`——损坏值（崩溃写/手改/恶意 NBT）**fail-closed 钳制到 [0, capacity]**（注释明确负值毒化全网结算、超容值注入免费能量的机理）+ NumberFormatException 回退空（下次 setCharge 覆盖坏值）；`setCharge(loc, data, charge)`——非负校验 + 容量钳制 + **幽灵记录拒写**（data 无 "id"=方块已删、写入会复活无主记录）+ 变更跳过（值未变不写盘）+ 电容纹理联动。双参 Config 复用变体避免重复 BlockStorage 查找（结算循环单次读取直通）。

**记录（上游语义，不改）**：AbstractArmorTask 异步遍历 `Bukkit.getOnlinePlayers()`——Paper 的玩家列表快照语义下实际安全（上游模式）。

**回归**：本轮零代码改动；全量 **3212 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 68 轮（2026-08-27）：ItemSetting 装载与配置回写面（1 项修复）

**List 元素类型宽松匹配（1 项修复，公开 API 健壮性）**：`ItemSetting.reload()` 旧逻辑 `configuredValue instanceof List && defaultValue instanceof List` 即视为类型匹配——**元素类型不校验**。Items.yml 手改坏值（List<Integer> 配进 List<String> 设置）被原样存入 value，破坏延迟到消费侧增强 for 强转时以 ClassCastException 形态在深处炸（ addon 定义 `ItemSetting<List<Material>>` 同样命中）。主源码自身全部 List 声明均为 List<String>（MaterialTagSetting/ignored-lores 等）不受影响——纯 addon API 防护面。修复：default 首个非空元素为样板逐元素 isInstance，不匹配回退默认值+专用警告（与标量分支行为对齐）；样板不可得（default 空/全 null）维持原宽松语义。

**红绿双向验证（TestItemSettings 追加 2 项）**：判别用例（配置 List<Integer> → 还原后恰以 ClassCastException **真实破坏形态**红）+ 匹配类型仍正常装载回归。测试踩坑：reload 的警告路径要求物品已注册（warn 的 addon 非空前置）——测试物品须先 register。

**其余核验（干净）**：标量分支类型不匹配回退默认+警告（既有）；validateInput 失败保留 null→getValue 警告回退默认（单测抛错/生产警告的双态合理）；子类族——MaterialTagSetting 元素级 Material.matchMaterial 校验、EnumSetting 存枚举名字符串、Int/DoubleRangeSetting 界内校验；hashCode/equals 按 (item, key) 一致；addItemSetting 重复 key 拒绝 + NotConfigurable 拒绝 + 注册后拒绝（r64 状态机门）。记录不改：YAML 整数回读为 Integer（dough 路径）与 getDefaultValue 的 setDefaultValue 回写路径正常；getValue 未初始化时生产警告+默认值的上游语义。

**回归**：全量 **3214 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21；+2）。

## 第 69 轮（2026-08-27）：ItemDataService NBT 面 + TabComplete 深层 + 区块时序边界（零缺陷轮·二十三）

**ItemDataService/物品 NBT 复核（干净，r32 结论维持）**：`CustomItemDataService` 无状态键读写——setItemData 双变体（ItemStack 经 getItemMeta/setItemMeta 往返；ItemMeta 直写 PDC STRING）；getItemData 三重防御（null/AIR/无 meta）+ Optional 语义；hasEqualItemData 短路 + Optional.equals 比值；伪造 SF-id PDC 面为创造模式固有限制（r32 记录在案）。PDC 消费者族既往加固维持（LimitedUseItem 钳制/ChargeUtils 负值 NaN 防御/损坏配方键回退）。

**命令族 TabComplete 深层（无剩余面）**：全源码仅 `SlimefunTabCompleter` 一处 onTabComplete——子命令族（14 个）**无任何独立补全覆写**，r37 对主分发器的审计（80 条上限/长度守卫/公开信息语义/null 回退玩家列表）即为全部覆盖面，结论完整维持。

**区块时序边界（WorldListener 之外无消费者）**：全源码区块/世界事件消费者仅 `WorldListener`（WorldLoadEvent→BlockStorage.getOrCreate、WorldUnloadEvent→drainQueues 先于 saveAndRemove——r45 核过时序）；无 ChunkLoad/ChunkUnload/EntitiesLoad 消费者——区块管理全部走 BlockStorage 内部路径（r38/r45 已核）。

**5.1.8 发布决策：本轮不发布**。v5.1.7 后累积仅 r68 一项（ItemSetting List 元素类型校验——纯 addon API 防护，主源码 List 声明全为 String 不受影响），分量不足，继续累积。

**回归**：本轮零代码改动；全量 **3214 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 70 轮（2026-08-27）：Androids 脚本执行/排序面（1 项修复）

**Script 菜单排序键笔误（1 项修复，功能与表述不符）**：`getUploadedScripts` 的排序键 `-upvotes + 1 - downvotes` 代数化简为 `1-(up+down)`——**总票数降序**而非净赞降序（上游 debc925bd/#1976 引入，疑为 `-(up-down)` 之笔误）。后果：差评轰炸的争议脚本（如 10 好 10 差、净赞 0、评分 50%）排在小而好评的脚本（2 好 0 差、净赞 +2、评分 100%）**之前**——与菜单展示评分百分比/好差评图标的意图相反，且可被恶意刷差评置顶滥用。修复：净赞降序 `down - up`（同净赞保持文件序稳定排序）。红绿双向验证：还原旧键恰 1 红（判别用例构造 3:1 vs 10:10 两脚本，断言净赞高者居首——错误信息精确记录旧排序的反直觉行为）。

**其余核验（干净）**：`Script` 构造 null 校验链（损坏 .sfs 在 loadScripts 逐文件 catch 隔离，author 非 UUID 同样隔离）；`isAuthor` 经 getUUID 损坏值 fail-closed；`rate` 写时一人一票复检 + reload-改-写全主线程（菜单点击路径无并发窗口）；`download` 同构；`getRating` 的 +1 防零除；`upload` UUID 文件名防路径穿越（r14 维持）；加载双目录合并（NONE 共享）正确。脚本执行消费侧（指令解析/AndroidAction）与 5d378756d 链式移动竞态修复维持。

**回归**：全量 **3215 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21；+1）。

## 第 71 轮（2026-08-27）：CheatSheet 模式边界（1 项修复）+ v5.1.8 发布闭环

**作弊指南给物品蒸发（1 项修复，数据完整性）**：cheat 分支 `pl.getInventory().addItem(clonedItem)` **返回值丢弃**——背包满时点击"给物品"实际物品静默消失（管理员常用功能；与 r33 BackpackCommand、r32 AutoCrafter dropLeftover 同族的 addItem 余量未处理缺陷）。修复：抽 `giveCheatItem(Player, SlimefunItem, boolean)`（shift 满堆语义保留），未装下余量 `dropItemNaturally` 落脚。红绿双向验证：背包满 41 格全填充判别恰 1 红（无掉落物=蒸发）/装得下正常入包回归。

**其余核验（干净）**：CheatSheetSlimefunGuide 极薄（组可见性含 Flex 三参判定/模式/header 移除设置面板）；cheat 点击路径的权限门在 handler 层（`slimefun.cheat.items`，#3548 末道防线——未授权者即便拿到指南物品也无法取物）；MultiBlockMachine 排除（多方块不可物品化）；锁定组/研究/权限 lore 三分支在 cheat 模式正确短路（291/295 的 isSurvivalMode 门）；handler try/catch 隔离 + printErrorMessage 归因。

**v5.1.8 发布闭环**：pom bump；全量 **3217 项测试通过、0 失败、0 错误、7 跳过**（+2）；独立实例实机 Paper 1.21.11：Slimefun v5.1.8 加载启用、Done 22.7s、**全日志 ERROR/SEVERE=0**、RCON stop 送达、优雅停机确认。发布内容：r68（ItemSetting List 元素类型）+ r70（Script 净赞排序）+ r71（cheat 给物品余量掉落）。

## 第 72 轮（2026-08-27）：GPSNetwork/GPSTransmitter 并发 + AutoCrafter 原版配方深层（零缺陷轮·二十四）

**GPSNetwork 并发面复核（干净，r30 成果维持）**：`updateTransmitter` 全程 compute() 原子（异步 ticker 注册 vs 主线程移除无交错丢失）+ 事件在 compute 外触发（监听器回调不再入 Map）+ 幂等更新静默（ticker 每 tick 重申不刷事件）；`cachedComplexity` 仅作事件对比基准（getNetworkComplexity 实时计算，无陈旧缓存消费面；两 transmitter 同 tick 增删致复杂度不变时不 fire 正确）；transmitters/cachedComplexity 均 CHM、value 为并发 Set（javadoc 明确线程模型）；`addWaypoint` 的 ChatInput 迟回调内**重新解析档案+上限+重名重校验**（r31 漂移族模式）+ 事件可否决 + id 消毒（点替换防 YAML 路径撕裂、纯色码名拒绝）；`openWaypointControlPanel` 异步档案回调经 runSync 开菜单且 isOnline 复查；世界未加载航点跳过。**记录（上游语义，不改）**：`getTransmitters` 返回内部并发 Set 引用（teleportation 读取用，线程安全但可变暴露）；发射器面板延迟显示 `1000/Y` 在 Y=0 时为 Infinity、负 Y 为负值（计算侧 getMultiplier 已 max(0)，纯显示瑕疵）。

**AutoCrafter 原版配方交互深层复核（干净，r32/r40/r65 成果衔接）**：`VanillaAutoCrafter.getSelectedRecipe` 损坏 PDC 键回退 null（防 ticker 错误累积销毁方块）；`matches` 排除 SF 物品（useableInWorkbench 例外）；`offerRecipe` 分页边界（index>0 / index<size-1）+ Validate 页界；菜单打开期间配方快照被 datapack 重载移除 → setSelectedRecipe 后 getSelectedRecipe 查 null 自愈。`AsyncRecipeChoiceTask`——runTaskTimer **主线程**执行（注释明确"Inventory 修改非线程安全"）、ReadWriteLock 保护 iterators、无观看者自终止、预览动画异常优雅停不重试。RecipeService.getRecipe/getRecipesFor（r65 修复后）衔接正确。

**回归**：本轮零代码改动；全量 **3217 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 73 轮（2026-08-27）：TeleportationManager 传送队列 + CargoNet 拓扑边界 + SlimefunRegistry 生命周期（零缺陷轮·二十五）

**TeleportationManager 高负载面复核（干净，既往成果完整）**：`openTeleporterGUI` 检查不提交（档案加载失败无残留条目软锁）+ runSync 内 `isOnline && add` **原子认领**（双开竞态败者被弃）；世界未加载航点跳过（防卡死 users 无 close handler）；`teleport` 先事件可否决、**先算时间后认领**（计算抛错无残留）；`getTeleportationTime` long 算术防回绕（r30）+ #3573 Math.max 防零 + 距离钳制 1e8；进度链主线程递归（runSync 10L）+ isValid 双重校验（跨世界/位移>√2 判中断）+ 中断/失败双路径 cancel 事件；`onTeleport` 异步传送回调 runSync 收敛；**click-close 顺序核验**：航点点击 handler 先 closeInventory（同步触发 close handler remove）再 teleport add——最终状态正确（先移后加无竞态）；`teleporterUsers` 全主线程访问（HashSet 安全）。传送中开 GUI 被 contains 拒绝 ✓。

**CargoNet 拓扑重算边界复核（干净，r32/r41 结论维持）**：网络发现/分类变化全重建、regulator 仲裁、世界卸载全清（含区块索引）+ 死网络阴影防护、`chunkIndexLock` 先翻旗标防异步重索引——既往轮次全覆盖，本轮无新改动面。

**SlimefunRegistry 生命周期（设计事实，无卸载面）**：Registry 为一次性注册表（随进程生命周期），无 clear/unload 路径——Slimefun 明确不支持 /reload；各表（items/ids/materials/categories/researches/mobDrops/geoResources/tickerBlocks/blockMenuPresets...）仅在启动期注册、停机随进程消亡。无"装载/卸载"状态机即无对应缺陷面。

**回归**：本轮零代码改动；全量 **3217 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 74 轮（2026-08-27）：Androids 执行引擎与世界交互面（零缺陷轮·二十六）

**执行引擎核验（干净，既往成果完整）**：`ProgrammableAndroid` 主类——`move` 链（TickerTask 队列占用/移动源双防——防两 android 的数据移动互覆（5d378756d）、世界边界、领地 PLACE_BLOCK 权限（ownerless legacy 保持旧行为的显式契约）、AndroidMoveEvent 可否决、皮肤经 runSync 且复查头类型）；`rotate` 事件可否决+rotation 持久化；`depositItems`/`refuel` 接口交互（ANDROID_INTERFACE 物品/燃料识别 + 领地 INTERACT_BLOCK 门防边界倾倒/抽取 + 事件否决 + addItem **余量回写**（908-914）+ 燃料 insertFuel 合并 rest 记账 + ItemUtils.consumeItem）；`consumeFuel` 事件可否决+fuelLevel 可改；`getOwner` 损坏 UUID 回退 null（javadoc 明确"null 时跳过所有权依赖动作"）。

**动作子族核验（干净）**：`MinerAndroid.dig/moveAndDig`——掉落非空 + UNBREAKABLE tag 排除 + **owner null fail-closed** + 领地 BREAK_BLOCK 门 + AndroidMineEvent 可否决（可改掉落）+ 非 SF 方块门（防拆 SF 机器）；`breakBlock`——世界边界 + pushItem **余量 dropItemNaturally**（140 行既往修复防输出满蒸发）+ InfiniteBlockGenerator 优化（firesEvent 可配置触发 BlockGenerateEvent）；`FarmerAndroid.farm`——世界边界 + owner null fail-closed + 领地门 + Ageable 成熟判定；Butcher（r30）/attack、Woodcutter/Fisherman 基类守卫（非该类型抛 UnsupportedOperationException）；`AndroidAction` 为函数接口无状态。

**回归**：本轮零代码改动；全量 **3217 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 75 轮（2026-08-27）：Woodcutter 链式砍伐 + Fisherman 产出 + TeleporterPylon 结构族（零缺陷轮·二十七）

**WoodcutterAndroid 复核（干净）**：`chopTree`——Vein.find 链式发现（LOGS tag、MAX_REACH 界内）+ 取**发现序最远者**先砍（自顶向下）+ owner null fail-closed + 领地 BREAK_BLOCK 门 + **"give up on this tree"返回 true 前进**（防受保护树导致的每 tick 音效+耗油死循环，注释在案）；`breakLog`——AndroidChopTreeEvent 可否决/可改掉落 + **双路径 pushItem 余量 dropItemNaturally**（事件改向与默认掉落均不蒸发）；补种判定按 `log.getY() == 基座 Y`（与 Vein 发现序解耦——横枝尾比底木更远时仍正确识别底木）；replant 树苗映射覆盖 8 主木种+1.19 mangrove/1.20 cherry 版本门 + **土壤需求判定**（真菌需 fungus_soil、红树需 mangrove_base——防错土壤上种树苗）。**记录（上游语义，不改）**：补种为 PLACE 语义但仅校验 BREAK 权限（砍+补种一体的上游设计，细权限插件下低危）。

**FishermanAndroid 复核（干净）**：fish——下方 WATER 判定 + 概率 `10 * tier` 缩放 + AndroidFishEvent 可否决/可改向（setter null 校验在案）+ **drop.clone() 两次**（事件对象与入包路径——静态 loot 表共享实例防污染）+ pushItem 余量掉落。loot 表构造（鱼类+垃圾+稀有战利品权重）为静态数据无状态。

**TeleporterPylon/结构族复核（干净）**：Pylon 为 RainbowBlock 特化（CYAN/PURPLE 玻璃颜色 tick，r43 彩虹族已核）；ElevatorService 不存在（电梯走 ElevatorPlate+BlockStorage，r31 全核）；CargoNet ConnectedExecutor 无此类型（拓扑族 r32/r41/r73 维持）。

**回归**：本轮零代码改动；全量 **3217 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 76 轮（2026-08-27）：Reactor 衰变/冷却 + CargoNet 分发次序 + Cooler/Talisman 冷却族（零缺陷轮·二十八）

**CargoNet ConnectedNode 分发次序详核（干净）**：`distributeItem` 的 round-robin 索引推进三路径全部对齐——成功（item==null）break 前 `put(index+1 % size)`（下一轮起点=成功者后一个）；事件取消 continue 前 index++（bookkeeping 推进）；领地拒绝走循环尾 index++（"Skip not continue"注释明确）。`roundRobinSort` 旋转语义与索引含义一致（起点=原列表第 index 个）。非 round-robin 模式的首输出吸干行为为**配置语义**（管理员选择顺序优先）。事件改向 air=显式销毁契约（返回 null 不回源）；单输出异常隔离 try-next（注释明确"DirtyChestMenu 变异钩子先于放置——未提交可安全试下一个"）。`CargoNet.roundRobin` Map 生命周期：TERMINUS 分类移除时清理（注释明确防高节点流转无界增长）、per-network 实例字段随网络销毁 GC、主线程访问（r32 线程模型）。

**Reactor 衰变/冷却深层复核（干净，r34 结论完整维持）**：接入端口 owner 隔离、损坏 charge 回退、explosionsQueue CHM、爆炸事件先于清理、水冷邻检 runSync、冷却消耗可否决、满载安全怠速——r34 已列全链，本轮无新改动面。

**Cooler/Talisman 冷却管理器族（设计事实）**：Cooldowns 类不存在于代码库——Talisman 无冷却机制、Cooler 的"冷却"为冷藏包语义（r35 已全核喂食 owner 门控/异步回调 runSync/消耗重扫/双事件可否决）。候选面按"无此机制"记录。

**回归**：本轮零代码改动；全量 **3217 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 77 轮（2026-08-27）：BlockPlacer 放置竞态 + GrapplingHook 补深 + ExplosiveTool 维持（零缺陷轮·二十九）

**BlockPlacer 放置竞态/#3218 边界详核（干净，既往成果完整）**：`hasPermission`——owner 缺失/损坏 UUID **双 fail-closed**（注释明确"拒绝放置而非静默绕过保护插件"+防异常逃逸致事件退化原版发射器丢物品）；`isAllowed`——非方块/蛋糕特例/忽略标签（床类不可靠放置）/unplaceable-blocks 配置四重门；`BlockPlacerPlaceEvent` 可否决且**替换物品二次 isAllowed**（防 addon 走私非方块/黑名单——注释在案）；`schedulePlacement`——runSync 延迟放置 + **b.isEmpty 复查**（延迟窗口被占的放置竞态防护）+ `removeItem(removedItem).isEmpty()` 返回值校验（**#2817 防复制**：物品实际未取到就不放）+ clone 定量消耗（"只移除实际发射的那一个"）；placeBlock 的 customName 经 BlockState 快照更新；#3218（ItemsAdder 自定义物品放置拦截）r40 在案维持。

**GrapplingHook 补深（干净）**：`getClickedBlock().isEmpty()` 经查为 `Optional<Block>.isEmpty()`（PlayerRightClickEvent 以 ofNullable 包裹）——**null 安全**（对空气使用语义正确，无 NPE 面）；#740 副手弓复制守卫、isGrappling 状态去重、GrapplingHookFireEvent 可否决可改向发射方向、LEAD 按配置消耗（剪刀变体 state=false 不消耗）、蝙蝠不可拾取/隐形/无 AI/无敌 全部在位（r49 结论+本轮 null 安全确认）。

**ExplosiveTool 连锁挖掘深层（干净，r49 结论完整维持）**：isEmpty 首查防同 tick 重叠区域双重掉落、逐块不可破坏/边界/权限门、SF 方块哑事件委托与"掉落先于清数据"次序——无新改动面。

**回归**：本轮零代码改动；全量 **3217 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 78 轮（2026-08-27）：ElevatorPlate 楼层合并/选择器边界（1 项修复）

**楼层选择器分页 off-by-one（1 项修复，r62 GEO 分页同族）**：`openFloorSelector` 的 `pages = 1 + floors.size() / GUI_SIZE`（GUI_SIZE=27）在楼层数恰为 27 倍数时**多算一页纯空页**——第 2 页 `index=27`、`min(27, size-27=0)`=0 项，仅背景与按钮；"下一页"按钮（page != pages）可翻入空页再翻回。修复：抽 `getPageCount(int)` 改为向上取整 `(size-1)/27+1`（openInterface 保证 size≥2，max(1,…) 防御）。红绿双向验证：边界用例（27→1/28→2/54→2/55→3），还原旧公式恰 1 红于 27 判别点。

**楼层合并/构建核验（干净）**：`getFloors` 自世界底向顶扫描同列同 id 电梯板——`addFirst` 使列表头为最高层（自上而下展示）、楼层号 index 自下而上递增（与玩家直觉一致）；`y==b.getY()` 分支正确并入当前层（避免重复扫描自身列的 getBlockAt）；列内方块均在玩家所在已加载区块（同世界同列）无强制加载面；`openInterface` 的 `users.remove` 防传送落地再触发（开关去抖）；`floors.size()<2` 无目的地提示 ✓；当前层 COMPASS 不可点、他层 PAPER 可传送；`ElevatorTeleportEvent` 可否决可改向楼层且"取消不占 users 可另选"（注释在案）；传送经 runSync 认领 users + teleportAsync 回调包裹（r31 结论维持）。改名 owner 重校验（r31 修复）维持。

**回归**：全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21；+1）。

## 第 79 轮（2026-08-27）：AncientAltar 迟到点击窗口 + 指南缓存/背包拖拽维持（零缺陷轮·三十）

**祭坛迟到点击窗口详核（干净）**：`usePedestal`——仪式中基座经 `altarsInUse` 拒绝（物品不可中途取放）；放置路径有阻碍检查；取回路径 PedestalItemTakeEvent 可否决可改向 + `removedItems` 30L 防抖（防同物品双取）+ addItem 余量落地（#3476）；`useAltar`——权限失败清 altar 占用（r30 修复）+ **逐基座权限门**（8 基座全查，防跨领地配方）+ 标记全部基座后才启动。`startRitual` 四条退出路径（事件否决/结果未解锁/未知配方/催化剂为空）**全部完整清理** altars + altarsInUse（祭坛与 8 基座）；`canPlayerUseItem` 结果预检；创造不消耗催化剂；仪式任务 10L 后由 AncientAltarTask 执行（基座物品消费的副本提交模式 r48 已核）。主线程串行 + 标记先于消费 → **无迟到点击交错窗口**。

**指南缓存层（干净，r46 维持）**：SlimefunItemStack.item() 的 delegate.clone 模板永不外泄、ItemStackWrapper 不可变契约（meta 单次缓存/clone 零拷贝）——r46 结论完整维持，无新改动面。

**背包 GUI 拖拽边界（干净，r33 维持）**：BackpackListener 拖拽事件独立校验（非 ClickEvent 族）、光标物品校验、数字键/副手/Shift 全覆盖、open-before-claim 次序——r33 深核结论维持。

**5.1.9 发布决策：本轮不发布**。v5.1.8 后累积仅 r78 一项（电梯分页空页——低危 UI 边界），分量不足，继续累积。

**回归**：本轮零代码改动；全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 80 轮（2026-08-27）：AncientPedestal 实体生命周期 + BlockStorage 索引/子命令维持（零缺陷轮·三十一）

**AncientPedestal 放置/取回实体生命周期详核（干净）**：`placeItem`——事件否决/改向 + air/空堆=**显式销毁契约**（消耗但无展示实体，注释明确"生成空气展示会留下不可移除的基座死状态"，与 cargo 事件族一致）+ `System.nanoTime()` 唯一前缀名 + 数量强制 1 + 创造不消耗 + spawnItem 后 setInvulnerable/防拾取标记 + 盔甲架乘客载具。**区块卸载/重载**：物品实体（前缀 displayName 持久化于 NBT）与乘客关系随区块持久化，重载后 `testItem` 前缀识别复找 ✓。**盔甲架生命周期**：物品被清除后重放 → getArmorStand 先查载具再按 testArmorStand 扫描——**旧架复用并刷新 customName**（不泄漏不重复生成）；onBreak 双查（载具+扫描）清理。`getOriginalItemStack`——clone 剥前缀名 + customName（原名）恢复自定义名物品 ✓。onBlockBreak 物品落地不丢失 + 盔甲架移除。前缀识别的伪造面为上游固有设计（r55 记录"两套识别并存"；且伪造成功者的后果是自己的物品被当祭品消耗——无获利面）。

**BlockStorage 区块索引并发（干净，r41 维持）**：chunkIndexLock 内先翻 registered 旗标防异步重索引死网络——结论维持。**CommandsSubCommand 分发深层（干净，r53 维持）**：14/14 子命令权限覆盖完备——结论维持。

**回归**：本轮零代码改动；全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 81 轮（2026-08-27）：HologramProjector/Service 交互边界 + GuideHistory/监视器族维持（零缺陷轮·三十二）

**Projector 与 HologramsService 交互边界复核（干净，既往成果完整）**：`updateText`——迟到聊天的 **owner 重校验**（r34 修复，镜像电梯 renameFloor）+ 方块仍存在的 #3445 检查（迟到消息时投影仪已破则丢弃，上游 TODO 提示消息未做——记录不改）+ 文本事件可否决可改向；`updateOffset`——编辑器循环重开下的同型漂移窗口 owner 重校验（r34）+ 偏移事件可否决；`getArmorStand(projector, true)` 经 HologramsService 池化复用（r55：PDC 精确绑定/重复实体清理/30s 重验证/区块卸载复找）——**投影仪侧的 get/set 与服务侧池化生命周期无交叉缺口**。偏移无 clamp 为上游语义（管理员自担，无功能损害面）。

**GuideHistory 深链剩余路径（干净，r52 维持）**：200 条有界双端队列、内存态设计、add/clear/back 遍历主线程——r11/r52/r59 结论完整维持。**DirtyChestMenu 监视器族未覆盖分支（干净）**：r6/r7 同步化族 + a48082b91（异步关闭 runSync/menus CHM）+ 58061ef53（pushItem 并发同步/合并分支置脏）全覆盖，r66 抽查维持——无未覆盖分支。

**5.1.9 发布决策：本轮不发布**（维持 r79 决策——累积仅 r78 一项低危 UI 边界）。

**回归**：本轮零代码改动；全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 82 轮（2026-08-27）：SlimefunProfiler 高频采集并发面（零缺陷轮·三十三）

**Profiler 核验（干净，既往优化在位）**：`start` 的**按需采集门**——`isProfiling = !requests.isEmpty()`，无 `/sf timings` 请求时 newEntry/closeEntry 走零成本 no-op 路径（endTick 仅记录 tick 总耗时保持 getTime/占位符常新，注释明确机理）；`closeEntry` **双路径合并**——无请求时调用线程内联 `timings.merge`（免 lambda 与 executor 往返）、有请求时 executor 异步（实时检查 requests，采集中途新增请求自动切换），timings 为 CHM 数据一致；`finishReport`——4000×1ms 等待上限 + 超时清空 requests 并发送超时消息（**queued 泄漏有界自愈**：ticker 异常路径未关闭的条目最多阻塞报告 4 秒）；"下一轮采集已开始"中止分支正确；stop() 与 endTick() 双 finishReport 场景幂等（第二份报告 queued==0 且 requests 已清，无重复 summary）；queued/ticks/totalNs 原子字段；kill() 停机收线程。scheduleEntries 负数移除与 TickerTask 衔接 ✓。

**回归**：本轮零代码改动；全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。5.1.9 维持不发（累积仍为 r78 一项）。

## 第 83 轮（2026-08-27）：BackupService 轮换边界 + LanguageService 重载 + bStats（零缺陷轮·三十四）

**BackupService 轮换清理边界详核（干净）**：目录存在守卫（外部清理 data-storage 的 mid-session 自愈——注释在案）；**先清后写次序**保证备份数峰值 MAX+1=21；purge 的名字过滤（杂散文件/残留 tmp 不炸 DateTimeParseException 且不参与轮换名额）+ 新前旧后排序 + **从尾删到 MAX**（既往修复"从错误端删"在位）；同名分钟备份跳过（`!file.exists()`——每分钟至多一份）；tmp+ATOMIC_MOVE/REPLACE 回退 + 失败 deleteTmp（r37）；purge 异常宽捕获不逃逸停机序列。

**LanguageService 运行时重载（设计事实：无此机制）**：languages 为启动期构造的 LinkedHashMap（final 字段发布前构造完成），运行期仅 getLanguage 读取；玩家语言切换经 PDC key 下次读取生效——**不存在运行时 reload 路径**，无对应缺陷面。setLanguage 的 getConfig().clear() 仅启动期路径。

**bStats/Metrics 上报面（设计事实：不存在）**：全源码无 org.bstats 引用（Profiler 中的 bStats 字样仅为历史注释）——本 fork 无遥测上报面。

**回归**：本轮零代码改动；全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。5.1.9 维持不发。

## 第 84 轮（2026-08-27）：EnhancedFurnace 原版熔炉增强族（零缺陷轮·三十五）

**EnhancedFurnace 族复核（干净，既往加固在位）**：`EnhancedFurnaceListener.onFuelBurn`——非原版熔炉排除（Smoker/Blast Furnace）+ #2958 世界禁用门 + fuelEfficiency>0 门 + EnhancedFurnaceBurnEvent 可否决可改向 + **long 乘法后钳制**（注释明确"addon 可经事件提高 fuelEfficiency，int*int 在 Short.MAX_VALUE 钳制生效前就回绕成负燃烧时长"）✓。`onItemSmelt`——#3741 smelting 槽 null 门 + 幸运材料 tag 判定随机产量 + **结果槽余量双重钳制**（事件前的 amount 与事件改向后的 finalAmount——注释明确"防 listener setAmount（事件仅校验 ≥1）超堆结果槽"）+ **clone 保留配方向 ItemMeta**（裸 new ItemStack(type, amount) 会剥展示名/lore）+ amount 解析为 0 时跳过（ItemStack 零数量构造抛异常）✓；HIGHEST+ignoreCancelled 优先级正确；事件改值族（setFuelEfficiency/setAmount）setter 校验与消费端钳制双层在位。`EnhancedFurnace` 本体为纯数据（getters/构造校验）；RecipeService.getFurnaceOutput（r65 修复后）衔接正确。

**回归**：本轮零代码改动；全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。5.1.9 维持不发。

## 第 85 轮（2026-08-27）：现代 API 清点 + GitHub 连接器网络超时（1 项修复）

**版本新 API 使用面清点（设计事实）**：本 fork 全源码未使用 EntityDisplay 族/RegionScheduler（Folia）等现代 API——HologramProjector 等仍为传统 ArmorStand+PaperLib 模式（Display 命中为注释误报）。用户目标"版本新 API 可用性"的审查重心实际落在 **1.21.11 运行时兼容**（历次实机回归零错误佐证）与 **java.net.http.HttpClient 等 JDK 层 API**（GitHub 连接器）——后者本轮命中缺陷。

**GitHub 连接器请求无超时（1 项修复，网络服务稳定性）**：`HttpClient.newHttpClient()` 无 connectTimeout、请求无 `.timeout()`——**防火墙静默丢包**（非 RST，DROP 场景在离线/受限网络极常见）下 `send()` 无限挂起。`GitHubTask.connectAndCache` 在**单一异步任务线程串行**执行全部连接器（逐连接器隔离仅防异常不防挂起）：一个挂起请求 → 该任务线程永久泄漏 + 后续连接器停摆直至重启；每轮调度的新任务再挂 → 线程累积。修复：请求加 `timeout(30s)`——`HttpTimeoutException` 属 IOException，自然汇入既有"连接失败→缓存文件回退→onFailure"链，行为兼容。防御性修复无独立红绿测试（挂起需真实 DROP 环境；单测下不可达地址走快速失败 IOException 路径而非挂起），以全量 3218 全绿佐证无回归。

**其余核验（干净，既往成果在位）**：GitHubTask 主线程防误调 + 逐连接器异常隔离 + grabTextures 的 #3241 30s future 超时 + 429 限速退避重试 + MAX_REQUESTS_PER_MINUTE 节流 + 未完成连接器跳过存盘；GitHubConnector 缓存文件读写（损坏回退 null + 目录重建重试）。

**回归**：全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。5.1.9 累积评估：r78+r85 两项（低危 UI 边界 + 网络防御），继续累积。

## 第 86 轮（2026-08-27）：玩家上下线档案/抓钩断线清理 + v5.1.9 发布闭环

**PlayerProfileListener 复核（干净）**：断线/踢出仅 `find` 缓存命中才 `markForDeletion`——真实删除由 auto-save 的"已保存 && 已标记 && 玩家不在线"三重门执行（r16/r61 链衔接）；无 PlayerJoinEvent 预载（档案惰性加载设计）。**GrapplingHookListener/Entity 全链复核（干净）**：`markHandled()` CAS 防同箭多事件（damage+projectile-hit+hanging+portal 四事件族）重复掉落抓钩（注释明确"Arrow#isValid 时序敏感且版本依赖"的加固动机）；#253 despawn 定时（箭+蝙蝠自清理）+ 定时后延迟清 map/免疫集合；断线/踢出清理条目；#2351 主手抓钩拴绳取消；坠落免疫 `remove` 单次语义；全主线程事件路径（HashMap 安全）。**SoulboundListener/GuideListener 的 quit 路径**维持 r35/r43 结论。

**v5.1.9 发布闭环**：全量 **3218 项测试通过、0 失败**；pom bump；独立实例实机 Paper 1.21.11：Slimefun v5.1.9 加载启用、Done 53.7s、**全日志 ERROR/SEVERE=0**、RCON stop 送达、优雅停机确认。发布内容：r78（电梯分页空页）+ r85（GitHub 连接器超时）。

## 第 87 轮（2026-08-27）：剩余小监听器清点（Join/MiddleClick/Network）（零缺陷轮·三十六）

**JoinListener 复核（干净）**：加入时经 `PlayerProfile.get` 异步回调缓存护甲哈希——null/AIR/`instanceof SlimefunArmorPiece` 三重门后 update（供 RadiationTask 判定），异常由 AbstractArmorTask 族外的此路径自身短路（数组恒 4 格）。**MiddleClickListener 复核（干净）**：创造模式中键取 SF 方块——#3483 的 `isActualMiddleClick` 双判定（仓外点击 cursor==方块类型且 currentItem==AIR + WALL_HEAD/PLAYER_HEAD 边角补判）+ `getTargetBlockExact(5)` null 守卫 + 热键栏已有同物**交换槽位优先**（cancel 而非新给）+ `setCursor(clone)` 不污染模板 + 事件可否决；创造中键取物本身即为创造权限域。**NetworkListener 复核（干净）**：MONITOR+ignoreCancelled 的三事件（破坏/放置/爆炸工具 #3013 补丁）触发 `updateAllNetworks` 全网重算——r41 NetworkManager 结论衔接。至此 `implementation/listeners` 目录 43 个监听器全部有轮次覆盖（本清单 + 历轮）。

**回归**：本轮零代码改动；全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 88 轮（2026-08-27）：交互监听器五族批量核验（VillagerTrading/Boots/SeismicAxe/BeeWings/ElytraImpact）（零缺陷轮·三十七）

**VillagerTradingListener 复核（干净）**：HOTBAR_SWAP 数字键无条件取消（防换入绕过）+ 点击/拖拽**双路径**（拖拽独立校验，r33 同族注释在案）+ `SlimefunItemVillagerTradeEvent` 的**否决语义反转**注释明确（"cancelled = 允许交易"）+ `isUnallowed` 四重门（null/VanillaItem/SyntheticEmerald/disabled）+ DENY 结果提示消息。**SlimefunBootsListener 复核（干净）**：canUse 研究门前置；坠落（Stomper 踩踏/LongFall 消除）、末影珍珠保护、农夫鞋防踩踏三族各自事件可否决；Ender/Trample 零监听者免分配门控在位（Fall 事件为低频路径无条件 fire，风格差异记录不改）。**SeismicAxeListener**（metadata 标记+命中即清理）、**BeeWingsListener**（isGliding 门+canUse+着陆任务）、**ElytraImpactListener**（gliding 集合 1-tick 窗口的"同 tick 双玩家互擦防护"注释在案 + find 空时 request 异步回退）均干净。

**回归**：本轮零代码改动；全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 89 轮（2026-08-27）：Hopper/Dispenser 监听器 + BeeWingsTask 着陆任务（零缺陷轮·三十八）

**HopperListener 复核（干净）**：`getLocation()` **null 前置于 BlockStorage.check**（注释明确"其它插件可对虚拟库存发合成 InventoryMoveItemEvent，getLocationInfo 对 null Location 会 NPE，旧顺序使 null 检查成为死代码"）+ NotHopperable 门 + HopperTransferPreventEvent 可否决（零监听者零分配）。**DispenserListener 复核（干净）**：#2959 世界禁用门 + SlimefunBlockDispenseEvent 零监听者免分配（"发射器可高频触发"注释）+ callItemHandler + BlockState instanceof Dispenser 门。**BeeWingsTask 复核（干净）**：下降判定（WORLD_SURFACE 最高块 + 负值结构下方时 6 格限深逐块 getDistanceToGround + ≤4 低空）双路径；`isValid` 五条件自取消（离线/无效/死亡/停滑/**已有缓降效果**）；**多任务并行自愈**——同一次滑翔中 ToggleGlide 边缘重复触发产生的第二个任务在首次 isValid 检查时因玩家已获缓降而自取消（幂等）；slowDown 的 setFallDistance(0)+效果施加幂等。记录不改：BeeWingsSlowFallEvent 无零监听者门控（玩家个体 3-tick 低频路径，风格差异）。

**回归**：本轮零代码改动；全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 90 轮（2026-08-27）：crafting/ 工作站监听器子目录清点（零缺陷轮·三十九）

**crafting/ 九监听器复核（干净）**：`SlimefunCraftingListener` 基接口——指南物品排除（isGuideItem 双参先判）、`isUnallowed` 三门（null/VanillaItem 豁免/disabled 豁免）、`findUnallowedItem` null 安全（注释明确"指南物品不参与事件——它们没有可暴露的 SlimefunItem"）；工作站族（Grindstone 抽查）模式一致——rawSlot 结果槽 + 容器类型门 + hasUnallowedItems 预检 + `SlimefunItemWorkstationEvent` 可否决（找到具体物品才 fire）+ **DENY 兜底在事件外**（findUnallowedItem 返回 null 时仍拒绝——防绕过）。CraftingTableListener（CraftItemEvent+PrepareItemCraftEvent 双挂点）、Anvil/Smithing/Cartography/Cauldron/BrewingStand/Crafter 同族模式。**entity/ 子目录**维持 r52 收尾清点结论（WitherProof/#3414 家族/简单门控型）。至此 listeners 全树（43 文件 + crafting/9 + entity/7）**覆盖闭合**。

**回归**：本轮零代码改动；全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 91 轮（2026-08-27）：implementation/setup 装载序列剩余路径（零缺陷轮·四十）

**PostSetup 深核（干净，既往成果维持+本轮补全）**：`loadItems`——null 物品移除 + 逐物品异常隔离（含 LinkageError）+ RegistryFinalized 事件 + 配置原子化双保存 + **autoLoading 最后置位**（装载完成前的运行期注册不触发 load——r44/r49 结论）；`loadOreGrinderRecipes`——GrindStone/OreCrusher 配对遍历（null 槽守卫）+ **高投入配方优先排序**（非 legacy 模式下 8 圆石→沙 优先于 1 圆石→沙砾——防低配抢匹配）；`loadSmelteryRecipes`——配对遍历 + 尘→锭**重定向双机**（INGOT_FACTORY 正向 8s/PULVERIZER 逆向 3s）+ 电力冶炼炉配方**按输入数量降序**（多输入优先防单输入抢匹配）+ MakeshiftSmeltery 同步注册；`isDust` 的 `_DUST` 后缀判定经 getByItem（SF 物身份而非裸材质）。**ResearchSetup（干净）**：`alreadyRan` 一次性门（防重复装载双绑定）+ 全声明式注册（SlimefunItems 枚举引用——绑定侧 addItems→setResearch 静态安全，r60 链）。**SlimefunItemSetup**（2743 行声明族，r45 配方冲突结论维持）、**DefaultItemGroups**（r59 锁组结论维持）、**RickFlexGroup**（FlexItemGroup 特化）。

**回归备注**：本轮全量首次执行遇**瞬态构建失败**（测试 3218 全绿但 jacoco 报告阶段失败——疑似与系统残留的并行 maven 进程争用 target 目录有关），**重跑 BUILD SUCCESS、3218 项通过、0 失败、0 错误、7 跳过**（Java 21），以重跑为准记录。

**瞬态失败根因补记（环境层，非项目缺陷）**：首次全量执行期间 surefire JVM 产生 hs_err_pid41088.log——`EXCEPTION_ACCESS_VIOLATION` 于 **G1 Concurrent GC 线程（"G1 Conc#2"）**，栈为纯 VM 内部帧（jvm.dll V 帧链 + ucrtbase/kernel32），**零应用/JNI 库帧**；命令行含 jacoco 0.8.12 javaagent（与用户 memory 中"特定 JVM 与 jacoco/MockBukkit 组合不兼容"同域）。判定：JDK 21.0.10 (21.0.10+8-LTS) G1 并发线程瞬态崩溃（环境层），与项目代码无关——同一测试集立即重跑 3218 项全绿佐证。诊断文件已清理（不入库）。

## 第 92 轮（2026-08-27）：SlimefunItemSetup 配方输出抽样 + attributes 消费侧（零缺陷轮·四十一）

**声明族配方输出抽样（干净，结构性排除）**：`SlimefunItemSetup`（2743 行）无 `registerRecipe` 调用——物品合成配方以 `new ItemStack[9]` 输入数组形式传入 SlimefunItem 构造器，**输出即物品自身**（构造器 item 参数），不存在独立的输出数组——"配方输出错误"面在结构上不存在；输出正确性等价于物品定义正确性（r28 已整读复核 SlimefunItems 全量）。程序化异常扫描（空输入数组/纯 null 输出/孤悬 null）仅命中合法形态（输入数组的 null 空槽、RecipeType 构造的 recipeOutput=null 参数）。**core/attributes 消费侧（既往覆盖维持）**：EnergyNetComponent（r63/r67）、DamageableItem（r49）、NotHopperable（r89）、RecipeDisplayItem（r84）、NotConfigurable/Placeable（r64）等主要接口的消费侧均已由对应轮次覆盖；CargoNet/DirtyChestMenu 的 Paper 1.21.11 兼容由历次实机回归（v5.1.3-v5.1.9 六轮零错误）与 benchmark（r45 无回归）佐证。

**回归**：本轮零代码改动；全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 93 轮（2026-08-27）：GitHubTask 皮肤拉取超时漏点（1 项修复）

**皮肤纹理 future.get() 无超时（1 项修复，r85 同族漏点）**：`pullTexture` 内两个网络 future 不对称——UUID 查询有 #3241 的 30 秒超时，**PlayerSkin 纹理查询 `future.get()` 无参无限等待**。sessionserver.mojang.com 被防火墙静默丢包时，grabTextures 所在异步任务线程无限挂起（贡献者纹理更新停摆、线程泄漏）——与 r85 GitHubConnector 无超时同族（r85 修 HttpClient，此处 future 漏点）。修复：同样 30 秒超时（TimeoutException 由 requestTexture 的既有 catch 兜底：WARNING 归因 + 429 退避重试 + 返回 -1 跳过）。防御性修复无独立红绿测试（同 r85 理由——挂起需真实 DROP 环境），全量 3218 全绿佐证无回归。

**回归**：全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。v5.1.9 后累积本项，待分量。

## 第 94 轮（2026-08-27）：无界等待族全局收口（零缺陷轮·四十二）

**无界等待族系统性扫描（干净——族收口完成）**：继 r85（HttpClient.send）、r93（future.get）两处修复后全局扫描全部无界等待形态——①`future.get()` 无参调用：仅剩 r93 修复处的声明行与注释行（已带 30s）；②`Thread.sleep`：三处全部有界（PlayerProfile 加载重试 100ms×attempt 三退避必终止 r16、Profiler 等待 4000×1ms 上限 r82、TickerTask 停机序列 r54）；③`CountDownLatch.await()`/`BlockingQueue.take()` 等阻塞原语：全源码零使用。**结论：无界等待族（网络挂起致线程泄漏的全部已知形态）在本代码库中已收口**——r85/r93 两处修复是仅有的实例。

**回归**：本轮零代码改动；全量 **3218 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。v5.1.9 后累积 r93 一项，继续观察。

## 第 95 轮（2026-08-27）：addItem 蒸发族全局收口（1 项修复，3 处漏点）

**addItem 返回值丢弃族系统性扫描（3 处漏点）**：全源码玩家 `Inventory.addItem` 共 8 处——5 处已处理余量（LimitedUseItem/GiveCommand excess/r33 BackpackCommand/r49 祭坛 #3476/r71 cheat），**3 处丢弃返回值**：①`GuideCommand`（/sf guide 发放）；②`DebugFishCommand`（/sf debug fish）；③`SlimefunGuideListener`（入服自动发放指南——背包满时蒸发且因 `!hasPlayedBefore()` 一次性标记**不再重发**，玩家永久拿不到指南）。均为 r32/r33/r71 同族。修复：抽 `SlimefunUtils.giveOrDrop(Player, ItemStack)`（addItem 余量 dropItemNaturally 落脚），三处统一改用；r71 的 giveCheatItem 保留独立实现（shift 满堆语义特殊）。红绿双向验证：背包满 41 格判别还原裸 addItem 恰 1 红（expected 2 was 1——掉落物缺失=蒸发实证）。**族收口结论：8 处全部处理余量，该缺陷族闭合。**

**回归**：全量 **3219 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21；+1）。v5.1.9 后累积 r93+r95（网络超时+蒸发族收口），分量已足，下轮可发 5.1.10。

## 第 96 轮（2026-08-27）：v5.1.10 发布闭环

**v5.1.10 发布**：pom bump；全量 **3219 项测试通过**（r95 后无代码改动沿用）；独立实例实机 Paper 1.21.11：Slimefun v5.1.10 加载启用、Done 35.1s、**全日志 ERROR/SEVERE=0**、RCON stop 送达、优雅停机确认。发布内容：r93（皮肤拉取 30s 超时）+ r95（addItem 蒸发族三处收口：/sf guide、/sf debug fish、入服指南发放——新增 SlimefunUtils.giveOrDrop）。

## 第 97 轮（2026-08-27）：SlimefunUtils 工具族全方法清点（零缺陷轮·四十三）

**SlimefunUtils 复核（干净，既往成果全覆盖）**：21 个公开方法逐一对照——noPickup 标记族（r80 祭坛消费侧）、soulbound 判定/设置双变体（r35/r48）、isRadioactive（r67）、getCustomHead（r55 头纹理）、containsSimilarItem/isItemSimilar×3/equalsLore（r46 ItemStackWrapper 不可变契约族）、giveOrDrop（r95）、**updateCapacitorTexture + clearStaleCapacitorTextures**（缓存有界化清扫——注释明确"宁错杀：误清存活条目无害可重建，长期繁忙服务器上无界增长被界定"+卸载区块/消失世界异常视同已清）、isSameOwner/getOwner/ownerTrackingPlaceHandler（owner 族——getOwner 损坏 UUID 回退 null 且 javadoc 明确"null 宽松"契约；消费侧 r30/r34 反应堆端口/安卓接口）、canPlayerUseItem（r79）、spawnItem 三变体（SlimefunItemSpawnEvent 可否决且**改向后的位置/物品生效**）、isInventoryEmpty（1.16+ 版本分派原生 API）。**CargoUtils vanilla 双界**维持 r56 结论；**benchmark 复测不适用**（r93/r95 均为冷路径修复——网络超时与背包满掉落，无热路径改动）。

**回归**：本轮零代码改动；全量 **3219 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 98 轮（2026-08-27）：Slimefun 主类 onEnable 启动序列（零缺陷轮·四十四）

**onEnable/onPluginStart 全序列复核（干净）**：三分支门（UNIT_TEST/版本不支持禁用自身/正常启动）；CS-CoreLib 劝退禁用、Java 版本建议、新装标记（data-storage 存在性）；目录创建→registry 装载→本地化→networkSize<1 回退 1→存储→GEO→tags→物品→研究→监听器→SlimefunStartupTask（延迟 0，PostSetup 衔接 r44）→命令注册异常隔离→armor/radiation/rainbow 任务族（**radiation 依赖 armor-effects 的显式警告**）→autoSave/holograms/ticker→集成/GitHub→**电容纹理缓存清扫定时任务**（r97 的 clearStaleCapacitorTextures 消费侧，注释明确有界化动机）。

**SolarHelmetTask interval 未乘 20 的语义澄清（设计非缺陷，记录不改）**：`SolarHelmetTask.schedule(armor-update-interval)` 裸值（armor/rainbow 族均 ×20L 转 tick）——对照 `SolarHelmet.charge-amount` 默认 **0.01J**（DoubleRangeSetting min 0.01）：微充量 × 高频（interval=2 → 每 2 tick≈每秒 10 次）= **涓流充电设计**（≈0.1J/s 持续小充），上游同款。命名共享"armor-update-interval"键但双频率语义有意——记录为命名混淆不改。

**回归**：本轮零代码改动；全量 **3219 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 99 轮（2026-08-27）：ChargeUtils 写侧收口（零缺陷轮·四十五）

**ChargeUtils 复核（干净，读写双向闭合）**：`setCharge`——三重 Validate（charge≥0/capacity>0/charge≤capacity，负值与超容在写入前拒绝）+ `BigDecimal.setScale(2, HALF_UP)` 精度规范化（防浮点尾差显示漂移）+ PDC FLOAT 写入 + **lore 行原位替换**（regex 匹配既有电量行则替换返回，否则追加——防 lore 无限增长）。`getCharge`——PDC 优先（负/NaN/无限值 → 0 的 fail-closed，r32 加固注释明确"防异步 ticker 异常销毁机器"）+ lore 回退路径**同源消毒**（regex 允许任意长数字串、parseFloat 饱和为 Infinity 不抛——显式 isFinite 检查）+ 消毒后回写 PDC（lore→PDC 一次性迁移）+ NumberFormatException → 0。**记录（上游行为，不改）**：多行重复电量 lore（addon 引入）只原位更新首个匹配行——PDC 为真值，纯显示层残留。至此 Rechargeable 充电链读写双向收口（读侧 r32、写侧本轮、SolarHelmet 消费侧 r98）。

**回归**：本轮零代码改动；全量 **3219 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 100 轮（2026-08-27，百轮里程碑）：UnbreakingAlgorithm + RadiationUtils（零缺陷轮·四十六）

**UnbreakingAlgorithm 复核（干净）**：ARMOR 公式 `random ≥ 0.6 + 0.4/(lvl+1)`（生效率上限 40% 的护甲特例）与 TOOLS 公式 `random ≥ 1/(lvl+1)`（无限递增）均与原版一致；`evaluate` 的 lvl>0 门（零级永不免伤）。记录不改：`Math.random()` 全局源（并发竞争由 Random 内部 CAS 保证正确性，仅吞吐差异——上游行为）。**RadiationUtils 复核（干净，r39 加固维持）**：`addExposure`/`removeExposure` 的**双端钳制**注释在案（上限 100 防永远达不到的积累、下限防 addon 巨值 int 溢出回绕为负——"负值持久化后 RadiationDamageEvent 的 ≥0 校验每 tick 主线程抛错"机理）；getExposure 的 orElse(0) 缺省。至此 `utils/` 目录 20 个文件全部闭合（SlimefunUtils r97 全方法、ChargeUtils r99 双向、本轮两件、其余既往轮次）。

**百轮里程碑总结**：5.1.x 审计 100 轮，累计修复 **26 项缺陷**（数据完整性 9/并发线程 5/API 正确性 6/功能语义 4/配置 2），发布 **10 个版本**（v5.1.1→v5.1.10），测试从 3153 增至 **3219 项全绿**（+66），完成三族系统性收口（无界等待族 r94、addItem 蒸发族 r95、充电链 r99），listeners 全树（59 文件）、utils 全目录、setup 全包、存储/网络/任务/命令/事件五层均有轮次覆盖；三个缺陷族收口 + 全目录覆盖 + 持续实机零错误构成收敛证据链，但循环按目标要求继续。

**回归**：本轮零代码改动；全量 **3219 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。

## 第 101 轮（2026-08-27）：dough PlayerSkin 网络实现内部（1 项修复，r93 补全）

**PlayerSkin.fromPlayerUUID 传输层无超时（1 项修复，r93 修复的补全）**：内部实现经 `new URL(...).openStream()`（HttpURLConnection 默认零超时=无限）——**r93 的调用侧 future.get(30s) 只让调用方放弃**，执行线程仍永久阻塞在流上：每挂一个请求泄漏一个 **Paper 有界异步调度池**线程，累积至池耗尽后**全插件所有异步任务饿死**（比线程泄漏更严重的级联面）。修复：HttpURLConnection 显式 connect/read 双 10s 超时，SocketTimeoutException 自然汇入既有 completeExceptionally 异常完成路径（调用侧 r93 超时兜底不变）。防御性修复无独立红绿测试（同 r85/r93 理由），全量 3219 全绿佐证。**至此 Mojang/GitHub 网络拉取链（GitHubConnector r85 + GitHubTask 调用侧 r93 + PlayerSkin 传输层 r101）三层超时全闭合。**

**回归**：全量 **3219 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 102 轮（2026-08-27）：dough UUIDLookup + StatusEffect 效果层（零缺陷轮·四十七）

**UUIDLookup 复核（干净）**：本 fork 已重写为现代 `java.net.http.HttpClient`（类注释"Version-compatible replacement"）——请求带 **`.timeout(Duration.ofMinutes(2))`**（无 r101 型传输层无超时缺陷）、用户名 `\w{3,16}` 正则门（防注入/非法名请求）、success 布尔判空返回 null 语义、异常经 CompletableFuture 链自然传播至 r93 的调用侧超时兜底。**PlayerSkin/UUIDLookup 两网络实现对照结论**：UUIDLookup 已被 fork 现代化（带超时），PlayerSkin 是遗留 openStream 路径（r101 已补）——皮肤拉取链（GitHubTask→UUIDLookup→PlayerSkin）三层全部带界。**StatusEffect 复核（干净）**：`level;expiry` 字符串编码（时间戳语义=当前+时长；0=永久）；isPresent 的损坏回退 false + 过期自动 clear + NumberFormatException 捕获；getLevel 损坏回退 empty；clear 直接移除键。**记录（上游 API 语义，不改）**：getLevel 不判过期（与 isPresent 分离）——RadiationUtils 全用 addPermanent（永不过期）无实际影响。**AutoSavingService 与 BlockStorage.save 写盘交互**维持 r32/r45/r54 结论（异步 saveAllBlocks 逐世界隔离 + computeChanges 快照 + onDisable 先关后存）。

**回归**：本轮零代码改动；全量 **3219 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。v5.1.10 后累积 r101 一项，继续观察。

## 第 103 轮（2026-08-27）：GitHubService 刷新周期单位错误（1 项修复）

**刷新周期 tick/毫秒混淆（1 项修复，功能与表述不符）**：`GitHubService.start` 的 `runTaskTimerAsynchronously(..., period)` 第三参数单位为 **tick**，但传入 `TimeUnit.HOURS.toMillis(1)`（=3,600,000 毫秒值）——实际刷新周期 = 3.6M tick / 20tps = **50 小时**而非意图的 1 小时（贡献者数据两天一更，与任务 javadoc "every X minutes" 及周期刷新语义不符）。上游同款笔误（经典 tick/ms 混淆族）。修复：抽 `getRefreshPeriodTicks()` = `HOURS.toSeconds(1) × 20L`（72000）。红绿双向验证：还原毫秒版恰 1 红（expected 72000 but was 3600000）。**遗留 HTTP 形态全局收口佐证**：本轮扫描确认全源码 `openStream/openConnection` 仅剩 PlayerSkin 的 r101 修复处（带双超时）——网络层遗留形态闭合。

**回归**：全量 **3220 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21；+1）。v5.1.10 后累积 r101+r103 两项（传输层超时+刷新周期），下轮可发 5.1.11。

## 第 104 轮（2026-08-27）：v5.1.11 发布闭环

**v5.1.11 发布**：pom bump；全量 **3220 项测试通过**（r103 后无代码改动沿用）；独立实例实机 Paper 1.21.11：Slimefun v5.1.11 加载启用、Done 55.5s、**全日志 ERROR/SEVERE=0**、RCON stop 送达、优雅停机确认。发布内容：r101（PlayerSkin 传输层双 10s 超时——防调度池线程泄漏）+ r103（GitHub 刷新周期 50 小时→1 小时）。至此 Mojang/GitHub 网络链全部带界且按正确周期刷新。

## 第 105 轮（2026-08-27）：tick/ms 混淆族收口 + ThreadService 双缺陷（2 项修复）

**tick/ms 混淆族全局扫描（干净——族收口）**：r103 之后全扫 `toMillis/toSeconds` 与调度调用——toMillis 仅剩 Profiler 的纳秒→毫秒统计换算（正确用法）；全部 runTaskTimer/Later 的 delay/period 均为显式 tick 字面量（6000L/600L/UPDATE_INTERVAL 等）——**r103 为该族唯一实例**。

**ThreadService 双缺陷（2 项修复）**：①`newScheduledThread(..., delay, period, unit)` 的 **period 参数被静默丢弃**（`scheduleWithFixedDelay(..., delay, delay, unit)` 把 delay 传两次）——声明的"每 period 重复"API 契约从未成立，任务按初始延迟间隔重跑；全源码无调用者（死代码）故无运行时影响，按 API 可用性目标修复契约。②**线程池无关闭**——`scheduledPool` 的非守护核心线程永不回收，服务器关停经 System.exit 强退无碍（历次实机优雅停机正常佐证），但 **reload/插件卸载重载场景**跨实例累积线程；新增 `shutdown()`（双池）接入 onDisable 紧随 profiler.kill（停机序列补全，r54 十步的第 2.5 步）。契约/停机修复无独立红绿（无行为消费者与无法单测停机），全量 3220 全绿佐证。

**回归**：全量 **3220 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。v5.1.11 后累积本两项。

## 第 106 轮（2026-08-27）：UpdaterService + PermissionsService（1 项修复）

**UpdaterService 复核（干净，设计事实）**：fork 已移除自动更新检查与下载——现仅做本地分支检测（UNOFFICIAL/Dev/RC/UNKNOWN 前缀判定），无任何外部连接面。**PermissionsService 复核（1 项修复）**：register 的 setDefaultValue 回写 permissions.yml（原子化保存）+ hasPermission 的 null item fail-open 与 "none" 免权限 + setPermission/save 运行期 API 均正确；**发现**：`permissions` 为 HashMap——`hasPermission` 经 `SlimefunItem#canUse` 可从**异步机器 ticker** 到达，与主线程运行期注册物品的 register put 构成并发读写面（结构损坏理论风险，与既往多处 CHM 修复同族）——改 ConcurrentHashMap（零语义变化）。防御性修复无独立红绿，全量 3220 全绿佐证。

**5.1.12 发布决策：本轮不发布**。v5.1.11 后累积 r105 两项（死代码契约+reload 边缘）+ r106 一项（理论并发面）——三项均为防御/边缘性质、无玩家可感知变化，继续累积至出现有分量修复。

**回归**：全量 **3220 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 107 轮（2026-08-27）：ChestMenuUtils 菜单工具族（零缺陷轮·四十八）

**ChestMenuUtils 复核（干净）**：静态 UI 模板（SlimefunItemStack.item() 的 delegate.clone 语义——r46 模板永不外泄）；**getDefaultOutputHandler 的输出槽三路防护**——NUMBER_KEY 检查热键栏目标槽（防数字键把物品换入输出槽）、SWAP_OFFHAND 检查副手（防 F 换入）、cursor 非空气禁止（防放替换）——仅空手可取；prev/next 按钮的 active/inactive 状态边界（page==1 / page==pages）正确；getBackButton/getMenuButton/getSearchButton/prev/next 均 CustomItemStack.create 包装（每次克隆+按玩家本地化），getWikiButton/getBackground 等裸返静态模板（无 per-player 定制、槽位均空点击 handler 无修改路径——上游设计，记录不改）；drawBackground varargs；**updateProgressbar 的无观看者早退**（性能优化注释在案）+ indicator.clone + Damageable 耐久进度；getProgressBar 的 20 格百分比条与 getDurability 的先除后乘近似均为上游算法。

**回归**：本轮零代码改动；全量 **3220 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。5.1.12 维持不发（累积仍为三项防御性）。

## 第 108 轮（2026-08-27）：dough 本地副本收尾（PlayerHead/CustomGameProfile）（零缺陷轮·四十九）

**dough 本地副本盘点（4 文件全闭合）**：本 fork 的 dough 副本仅 skins 包 4 文件——PlayerSkin（r101 传输层超时）、UUIDLookup（r102 已现代化）、**PlayerHead（本轮）**——getItemStack 双变体 null 校验 + meta null 守卫 + 每次 new（无共享模板污染面）；setSkin 的材质门（非头方块抛带信息的 IAE）+ Skull instanceof + **Paper profile 路径**（createProfile + textures Property，texture null 守卫）+ state.update(true, sendBlockUpdate)；**CustomGameProfile（本轮）**——全 final 不可变 + toPlayerProfile 的 textures.setSkin(url) + apply 设置 ownerProfile。**dough 依赖 jar 内的其余类（Config/OptionalMap 等）非本地副本**，其 Config#save 原子化已由 r29 的本地适配覆盖。至此 dough 本地副本面全部闭合。

**回归**：本轮零代码改动；全量 **3220 项测试通过、0 失败、0 错误、7 跳过**（本轮重新执行确认，Java 21 BUILD SUCCESS）。5.1.12 维持不发。

## 第 109 轮（2026-08-27）：TextureService/SoundService（1 项修复）

**CustomTextureService 复核（干净）**：item-models.yml 内置资源加载（异常隔离+SEVERE 归因）、逐物品 setDefaultValue 回写（原子化保存）、`modified` 激活标记（判定资源包使用中）、`setTexture` 的 0→null 清除 CustomModelData 语义正确。**SoundService 复核（1 项修复）**：reload 的**逐音效异常隔离** + 默认三键（sound/volume/pitch）+ volume<0→0、pitch<0.5→0.5 的越界钳制（带 WARNING 归因）均正确；**发现**：`soundMap` 为 EnumMap（非线程安全）——reload 在主线程 put（启动/StartupTask），而 `SoundEffect#playFor/playAt` 的读者可运行于**异步机器 ticker**（r74 机器人/r80 祭坛等）——与 r106 PermissionsService 同族的并发读写面。修复：改 ConcurrentHashMap（soundMap 无迭代序依赖，零语义变化）。防御性修复无独立红绿，全量 3220 全绿佐证。

**回归**：全量 **3220 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。v5.1.11 后累积四项防御性修复，继续观察。

## 第 110 轮（2026-08-27）：非同步 Map 异步读写族全局收口（2 项修复）

**族扫描（r106/r109 暴露同族系统性清点）**：全源码非并发 Map 字段 14 处逐一判定——安全 12 处：EnchantmentRune（构造期填充+JMM final 冻结+registry CHM 发布链）、LocalizationService.languages（r83 启动构造/运行只读）、EnergyNet 内部三表（网络 tick 单线程 r63）、CargoNet.roundRobin/CargoNetworkTask 内部（主线程 r32/r76）、SlimefunCommand.commandUsage（主线程命令）、ContributionsConnector.aliases（构造期）、blockMenuPresets（无异步读路径，r49 结论维持）、guides（EnumMap 启动注册/主线程读）。**修复 2 处**：①`SlimefunRegistry.slimefunIds`——`getById` 被异步机器 ticker 调用（自动合成器解析机器等）vs 主线程运行期注册 put——r49 曾结论"注册仅主线程可接受"，按 r106（PermissionsService）修复先例一致性升级为 CHM（getById 热路径无锁读零性能损失）；②`PerWorldSettingsService.disabledAddons`——WorldLoadEvent 可在任意时刻触发 `load→putIfAbsent`（主线程）vs 异步 ticker 的 `isEnabled` 读——同族。**至此非同步 Map 异步读写族全部收口**（r106 Permissions + r109 soundMap + r110 两处）。

**回归**：全量 **3220 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。v5.1.11 后累积六项防御性修复（并发族五项+停机一项）——并发族收口完成，已达发布分量，下轮发 5.1.12。

## 第 111 轮（2026-08-27）：v5.1.12 发布闭环

**v5.1.12 发布**：pom bump；全量 **3220 项测试通过**（r110 后无代码改动沿用）；独立实例实机 Paper 1.21.11：Slimefun v5.1.12 加载启用、Done 38.9s、**全日志 ERROR/SEVERE=0**、RCON stop 送达、优雅停机确认。发布内容：并发族五项（r106 PermissionsService、r109 SoundService、r110 slimefunIds+disabledAddons 的 CHM 化）+ 停机一项（r105 ThreadService shutdown 与 period 契约）。**"非同步 Map 异步读写"成为第 4 个系统性收口的缺陷族**（前三：无界等待 r94、addItem 蒸发 r95、充电链 r99）。
