# SlimeFun4.1 v5.1.x 审计追踪

延续 [5.0.0-audit.md](5.0.0-audit.md) 的 /loop 审计循环，覆盖 5.1.0（全面汉化）与 5.1.1 之后的新改动面。每轮覆盖不同方面，本文件记录已覆盖范围、发现与处置。

## 第 1 轮（2026-08-27）：5.1.0 汉化的逻辑闭合——双语兼容机制 + 字符串匹配契约全量核查

**背景**：5.0.0 审计循环（r1-r170）结束于汉化之前，5.1.0 是此后最大的一批改动（显示层/资源层全量替换）。本轮核查发布说明声称的兼容机制是否真实成立（"表述与实现相符"检查），以及汉化是否引入新的字符串匹配失配。

**4 处双语兼容机制逐条核实（全部成立）**：
1. **灵魂绑定行**（`SlimefunUtils:74-81`）：`isSoulbound` PDC+lore 双因子，lore 侧 `contains(SOULBOUND_LORE) || contains(SOULBOUND_LORE_LOCALIZED)` 新旧两行都认（整行 equals，符文 lore 里的句子不会误匹配）；`setSoulbound(false)` 两版都删；`setSoulbound(true)` 只写中文行 ✓
2. **剩余次数正则**（`PatternUtils.USES_LEFT_LORE`）：`§e[0-9]+ (Uses? §7left|次 §7剩余)` 与写入侧 `LoreBuilder.usesLeft`（`&e<N> 次 &7剩余`）格式精确一致，`matches()` 全行匹配成立；英文 `Uses?` 兼容单复数 ✓
3. **多功能工具模式行**（`MultiTool`）：`LORE_PREFIX`（中文）/`LORE_PREFIX_LEGACY`（英文）经 `Pattern.quote` 交替匹配，写入中文前缀 + stripColor 物品名，下次匹配闭环；越界回退与 `add(Math.min(2, size))` 守卫在位 ✓
4. **MOB_DROP 反解析**（`RecipeType.resolveEntityType`）：全项目 MOB_DROP 配方仅 1 处（铁傀儡，在别名表内）；猪灵图标实际走 `BARTER_DROP`（不解析名字），别名属防御性冗余；`toUpperCase` 对中文无操作，别名键稳定匹配 ✓

**其它字符串匹配契约核查（全部同源自洽）**：
- 充能行（`ChargeUtils`）：`LORE_PREFIX` 与 `REGEX` 同源常量，语言无关格式 `N / M J` ✓
- 背包 ID 行：模板 `&7ID: <ID>` 与 `AbstractCraftingTable:93/104/121`、`PlayerProfile:795` 匹配侧全部保留英文 ✓
- 刷怪笼 `Type: ` 前缀：模板保留英文，`getEntityType` 匹配不受影响 ✓
- 祭坛探针前缀 `§dALTAR §3Probe - §e`：保留英文， AncientPedestal/ItemPickupListener 两侧同源 ✓
- 研究烟花名 `"Slimefun Research"`：`FireworkUtils:49`（发射）与 `FireworksListener:42`（免伤比较）同源保留英文 ✓
- 固定索引 lore 读取：全项目仅 `KnowledgeTome` get(0)/get(1)，模板行结构与读写索引自洽，老英文存档同结构兼容 ✓
- 无任何 `getTitle()` 字符串比较（GUI 标题纯显示层，6 处硬编码标题已汉化无副作用）✓

**lore-phrases 动态翻译基础设施（显示层安全性核实）**：
- `translateLore` 返回新列表、`getLocalizedItem` 经 `CustomItemStack.create` 构造克隆——只作用于指南显示，实际物品 lore 不被污染（`Type: `/`ID: ` 存储契约不受"类型：/编号："显示翻译影响）
- 空键守卫（防 `replace("", v)` 逐字符插入）、长短语优先排序、按语言缓存（ConcurrentHashMap）均在位
- 作弊模式 shift 点击给的是 `sfitem.getItem().clone()`（真实模板），显示克隆不流入玩家背包 ✓
- 全部 5 个调用点均在 SurvivalSlimefunGuide 显示层 ✓

**本地化资源完整性（脚本核对）**：
- 代码引用的 105 个消息键在 zh-CN（337 键）与 en（310 键）双侧全部存在，无缺键
- config.yml 默认 `options.language: zh-CN` ✓

**红线 1（老存档兼容）核实**：
- SF 物品经 PDC id 识别（r61 已深审），改名不影响
- 配方数组中仅有的 2 个 `CustomItemStack`（铁傀儡/猪灵）均为 4 号槽展示图标而非输入——**无任何靠显示名匹配的非 SF 配方输入**，不存在"汉化改名致老物品无法参与配方"的失配面

**发现并修复（1 项，表述与实现不符）**：
- `fix(i18n)`（0ff8acccc）：多功能工具模式切换消息的 `%device%` 硬编码英文 `"Multi Tool"`——与 5.1.0"运行时状态文本全部中文"表述不符，且无法区分 I/II/III 档位。改用 `getItemName()` 本地化名（stripColor），与 `%mode%` 处理一致

**回归**：全量 **3153 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21，33.6s）。

## 第 2 轮（2026-08-27）：新 API 可用性——研究系统数字 id 的持久化碰撞（2 项修复，其中 1 项 HIGH）

**背景**：目标要求把 5.0.0 新增 API 的可用性/稳定性作为审查重点。本轮从 `ResearchBuilder`（5.0.0 新增流式构建器）切入，核实其与存储层的交互。

**关键事实核实**：本 fork 的活跃存储实现仅有 `LegacyStorage`——研究解锁状态以**数字 id 为持久化键**（`researches.<id>`，读写均在 LegacyStorage:44/167）。因此数字 id 的唯一性是数据完整性的硬约束，而不仅是"遗留兼容字段"。

**发现并修复（2 项）**：

1. `fix(api)`（35cba9c72，HIGH）：**ResearchBuilder 默认 `id=0` 与持久化键碰撞**——内置首个研究 walking_sticks 正是 id 0（ResearchSetup:37），任何用 builder 不调 `id()` 的附属研究都会与它（以及彼此）共享 `researches.0`：解锁 walking_sticks（几乎人人解锁的第一个研究）即免费解锁所有默认 id 的附属研究，反向亦然。修复：未显式指定 id 时从 `NamespacedKey` 确定性派生（`floorMod(hashCode, 2^30)`，跨重启稳定、避开内置 0-8800 密集段）；`id()` Javadoc 更新。既有测试全部显式传 id，此默认路径此前零覆盖（漏检原因）
2. `fix(storage)`（e2d5f3d7b，HIGH，上游既有）：**内置 coal_generator 与 bio_reactor 共用 id 173**（upstream/experimental 同款，上游保存侧注释自认 hack 绕过）——解锁其一即双双永久解锁（14 级研究白送 18 级研究）。修复三件套：
   - bio_reactor 重编号 173→1730（静态核验 ResearchSetup 全部 id 唯一，仅此一处重复）
   - LegacyStorage 加载侧兼容：老存档 `researches.173` 存在时同时恢复两个研究（保留玩家既有进度，不丢解锁）；下次保存起各自独立落键
   - `Research.register()` 新增重复 id 告警——把静默数据污染变为启动可见（防附属再引入）
   - 保存侧防御分支保留（防未来任意重复 id），过时注释更新

**确认无问题**：
- `ResearchBuilder` 其余面：必填字段校验（key/name notNull、cost≥0）、LinkedHashSet 保序、build/register 分离语义正确；既有 10 项测试覆盖
- 研究依赖系统（addDependency/meetsDependencies/unlock 强制）：r9/r11 已深审，本轮无复发
- `SlimefunWeapon`/`SlimefunFood` 标记接口：纯标记无状态，TestSlimefunCombatEvents/TestSlimefunFoodEvents 已覆盖
- 保存侧清理分支（`anyMatch(getID()==)`）在重编号后对新旧数据均正确（测试第二段验证：解锁状态可独立撤销）

**测试**：新增 3 项（builder 派生 id 稳定且非 0/显式 id 保留、legacy 173 双恢复 + 独立撤销往返）；全量 **3156 项通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21，基线 3153→3156）。

**行为变更提示（发版须知）**：5.0.0-5.1.1 期间若有附属用 ResearchBuilder 默认 id 创建过研究，升级后其解锁状态按新派生 id 重新计算（旧 `researches.0` 状态本就与 walking_sticks 混淆，属污染数据）；coal_generator/bio_reactor 的既有解锁经兼容逻辑无损保留。

## 第 3 轮（2026-08-27）："数字 id 作持久化键"bug 类全项目推广清扫

**背景**：第 2 轮的两个 HIGH 均属"标识符默认值/重复值在持久化键上静默碰撞"。本轮把该 bug 类推广到全项目所有按数字 id 持久化的体系逐一核验。

**清扫结果（干净，第 2 轮两处为仅有实例）**：
- **背包 id**（PlayerProfile.createBackpack:397）：`max-key+1`（非 map.size，r68 已修），per-player 命名空间，lore 携带 `ID: <hash>#<id>` 读写两侧同源（第 1 轮已核）✓
- **机器人脚本 id**（ProgrammableAndroid.uploadScript:478）：`作者已传脚本数+1`——脚本体系 **append-only**（全代码库无删除路径），id 严格单调不碰撞；文件名嵌入作者 UUID（r14 已修路径穿越），跨作者命名空间隔离；同码防重传在 id 分配之前 ✓
- **传送点 id**（GPSNetwork）：字符串键、净化+空 id 拒绝（r12/r58 已审），非数字 id 体系 ✓

**回归**：本轮无代码改动；全量基线维持 **3156 项通过**（第 2 轮回归即本类闭合验证）。

## 第 4 轮（2026-08-27）：高频新事件 API 附属开发者视角端到端契约演练 + 1.21.11 兼容面复查

**范围**：目标要求把新 API 可用性作为审查重点。本轮对三族最高频的新事件（EnergyGenerate / CargoItemWithdraw|Insert / AndroidMine）以"附属注册真实监听器 → 生产触发路径 → 修改/取消路径落地"的方式演练既有覆盖与缺口，随后复查 1.21.11 符号兼容面与本循环修复的热路径归属。

**三族覆盖盘点与补齐**：
- **Cargo 族（既有覆盖完备，无缺口）**：TestCargoNetworkTaskItemFlow 8 场景全部经真实 routeItems 驱动——无监听器传输/取消退回源槽/setItem 替换分发物/setItem 空气=销毁/插入侧对称三场景/监听器事件中途拆源容器不吞没
- **Energy 族（补齐 setValue 修改路径，新增 3 项测试全绿）**：既有覆盖为契约测试 + 结算 cancel/异常/池化/回存（TestEnergyNetSettlement），缺"修改值流经真实结算"。新增 `TestEnergyGenerateModificationDrill`（经 pluginManager 注册真实监听器 + 反射驱动真实 tickAllGenerators/storeRemainingEnergy）：
  - 倍增监听器：可见值（发电 10 + 池化存储 50 = 60）×2 → 真实 supply=120；余量回存与重写语义在修改后依然成立
  - 归零监听器（setValue(0) 非取消）：supply=0 且发电机仍属被结算集合（区别于 cancel 的 unpooled 保留语义）——与 Javadoc"缩放贡献量"契约一致
  - setEnergy(-1) 拒绝（Validate ≥0）
- **AndroidMine 族（MockBukkit 限制，代码检视验证）**：MockBukkit 3.133.2 的 `BlockMock.getDrops(ItemStack)` 抛 UnimplementedOperationException（javap 实证），真实 dig() 演练不可行（与第 9/18 轮同类限制，按惯例转代码检视）。检视确认：MinerAndroid.dig/moveAndDig 触发事件后消费侧 `breakBlock(menu, event.getDrops(), block)` 直读监听器可变列表——修改必然生效；TestAndroidEvents 已覆盖字段暴露/可变性/分发可见性（dispatch 层）

**1.21.11 符号兼容面复查（干净）**：
- **版本门控**：`MINECRAFT_1_21(21, 0, "1.21.x")` 走三参构造 → `maxMinorVersion=-1`（无上界）；服务端 "1.21.11"（version=21, patch=11）经 `isMinecraftVersion` 正确匹配 MINECRAFT_1_21，所有 `isAtLeast(MINECRAFT_1_21)` 门控行为正确，**不会**降级 UNKNOWN（isAtLeast 对 UNKNOWN 恒 false 的隐患不适用）
- **compatibility 层**：7 个 Versioned* 助手全部为 `Registry.X.get(NamespacedKey.minecraft(key))` 键查找——1.21.x 稳定 API、条目缺失返回 null 由调用方处理
- 遗留已知项（README 已声明）：shaded dough/paperlib 内部符号的实机表现需上真实服务器观察，环境级风险不变

**热路径归属评估（免基准依据）**：本审计循环（第 1-4 轮）全部修复位于启动/加档/玩家消息路径——ResearchBuilder.build（附属启动期）、Research.register 重复扫描（启动期一次性 O(n²)，n≈250 即数万次 int 比较）、LegacyStorage.loadPlayerData（每次档案加载多一次已加载 Config 的 contains 查询）、MultiTool 消息（交互路径）。均不在 benchmark/ 覆盖的 tick 热路径（ticker-run/charge 写入/放置破坏/电容/全息）上，**无需重跑基准**。

**回归**：全量 **3159 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21，基线 3156→3159）。

## 第 5 轮（2026-08-27）：中频事件族（Research 解锁链 / Talisman 激活 / Backpack 开关-扩容）附属视角覆盖核验 + SlimefunLocalization 并发专项

**三族端到端覆盖盘点（全部完备，无需补测）**：
- **Research 解锁链**：TestResearchCostEvent 9 场景（扣费触发/取消=免费/setCost 调整落地/附加费超额拒解不扣/零费用=取消/解锁事件取消退还已扣等级/无监听器原行为）+ TestResearchUnlocking（时长调整端到端完成解锁）+ 依赖强制 3 场景（r11）+ 知识典籍分享（含失败退款）。全链闭合
- **Talisman 激活**：TestTalismanActivateEvent 5 场景——真实铁砧触发路径、字段正确性、取消、preventConsumption、可消耗护身符激活后真实库存移除（r19 实证 live mirror 持久化）
- **Backpack 开关-扩容**：TestBackpackEvents（PlayerBackpackOpen 经真实打开路径触发/取消阻止视图/关闭事件）+ TestBackpackResizeEvent 9 场景（扩容生效/取消保原/无监听器原行为/内容保留/观看者重定向/越界与破坏性缩小抛异常不触发）+ TestBackpackCreateEvent 6 场景（含修改尺寸落地）

**SlimefunLocalization 并发专项（结论：干净，无代码改动）**：
- **`languages` LinkedHashMap（非并发表）**：构造期（主线程 onEnable）经 loadEmbeddedLanguages 填充后**无任何运行时写入路径**（addLanguage 为 protected 且仅构造器调用；setLanguage 私有仅构造器）——启动后等效不可变；异步 ticker 线程经任务提交获得 happens-before 可见性。与 5.0.0 审计接受的 BlockStorage Config 并发读同一 doctrine
- **`lorePhraseCache`（5.1.0 新增，CHM<Language, List<String[]>>）**：短语列表在 `put` 前完成构建与排序——CHM put 即安全发布，读侧仅迭代不可变列表；并发首次访问的重复计算为良性（两侧等价，一方胜出）；键为 Language 身份语义（无 equals 重写），同一逻辑语言最多两个实例（构造器 defaultLanguage 与 languages 表项）各持等价缓存——条目数 ≤2×语言数，有界正确；短语源自 jar 内流加载、运行时不可变，无需失效机制
- **多语言切换路径**：主线程菜单点击 → PlayerLanguageChangeEvent（可否决/可重定向，r51 构造器三重校验在位）→ `setSelectedOption` 写玩家 PDC——共享本地化状态零写入；`getLanguage(Player)` 每次调用读 PDC，无缓存一致性窗口
- **记录（上游继承，不改）**：异步线程（如异步 ticker 事件监听器收 sendMessage）读玩家 PDC 与主线程语言切换写入存在理论竞态，最坏影响为单条消息使用旧语言——上游一贯设计，非本 fork 引入

**回归**：本轮无代码改动；全量基线维持 **3159 项通过**（本会话第 4 轮全量回归，工作树自该点仅新增测试与文档）。

## 第 6 轮（2026-08-27）：DirtyChestMenu/BlockMenu 多写入者槽位竞争专项 + GEO 供给并发复验（2 项修复，含 1 项实证并发丢更新）

**范围**：菜单体系的多写入者现实——异步机器 ticker（AContainer 等输出 pushItem/consumeItem）与主线程（玩家点击/cargo runSync/方块破坏 dropItems）操作同一 BlockMenu；GEO 供给 map 跨区块写入复验 r64 结论。

**发现并修复（2 项，提交 58061ef53）**：

1. **pushItem/consumeItem 丢更新窗口（并发实证）**：两个方法均为"读槽→改量→写回"的 check-then-act，无同步。8 线程同槽推入探针（200 轮）实证 **3 轮丢更新、共 11 个物品凭空消失**（两线程同时读到旧 amount，后写覆盖先写）。生产对应面：异步 ticker 推产出 vs 主线程 cargo/玩家交互同一机器菜单。修复：两方法加菜单实例级 `synchronized`——合并分支与 consumeItem 为纯菜单内操作零死锁面；null 槽分支的 `preset.onItemStackChange` 回调在锁内运行属已文档化取舍（默认实现纯函数；addon 不得在其内重入其它菜单，注释明示）。修复后探针 **0/200**。`replaceExistingItem` 保持不同步：其丢失窗口仅存于 null↔非 null 槽态跃迁的精确交错，且它是 addon 回调面，全面加锁的死锁/行为风险大于收益——记录为残留理论窗口
2. **pushItem 合并分支持久化缺失（数据丢失）**：合并分支原地 `stack.setAmount` 后**从不 markDirty**（r42"pushItem→replaceExistingItem→markDirty 链式完整"的结论只对空槽分支成立，予以纠正）——`BlockMenu.save` 以 `changes>0` 门控，菜单唯一改动为合并时永不重存，**重启丢物品**。受影响面：GEOMiner/收割机器人/砍树机器人等"只推产出不消耗输入"的机器（消耗型机器被同 tick 的 consumeItem markDirty 掩蔽）。修复：合并发生时补 `markDirty()`。新增确定性回归测试（模拟 save 后 changes=0，合并推入后必须 isDirty）

**GEO 供给并发专项（复验 r64 结论，成立）**：供给存储于 `BlockStorage` chunk-info——`SlimefunRegistry.chunks` 为 ConcurrentHashMap、`setChunkInfo` 经 get+putIfAbsent 原子建块（代码注释自证）、同区块多机器的读-改-写在单异步 ticker 线程序列化、跨区块为独立键无共享结构。无需改动。

**测试**：TestPushItem +2（合并置脏确定性断言、并发推入不腐蚀槽位弱不变量守卫）；探针为临时取证文件未入库（时序断言不可入库，r76 教训）。全量 **3161 项通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21，基线 3159→3161）。

## 第 7 轮（2026-08-27）：me.mrCookieSlime 遗留包系统性 synchronized 面复核 + 菜单同步化的基准回归（1 项修复 + 零性能回归实证）

**范围**：验证器指定的第 6 轮同类竞态推广——遗留包（BlockMenu/BlockMenuPreset/UniversalBlockMenu/BlockStorage.inventories）全部跨线程读写面复核；并因第 6 轮同步化触及 tick 热路径重跑 benchmark。

**复核结论（逐面）**：
- **发现并修复（1 项，提交 00f58ddd5）**：`BlockMenu.dropItems` 的读槽→掉落→清槽序列无同步——与异步 ticker 同槽 pushItem 构成 r6 同类竞态，窗口内推入的物品被清槽 `replaceExistingItem(null)` 静默虚空。修复：加菜单监视器同步（dropItems 全部调用面均主线程——破坏/爆炸/4 错终结均经 sync 调度，无死锁面；锁内含 world 掉落与 onItemStackChange 回调，取舍同 r6 注释）
- **其余面核验（干净）**：`UniversalBlockMenu` 继承已同步的 pushItem/consumeItem；块数据删除路径 r13 已加固（persistenceLock 下 markForFileDeletion/remove/dirty 三步原子，save 同锁排水）；`inventories` 为 CHM、moveLocationInfoUnsafely 的 put 新键原子；`BlockMenu.save` 异步快照撕裂为良性瞬态（changes 只减已写量，并发改动保持 dirty 下周期重存——r13 语义）；`ChestMenu.getItemInSlot` 单引用读原子；`BlockMenuPreset.newInstance` 仅主线程（放置事件）
- **记录（teardown 竞态，理论）**：异步 ticker 正在 tick 的机器被主线程注销（clearInventory→disableTicker 之间）——最后一个 tick 的推入落在已脱离表的对象上被 GC，单次微量产出损失，上游单循环删除语义既有行为，不改

**benchmark 基准回归（3 轮，JDK 21，对照 5.0.0-r73 基线）**——第 6/7 轮菜单同步化的直接受热场景为 `machine-processing`（活跃机器每 tick pushItem/consumeItem）：
- machine-processing active：929/795 ns（基线）→ **820/622/663 ns——零回归，反略优**（无竞争监视器获取 ~10ns 级，在 600-900ns 场景不可见）
- ticker-run 5000 块：2.28/1.20 ms → 0.81/0.83/0.81 ms（偏好）
- place/break-block、capacitor、hologram、energy-settlement：全部持平或偏好
- charge-write：196/225 ns（基线）→ 201/**317**/206 ns——r2 中位数为环境方差（r3 回落 206；该场景完全不触菜单代码，历史波带 ±30%，r74 期 4.9.4→5.0.0 亦有 245/212 先例）
- 结论：**全部场景零性能回归**。结果文件 `benchmark/report/results-r7-menu-sync-r{1,2,3}.txt`

**回归**：全量 **3161 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 8 轮（2026-08-27）：菜单监视器锁交互时序推演 + ChestMenu 基类写入面审查（结论：无锁序倒置，零代码改动）

**背景**：第 6/7 轮引入的 pushItem/consumeItem/dropItems 菜单监视器是全新锁面，须证明与既有锁体系（persistenceLock、ticker 调度）无环、与回调/事件无锁序倒置。

**锁交互推演（逐向核验，全部干净）**：
- **persistenceLock → 菜单监视器方向：不存在**。BlockStorage.save 的排水模式（:402）在 persistenceLock 内仅做快照并清空，随后**释放锁再序列化**；menu.save()（:499）在锁外调用且本身非 synchronized——该方向嵌套为零，环不成立
- **菜单监视器 → persistenceLock 方向：不存在**。监视器持有期间的调用面：getItemInSlot（live Inventory 读）、ItemStackWrapper/ItemUtils/CustomItemStack（纯物品操作）、markDirty（AtomicInteger）、replaceExistingItem → `preset.onItemStackChange`（**全项目零覆写**，基类默认纯返回 next——grep 实证仅 cargo 注释引用该钩子）、dropItems 的 world.dropItemNaturally（Bukkit 调用，见下）。无任何 BlockStorage 锁获取
- **TickerTask：跨机器 tick 不持锁**（全文件无 synchronized 块，"synchronized tick" 仅为 javadoc 措辞指主线程 tick）——机器 ticker→菜单操作链上无外层锁
- **EnergyNet 结算：零菜单引用**（grep 实证）——结算只读写 Config 充电数据，与菜单监视器无交集
- **锁内事件/回调面（已文档化取舍，复核成立）**：dropItems 监视器内的 dropItemNaturally 触发 ItemSpawnEvent、replaceExistingItem 调用 onItemStackChange——监听器/覆写代码在锁内运行。死锁需两线程构成 A→B 与 B→A 的跨菜单环：B→A 只能经"锁内回调再触其它菜单"，内置代码无此路径（零覆写+无菜单内触发事件再入菜单），仅理论存在于自设环的 addon——代码注释已警示
- **BlockMenu 构造即物化**：两个构造器尾部 `getContents()`（:29/:48）——inv 在构造线程建好后经 inventories CHM 发布，异步读者恒见非 null inv，基类 setup() 惰性创建竞态对 BlockMenu 不可达

**ChestMenu 基类（me.mrCookieSlime.CSCoreLibPlugin，fork 内源码非 shaded）写入面审查（记录，不改）**：
- `setup()` 惰性建库存的 check-then-act 无同步——BlockMenu 不可达（上述构造即物化）；裸 ChestMenu 由 addon 直接跨线程构造+首次触碰理论上可触发（一个库存被丢弃）——基类遗留行为，记录
- `items` ArrayList / `handlers` HashMap 仅构造期（preset init/clone）与主线程（MenuListener 点击分发）触碰，无跨线程运行时写
- 点击处理面（MenuListener→handler→replaceExistingItem 未同步）与异步 ticker pushItem 的交错为 r6 已记录的残留理论窗口（null↔非 null 槽态跃迁），本轮无新证据升级其风险等级

**回归**：本轮零代码改动；全量基线维持 **3161 项通过**（第 7 轮全量回归）。

## 第 9 轮（2026-08-27）：MenuListener.menus 与观看者生命周期并发面 + 命令层输入面复检（1 项修复）

**观看者生命周期逐面核查**：
- **断线清理 ✓**：玩家断线时服务端关闭其打开的库存并触发 InventoryCloseEvent → `onClose` 即时 `menus.remove`（r5 已证"关界面即清"，无泄漏）
- **连续开菜单次序 ✓**：`ChestMenu.open` 先 `openInventory`（同步触发旧菜单 close → remove）后 `put` 新菜单——新条目不会被旧 close 误删（r9 背包两段式同型正确次序）
- **发现并修复（1 项，提交 a48082b91）**：**UniversalBlockMenu 异步关闭观看者**——异步 ticker 删除队列 → `deleteLocationInfoUnsafely` destroy 分支 → `universalInventory.close()` → `DirtyChestMenu.close()` **直接在异步线程调 `human.closeInventory()`**。双重危害：① 异步触碰实体/库存状态（同文件 `clearInventory` :1253 早有 `runSync` 包裹并注释"Asynchronous entity add"原因，此路径漏包裹）；② 异步线程触发 InventoryCloseEvent → `MenuListener.onClose` 在异步线程 `menus.remove`，与主线程 `menus.put/get`（点击/开菜单）构成 **HashMap 跨线程访问**（结构损坏/幽灵条目——玩家后续点击被误路由到已死菜单）。修复：close 的观看者关闭改经 `Slimefun.runSync`（对齐 clearInventory 既有语义，destroy 路径的关闭延迟一 tick 属可接受统一）；`menus` 换 ConcurrentHashMap 作纵深防御（防任意第三方异步 close 再入）
- **记录（上游语义，不改）**：close handler 内同步再开菜单的重入递归为 addon 行为面；关服时表随 JVM 消亡非问题

**命令层不可信输入面复检（干净，引用既有轮次 + 变更史证明）**：自受审提交 ad5cd1277（r11 依赖多趟解锁）以来命令目录仅 `VersionsCommand` 12 行显示文本变更（git diff 证明），解析/门控逻辑零改动——r48（NUMERIC 正则、命令方块排除门、cheat 权限门、结果上限/crop）与 r81（GiveCommand 命令方块排除、溢出捕获、excess 落地配置）结论继续成立

**回归**：全量 **3161 项测试通过、0 失败、0 错误、7 跳过**（BUILD SUCCESS，Java 21）。

## 第 10 轮（2026-08-27）：destroy 路径时序复核（第 9 轮 close 改动影响面）+ lore-phrases 相关解析边界清扫扩展（结论：全部干净，零代码改动）

**destroy 路径时序（第 9 轮 close 延迟一 tick 的新交互，逐路径推演）**：
- **保存/计数器一致性**：`BlockMenu.save` 与 `UniversalBlockMenu.save` 同款门控——`changes.get()` 快照 → 写槽 → `addAndGet(-快照)` 只减已计数写入；窗口内并发 markDirty（含 r6 新增的合并分支置脏）的增量在减法后存活 → 菜单保持 dirty 下周期重存。保存槽位快照与监视器持有者的交错撕裂为已接受的良性瞬态（r7/r13 doctrine，下周期覆盖）
- **universal 菜单 destroy 窗口**：`deleteLocationInfoUnsafely` destroy 分支 close（现延迟一 tick）→ save（异步立即）。universal 菜单按 preset id 驻留注册表、不随方块删除移除——观看者在窗口内的点击 markDirty 存活计数 → 下一保存周期落盘，**零丢失**
- **clearInventory 对照**：其"延迟关闭（r13 起既有 runSync 模式）→ 删文件 → 移表"次序未变；第 9 轮改动只是让 universal 路径对齐这一既有模式。窗口内观看者点击取出物品属合法取回（等价于破坏掉落语义）
- **监视器交互**：save 不获取菜单监视器（r8 已证双向嵌套不存在），新时序未引入锁面

**lore-phrases 解析边界清扫扩展（r55 第十一个清扫在 5.1.0 后的扩展）**：
- **模板不可污染（关键证明）**：`SlimefunItem.getItem()` 返回 `itemStackTemplate.clone()`——`getLocalizedItem` 的 `CustomItemStack.create(item.getItem(), meta->...)` 操作新鲜克隆，显示翻译（含 "Type: "→"类型： "）**不可能**写入共享模板，契约行不可能经显示层进入真实物品；作弊模式发放 `getItem().clone()` 同为模板克隆（r1 已证）
- **翻译 lore 无回流解析面**：`translateLore`/`getLocalizedItem` 全部 5 个调用点在指南显示层（r1 盘点），无任何代码解析翻译后的 lore
- **5.1.0 新增解析面复核**：灵魂绑定双行/USES_LEFT 双语正则/MultiTool 双前缀（r1 已证同源）；`getLorePhrases` 空键守卫 + 长短语优先 + 按语言缓存（r5 并发专项已证安全发布）；管理员误配 phrases 仅影响显示层
- KnowledgeTome 固定索引读取（唯一 index-based lore 读）r1 已证模板自洽

**回归**：本轮零代码改动；全量基线维持 **3161 项通过**（第 9 轮全量回归）。

## 第 11 轮（2026-08-27）：integrations/ 软依赖适配层 1.21.11 符号面 + guide 搜索/历史内存面（结论：全部干净，零代码改动）

**integrations/ 适配层核查（r43"fork 零改动"结论换符号兼容视角复验）**：
- **注册门控结构天然韧化**：`IntegrationsManager.load()` 对每个软依赖做"插件在场 + isEnabled"检查，回调整体 `catch (Exception | LinkageError)` 并带版本号记日志——第三方插件 API 变更（NoSuchMethodError/NoClassDefFoundError）最坏降级为一条 WARNING + 跳过该集成，**不会崩插件**；缺失即回调不执行（干净降级）。ProtectionManager 装载同样 try/catch 包裹
- **逐适配类调用面**：PlaceholderAPI（expansion 注册与 placeholder 解析，PAPI 自身 API 与 MC 版本解耦）；WorldEdit（构造器 `Class.forName("...extent.Extent")` 显式版本守卫，缺失时转为 IllegalStateException 交由 load() 捕获——设计良好）；mcMMO（事件监听 + SkillUtils 调用）；ClearLag（EntityRemoveEvent 监听保展示物品）；Orebfuscator（ServicesManager 服务查找——`getRegistration(...).getProvider()` 对"插件在场但服务未注册"会 NPE，但发生在 load() 的 catch 内，降级为警告，记录为上游可接受行为）；ItemsAdder 仅标记位
- **1.21.11 面**：适配类的 Bukkit 侧符号（事件/ServicesManager/PluginManager）均为 1.21.x 稳定 API；第三方符号与 MC 版本解耦（随各插件版本演进，运行时不兼容由 LinkageError 兜底捕获）
- **反射面**：全层仅 WorldEditIntegration 一处 Class.forName，且为守卫用途（上述）

**guide 搜索/GuideHistory 内存面（高频使用视角）**：
- **GuideHistory 有界**：`MAX_HISTORY_SIZE=200` 逐出最旧（r48 引入）；条目多为 SlimefunItem/ItemGroup **单例引用**（零拷贝）与搜索词（≤聊天协议 256 字符）；最坏情形全为配方展示 ItemStack 克隆约 200KB/玩家，且随 PlayerProfile 卸载（quit → markedForDeletion → auto-save 收口）整体释放——有界 + 生命周期受限
- **搜索路径**：结果 44 条上限 + 标题 crop 截断 + 字面 contains（r48 已证无 ReDoS/OOM 面），`refresh` 对同对象连续翻页仅更新页码不新增条目

**回归**：本轮零代码改动；全量基线维持 **3161 项通过**（第 9 轮全量回归）。

## 第 12 轮（2026-08-27）：SlimefunProfiler 按需收集开销 + 盔甲周期异步任务时序/取消语义（结论：全部干净，零代码改动）

**Profiler 专项（长时高负载自身开销视角）**：
- **空闲路径成本**：`newEntry()` 一次 volatile 读短路返回 0；`closeEntry` 两次 notNull + timestamp==0 短路；`endTick` volatile 写——该成本已含在基准 ticker-run（0.8ms/5000 块）内（4.9.5 优化产物）
- **字段并发正确**：`isProfiling`/`lastTickTotalElapsed` volatile、`queued` AtomicInteger、`timings` CHM、`requests` ConcurrentLinkedQueue；`start()` 仅在有请求时开启逐块收集并清表
- **finishReport 有界**：等待循环上限 4000×1ms（4s），超时向请求者发送诚实的超时消息并排水请求——请求不会滞留堆积；下一周期已开始（isProfiling && queued>0）时中止防跨周期污染；重复 /sf timings 每次新建请求、服务即移除
- **closeEntry 合并**：无人查看时在调用线程就地 merge（免 lambda 分配与 executor 往返）；有人查看时经 executor（多线程池，阻塞其一可接受——代码注释自证）
- **kill()** 关停 executor；DebugFish 查单块耗时需先 /sf timings 的诊断工作流变化在 4.9.5 发布说明中已声明——表述相符

**盔甲三任务 + SolarHelmet（调度/取消/重入语义）**：
- **间隔钳制**：`tickInterval < 1` 钳 1 并 WARNING（0/负配置不产生零周期任务风暴）
- **逐玩家隔离**：`run()` 对每玩家 `catch (Exception | LinkageError)`——注释明示未捕获异常会取消重复任务并永久静默禁用全部盔甲处理（此防线在位）
- **关服取消**：onDisable `cancelTasks` 全量覆盖
- **条件门控**：radiation 嵌套于 enable-armor-effects 之下，冲突时 WARNING 提示（表述与行为一致）
- **SolarHelmet 细粒度间隔（记录，上游同款）**：与盔甲任务共用 `armor-update-interval` 但不带 ×20（10 tick vs 200 tick）——upstream/experimental 逐字相同，充电速率按此间隔标定，上游有意设计非缺陷

**回归**：本轮零代码改动；全量基线维持 **3161 项通过**（第 9 轮全量回归）。
